diff --git a/clang/include/clang/Basic/Attr.td b/clang/include/clang/Basic/Attr.td
index d874ccb1b..c88803832 100644
--- a/clang/include/clang/Basic/Attr.td
+++ b/clang/include/clang/Basic/Attr.td
@@ -481,6 +481,7 @@ def TargetAMDGPU : TargetArch<["amdgpu", "r600"]>;
 def TargetAnyArm : TargetArch<!listconcat(TargetARM.Arches, TargetAArch64.Arches)>;
 def TargetAVR : TargetArch<["avr"]>;
 def TargetBPF : TargetArch<["bpfel", "bpfeb"]>;
+def TargetEclipse : TargetArch<["eclipse"]>;
 def TargetLoongArch : TargetArch<["loongarch32", "loongarch64"]>;
 def TargetMips32 : TargetArch<["mips", "mipsel"]>;
 def TargetAnyMips : TargetArch<["mips", "mipsel", "mips64", "mips64el"]>;
@@ -1093,6 +1094,13 @@ def AVRSignal : InheritableAttr, TargetSpecificAttr<TargetAVR> {
   let Documentation = [AVRSignalDocs];
 }
 
+def EclipseInterrupt : InheritableAttr, TargetSpecificAttr<TargetEclipse> {
+  let Spellings = [GCC<"interrupt">];
+  let Subjects = SubjectList<[Function]>;
+  let ParseKind = "Interrupt";
+  let Documentation = [EclipseInterruptDocs];
+}
+
 def AsmLabel : InheritableAttr {
   let Spellings = [CustomKeyword<"asm">, CustomKeyword<"__asm">, CustomKeyword<"__asm__">];
   let Args = [
diff --git a/clang/include/clang/Basic/AttrDocs.td b/clang/include/clang/Basic/AttrDocs.td
index 3b469437d..dc0b9fc56 100644
--- a/clang/include/clang/Basic/AttrDocs.td
+++ b/clang/include/clang/Basic/AttrDocs.td
@@ -3319,6 +3319,24 @@ Interrupt handler functions defined with the signal attribute do not re-enable i
 }];
 }
 
+def EclipseInterruptDocs : Documentation {
+  let Category = DocCatFunction;
+  let Heading = "interrupt (Eclipse)";
+  let Content = [{
+Clang supports the GNU style ``__attribute__((interrupt))`` attribute on
+Eclipse S/140 targets. This attribute may be attached to a function
+definition and instructs the backend to generate entry/exit code matching
+the real hardware interrupt convention (saving every register the handler
+body touches, and returning via the interrupt-specific return sequence
+rather than an ordinary function return) so it can be used directly as the
+target of the interrupt vector at memory location 1.
+
+A function with this attribute must return ``void`` and take no parameters,
+matching the fact that hardware interrupt entry passes no arguments and
+uses no return value.
+  }];
+}
+
 def TargetDocs : Documentation {
   let Category = DocCatFunction;
   let Content = [{
diff --git a/clang/include/clang/Basic/DiagnosticSemaKinds.td b/clang/include/clang/Basic/DiagnosticSemaKinds.td
index b57ff6c19..35a5624c9 100644
--- a/clang/include/clang/Basic/DiagnosticSemaKinds.td
+++ b/clang/include/clang/Basic/DiagnosticSemaKinds.td
@@ -436,7 +436,8 @@ def warn_arm_interrupt_save_fp_without_vfp_unit : Warning<
 def err_arm_interrupt_called : Error<
   "interrupt service routine cannot be called directly">;
 def warn_interrupt_signal_attribute_invalid
-    : Warning<"%select{MIPS|MSP430|RISC-V|AVR}0 '%select{interrupt|signal}1' "
+    : Warning<"%select{MIPS|MSP430|RISC-V|AVR|ECLIPSE}0 "
+              "'%select{interrupt|signal}1' "
               "attribute only applies to functions that have "
               "%select{no parameters|a 'void' return type}2">,
       InGroup<IgnoredAttributes>,
diff --git a/clang/include/clang/Sema/Sema.h b/clang/include/clang/Sema/Sema.h
index 778c1a2f5..f728b0e78 100644
--- a/clang/include/clang/Sema/Sema.h
+++ b/clang/include/clang/Sema/Sema.h
@@ -164,6 +164,7 @@ class SemaBPF;
 class SemaCodeCompletion;
 class SemaCUDA;
 class SemaDirectX;
+class SemaEclipse;
 class SemaHLSL;
 class SemaHexagon;
 class SemaLoongArch;
@@ -1484,6 +1485,11 @@ public:
     return *DirectXPtr;
   }
 
+  SemaEclipse &Eclipse() {
+    assert(EclipsePtr);
+    return *EclipsePtr;
+  }
+
   SemaHLSL &HLSL() {
     assert(HLSLPtr);
     return *HLSLPtr;
@@ -1623,6 +1629,7 @@ private:
   std::unique_ptr<SemaCodeCompletion> CodeCompletionPtr;
   std::unique_ptr<SemaCUDA> CUDAPtr;
   std::unique_ptr<SemaDirectX> DirectXPtr;
+  std::unique_ptr<SemaEclipse> EclipsePtr;
   std::unique_ptr<SemaHLSL> HLSLPtr;
   std::unique_ptr<SemaHexagon> HexagonPtr;
   std::unique_ptr<SemaLoongArch> LoongArchPtr;
diff --git a/clang/include/clang/Sema/SemaEclipse.h b/clang/include/clang/Sema/SemaEclipse.h
new file mode 100644
index 000000000..eb5ac0246
--- /dev/null
+++ b/clang/include/clang/Sema/SemaEclipse.h
@@ -0,0 +1,32 @@
+//===----- SemaEclipse.h --- Eclipse target-specific routines -*- C++ -*---===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+/// \file
+/// This file declares semantic analysis functions specific to the Eclipse
+/// S/140 target.
+///
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_CLANG_SEMA_SEMAECLIPSE_H
+#define LLVM_CLANG_SEMA_SEMAECLIPSE_H
+
+#include "clang/AST/ASTFwd.h"
+#include "clang/Sema/SemaBase.h"
+
+namespace clang {
+class ParsedAttr;
+
+class SemaEclipse : public SemaBase {
+public:
+  SemaEclipse(Sema &S);
+
+  void handleInterruptAttr(Decl *D, const ParsedAttr &AL);
+};
+
+} // namespace clang
+
+#endif // LLVM_CLANG_SEMA_SEMAECLIPSE_H
diff --git a/clang/lib/Basic/CMakeLists.txt b/clang/lib/Basic/CMakeLists.txt
index 6329c3fa8..1726cea0d 100644
--- a/clang/lib/Basic/CMakeLists.txt
+++ b/clang/lib/Basic/CMakeLists.txt
@@ -103,6 +103,7 @@ add_clang_library(clangBasic
   Targets/BPF.cpp
   Targets/CSKY.cpp
   Targets/DirectX.cpp
+  Targets/Eclipse.cpp
   Targets/Hexagon.cpp
   Targets/Lanai.cpp
   Targets/LoongArch.cpp
diff --git a/clang/lib/Basic/Targets.cpp b/clang/lib/Basic/Targets.cpp
index 8c01cfca8..e8a28185f 100644
--- a/clang/lib/Basic/Targets.cpp
+++ b/clang/lib/Basic/Targets.cpp
@@ -21,6 +21,7 @@
 #include "Targets/BPF.h"
 #include "Targets/CSKY.h"
 #include "Targets/DirectX.h"
+#include "Targets/Eclipse.h"
 #include "Targets/Hexagon.h"
 #include "Targets/Lanai.h"
 #include "Targets/LoongArch.h"
@@ -135,6 +136,9 @@ std::unique_ptr<TargetInfo> AllocateTarget(const llvm::Triple &Triple,
   case llvm::Triple::lanai:
     return std::make_unique<LanaiTargetInfo>(Triple, Opts);
 
+  case llvm::Triple::eclipse:
+    return std::make_unique<EclipseTargetInfo>(Triple, Opts);
+
   case llvm::Triple::aarch64_32:
     if (Triple.isOSDarwin())
       return std::make_unique<DarwinAArch64TargetInfo>(Triple, Opts);
diff --git a/clang/lib/Basic/Targets/Eclipse.cpp b/clang/lib/Basic/Targets/Eclipse.cpp
new file mode 100644
index 000000000..3c7f03129
--- /dev/null
+++ b/clang/lib/Basic/Targets/Eclipse.cpp
@@ -0,0 +1,25 @@
+//===--- Eclipse.cpp - Implement Eclipse target feature support ---------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "Eclipse.h"
+#include "clang/Basic/MacroBuilder.h"
+
+using namespace clang;
+using namespace clang::targets;
+
+const char *const EclipseTargetInfo::GCCRegNames[] = {"0", "1", "2", "3"};
+
+ArrayRef<const char *> EclipseTargetInfo::getGCCRegNames() const {
+  return llvm::ArrayRef(GCCRegNames);
+}
+
+void EclipseTargetInfo::getTargetDefines(const LangOptions &Opts,
+                                          MacroBuilder &Builder) const {
+  Builder.defineMacro("__ECLIPSE__");
+  Builder.defineMacro("__eclipse__");
+}
diff --git a/clang/lib/Basic/Targets/Eclipse.h b/clang/lib/Basic/Targets/Eclipse.h
new file mode 100644
index 000000000..0307994e6
--- /dev/null
+++ b/clang/lib/Basic/Targets/Eclipse.h
@@ -0,0 +1,135 @@
+//===--- Eclipse.h - Declare Eclipse target feature support -----*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// TargetInfo for the Data General Nova/Eclipse (16-bit) target. Frontend
+// type sizes here must stay in lockstep with llvm/lib/Target/Eclipse's own
+// choices. `char` is the usual 8 bits (Clang doesn't allow a target to
+// customize that), even though the real machine has no byte-addressing
+// instructions — the backend handles this the ordinary way any
+// wider-than-8-bit-native target does: i8 values are promoted to i16 (see
+// EclipseISelLowering.cpp's Promote actions), so `char` physically occupies
+// a full word in a register or stack slot, matching the sibling `eccc`
+// project's own "char is stored one per 16-bit word" convention at the
+// storage-layout level even though the IR type is still formally i8.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_CLANG_LIB_BASIC_TARGETS_ECLIPSE_H
+#define LLVM_CLANG_LIB_BASIC_TARGETS_ECLIPSE_H
+
+#include "clang/Basic/TargetInfo.h"
+#include "clang/Basic/TargetOptions.h"
+#include "llvm/ADT/APFloat.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/TargetParser/Triple.h"
+
+namespace clang {
+namespace targets {
+
+class LLVM_LIBRARY_VISIBILITY EclipseTargetInfo : public TargetInfo {
+  static const char *const GCCRegNames[];
+
+public:
+  EclipseTargetInfo(const llvm::Triple &Triple, const TargetOptions &)
+      : TargetInfo(Triple) {
+    TLSSupported = false;
+    NoAsmVariants = true;
+
+    // `char` is not a customizable width in this Clang version — it's
+    // always 8 bits, so this target's LLVM backend has to actually
+    // support i8 (promoted to i16), not lean on a nonstandard char size
+    // the way the earlier design comment here assumed. See
+    // EclipseISelLowering.cpp's i8/i1 Promote actions.
+    BoolWidth = BoolAlign = 16;
+    ShortWidth = ShortAlign = 16;
+    IntWidth = IntAlign = 16;
+    // No genuine 64-bit integer type is supported by the backend yet
+    // (see lib/Target/Eclipse/README.md "Known limitations") — `long
+    // long` is given the same width as `long` rather than silently
+    // producing i64 IR the backend can't legalize into anything correct.
+    LongWidth = LongLongWidth = 32;
+    LongAlign = LongLongAlign = 16;
+
+    // No hand-verified Eclipse FPU opcodes exist yet either (same
+    // README section) — float relies on the backend's soft-float
+    // (libcall) runtime rather than real hardware FP instructions.
+    FloatWidth = 32;
+    FloatAlign = 16;
+
+    // `double` is a 32-bit IEEE-754 *single*-precision alias for `float`
+    // on this target — the same choice AVR-GCC/AVR-Clang made (see
+    // clang/lib/Basic/Targets/AVR.h) for the same underlying reason: a
+    // real 64-bit `double` needs genuine 64-bit integer arithmetic to
+    // implement (mantissa shifts/adds/etc.), which this backend doesn't
+    // have (see README.md "Known limitations"), while the existing f32
+    // soft-float runtime (eclipse_rt.c) is already verified working.
+    // Setting DoubleFormat to IEEEsingle (not just DoubleWidth=32) is
+    // what actually makes Clang emit LLVM IR type `float` for C `double`
+    // (ASTContext::getFloatTypeSemantics drives CodeGenTypes::ConvertType)
+    // — without it, `double` would still be legalized as a 64-bit value
+    // this backend can't lower. `long double` is aliased the same way,
+    // matching AVR's precedent again.
+    DoubleWidth = LongDoubleWidth = 32;
+    DoubleAlign = LongDoubleAlign = 16;
+    DoubleFormat = LongDoubleFormat = &llvm::APFloat::IEEEsingle();
+
+    PointerWidth = PointerAlign = 16;
+    SuitableAlign = 16;
+
+    SizeType = UnsignedInt;
+    IntPtrType = SignedInt;
+    PtrDiffType = SignedInt;
+    IntMaxType = SignedLong;
+    SigAtomicType = SignedInt;
+
+    // Must match EclipseTargetMachine.cpp's computeDataLayout() exactly.
+    resetDataLayout("E-p:16:16-i16:16-i32:16-f32:16-f64:16-n16");
+  }
+
+  void getTargetDefines(const LangOptions &Opts,
+                         MacroBuilder &Builder) const override;
+
+  llvm::SmallVector<Builtin::InfosShard> getTargetBuiltins() const override {
+    return {};
+  }
+
+  bool allowsLargerPreferedTypeAlignment() const override { return false; }
+
+  bool hasFeature(StringRef Feature) const override {
+    return Feature == "eclipse";
+  }
+
+  ArrayRef<const char *> getGCCRegNames() const override;
+
+  ArrayRef<TargetInfo::GCCRegAlias> getGCCRegAliases() const override {
+    static const TargetInfo::GCCRegAlias GCCRegAliases[] = {
+        {{"ac0"}, "0"}, {{"ac1"}, "1"}, {{"ac2", "fp"}, "2"},
+        {{"ac3", "lr"}, "3"},
+    };
+    return llvm::ArrayRef(GCCRegAliases);
+  }
+
+  bool validateAsmConstraint(const char *&Name,
+                              TargetInfo::ConstraintInfo &) const override {
+    // No target-specific inline-asm constraints yet.
+    return false;
+  }
+
+  std::string_view getClobbers() const override { return ""; }
+
+  // `va_list` is just a plain pointer into the flat software stack (see
+  // EclipseISelLowering.cpp's LowerVASTART/LowerVAARG) — verified working
+  // end to end via eclipse_rt.c's printf/scanf.
+  BuiltinVaListKind getBuiltinVaListKind() const override {
+    return TargetInfo::CharPtrBuiltinVaList;
+  }
+};
+
+} // namespace targets
+} // namespace clang
+#endif // LLVM_CLANG_LIB_BASIC_TARGETS_ECLIPSE_H
diff --git a/clang/lib/CodeGen/CMakeLists.txt b/clang/lib/CodeGen/CMakeLists.txt
index 853f038c4..0ab441586 100644
--- a/clang/lib/CodeGen/CMakeLists.txt
+++ b/clang/lib/CodeGen/CMakeLists.txt
@@ -143,6 +143,7 @@ add_clang_library(clangCodeGen
   Targets/BPF.cpp
   Targets/CSKY.cpp
   Targets/DirectX.cpp
+  Targets/Eclipse.cpp
   Targets/Hexagon.cpp
   Targets/Lanai.cpp
   Targets/LoongArch.cpp
diff --git a/clang/lib/CodeGen/CodeGenModule.cpp b/clang/lib/CodeGen/CodeGenModule.cpp
index 9ce77917f..8845ce172 100644
--- a/clang/lib/CodeGen/CodeGenModule.cpp
+++ b/clang/lib/CodeGen/CodeGenModule.cpp
@@ -289,6 +289,8 @@ createTargetCodeGenInfo(CodeGenModule &CGM) {
     return createHexagonTargetCodeGenInfo(CGM);
   case llvm::Triple::lanai:
     return createLanaiTargetCodeGenInfo(CGM);
+  case llvm::Triple::eclipse:
+    return createEclipseTargetCodeGenInfo(CGM);
   case llvm::Triple::r600:
     return createAMDGPUTargetCodeGenInfo(CGM);
   case llvm::Triple::amdgpu:
diff --git a/clang/lib/CodeGen/TargetInfo.h b/clang/lib/CodeGen/TargetInfo.h
index fae32068b..d4449a19f 100644
--- a/clang/lib/CodeGen/TargetInfo.h
+++ b/clang/lib/CodeGen/TargetInfo.h
@@ -543,6 +543,9 @@ createBPFTargetCodeGenInfo(CodeGenModule &CGM);
 std::unique_ptr<TargetCodeGenInfo>
 createCSKYTargetCodeGenInfo(CodeGenModule &CGM, unsigned FLen);
 
+std::unique_ptr<TargetCodeGenInfo>
+createEclipseTargetCodeGenInfo(CodeGenModule &CGM);
+
 std::unique_ptr<TargetCodeGenInfo>
 createHexagonTargetCodeGenInfo(CodeGenModule &CGM);
 
diff --git a/clang/lib/CodeGen/Targets/Eclipse.cpp b/clang/lib/CodeGen/Targets/Eclipse.cpp
new file mode 100644
index 000000000..3c79c9498
--- /dev/null
+++ b/clang/lib/CodeGen/Targets/Eclipse.cpp
@@ -0,0 +1,50 @@
+//===- Eclipse.cpp --------------------------------------------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// ABI lowering for the Data General Nova/Eclipse target. There's no
+// per-target struct/complex-type classification here — every C-level
+// argument and return value is just whatever DefaultABIInfo's generic
+// size-based classification decides (direct for small scalars, indirect
+// for large aggregates), matching how lib/Target/Eclipse's own calling
+// convention has no argument registers to allocate specially in the
+// first place (every argument is pushed on the software stack — see
+// docs/CALLING_CONVENTION.md in the sibling eccc project).
+//
+//===----------------------------------------------------------------------===//
+
+#include "ABIInfoImpl.h"
+#include "TargetInfo.h"
+
+using namespace clang;
+using namespace clang::CodeGen;
+
+namespace {
+class EclipseTargetCodeGenInfo : public TargetCodeGenInfo {
+public:
+  EclipseTargetCodeGenInfo(CodeGenTypes &CGT)
+      : TargetCodeGenInfo(std::make_unique<DefaultABIInfo>(CGT)) {}
+
+  void setTargetAttributes(const Decl *D, llvm::GlobalValue *GV,
+                            CodeGen::CodeGenModule &CGM) const override {
+    if (GV->isDeclaration())
+      return;
+    const auto *FD = dyn_cast_or_null<FunctionDecl>(D);
+    if (!FD)
+      return;
+    auto *Fn = cast<llvm::Function>(GV);
+
+    if (FD->getAttr<EclipseInterruptAttr>())
+      Fn->addFnAttr("interrupt");
+  }
+};
+} // namespace
+
+std::unique_ptr<TargetCodeGenInfo>
+CodeGen::createEclipseTargetCodeGenInfo(CodeGenModule &CGM) {
+  return std::make_unique<EclipseTargetCodeGenInfo>(CGM.getTypes());
+}
diff --git a/clang/lib/Sema/CMakeLists.txt b/clang/lib/Sema/CMakeLists.txt
index 88f0c9938..f1eafeaad 100644
--- a/clang/lib/Sema/CMakeLists.txt
+++ b/clang/lib/Sema/CMakeLists.txt
@@ -52,6 +52,7 @@ add_clang_library(clangSema
   SemaDeclAttr.cpp
   SemaDeclCXX.cpp
   SemaDeclObjC.cpp
+  SemaEclipse.cpp
   SemaExceptionSpec.cpp
   SemaExpand.cpp
   SemaExpr.cpp
diff --git a/clang/lib/Sema/Sema.cpp b/clang/lib/Sema/Sema.cpp
index 0f291900d..7058d8f2e 100644
--- a/clang/lib/Sema/Sema.cpp
+++ b/clang/lib/Sema/Sema.cpp
@@ -49,6 +49,7 @@
 #include "clang/Sema/SemaCodeCompletion.h"
 #include "clang/Sema/SemaConsumer.h"
 #include "clang/Sema/SemaDirectX.h"
+#include "clang/Sema/SemaEclipse.h"
 #include "clang/Sema/SemaHLSL.h"
 #include "clang/Sema/SemaHexagon.h"
 #include "clang/Sema/SemaLoongArch.h"
@@ -291,6 +292,7 @@ Sema::Sema(Preprocessor &pp, ASTContext &ctxt, ASTConsumer &consumer,
           std::make_unique<SemaCodeCompletion>(*this, CodeCompleter)),
       CUDAPtr(std::make_unique<SemaCUDA>(*this)),
       DirectXPtr(std::make_unique<SemaDirectX>(*this)),
+      EclipsePtr(std::make_unique<SemaEclipse>(*this)),
       HLSLPtr(std::make_unique<SemaHLSL>(*this)),
       HexagonPtr(std::make_unique<SemaHexagon>(*this)),
       LoongArchPtr(std::make_unique<SemaLoongArch>(*this)),
diff --git a/clang/lib/Sema/SemaDeclAttr.cpp b/clang/lib/Sema/SemaDeclAttr.cpp
index 0645f9949..75da18bff 100644
--- a/clang/lib/Sema/SemaDeclAttr.cpp
+++ b/clang/lib/Sema/SemaDeclAttr.cpp
@@ -48,6 +48,7 @@
 #include "clang/Sema/SemaAVR.h"
 #include "clang/Sema/SemaBPF.h"
 #include "clang/Sema/SemaCUDA.h"
+#include "clang/Sema/SemaEclipse.h"
 #include "clang/Sema/SemaHLSL.h"
 #include "clang/Sema/SemaInternal.h"
 #include "clang/Sema/SemaM68k.h"
@@ -6686,6 +6687,9 @@ static void handleInterruptAttr(Sema &S, Decl *D, const ParsedAttr &AL) {
   case llvm::Triple::avr:
     S.AVR().handleInterruptAttr(D, AL);
     break;
+  case llvm::Triple::eclipse:
+    S.Eclipse().handleInterruptAttr(D, AL);
+    break;
   case llvm::Triple::riscv32:
   case llvm::Triple::riscv64:
   case llvm::Triple::riscv32be:
diff --git a/clang/lib/Sema/SemaEclipse.cpp b/clang/lib/Sema/SemaEclipse.cpp
new file mode 100644
index 000000000..ab7966cb0
--- /dev/null
+++ b/clang/lib/Sema/SemaEclipse.cpp
@@ -0,0 +1,50 @@
+//===------ SemaEclipse.cpp ------ Eclipse target-specific routines -----===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+//  This file implements semantic analysis functions specific to the Eclipse
+//  S/140 target.
+//
+//===----------------------------------------------------------------------===//
+
+#include "clang/Sema/SemaEclipse.h"
+#include "clang/AST/DeclBase.h"
+#include "clang/Basic/DiagnosticSema.h"
+#include "clang/Sema/Attr.h"
+#include "clang/Sema/ParsedAttr.h"
+#include "clang/Sema/Sema.h"
+
+namespace clang {
+SemaEclipse::SemaEclipse(Sema &S) : SemaBase(S) {}
+
+void SemaEclipse::handleInterruptAttr(Decl *D, const ParsedAttr &AL) {
+  if (!isFuncOrMethodForAttrSubject(D)) {
+    Diag(D->getLocation(), diag::warn_attribute_wrong_decl_type)
+        << AL << AL.isRegularKeywordAttribute() << ExpectedFunction;
+    return;
+  }
+
+  if (!AL.checkExactlyNumArgs(SemaRef, 0))
+    return;
+
+  // Real hardware interrupt entry passes no arguments and has no return
+  // value slot, so a handler must be `void handler(void)`.
+  if (hasFunctionProto(D) && getFunctionOrMethodNumParams(D) != 0) {
+    Diag(D->getLocation(), diag::warn_interrupt_signal_attribute_invalid)
+        << /*ECLIPSE*/ 4 << /*interrupt*/ 0 << 0;
+    return;
+  }
+  if (!getFunctionOrMethodResultType(D)->isVoidType()) {
+    Diag(D->getLocation(), diag::warn_interrupt_signal_attribute_invalid)
+        << /*ECLIPSE*/ 4 << /*interrupt*/ 0 << 1;
+    return;
+  }
+
+  handleSimpleAttribute<EclipseInterruptAttr>(*this, D, AL);
+}
+
+} // namespace clang
diff --git a/llvm/include/llvm/TargetParser/Triple.h b/llvm/include/llvm/TargetParser/Triple.h
index 18634a8bb..8a7612ff1 100644
--- a/llvm/include/llvm/TargetParser/Triple.h
+++ b/llvm/include/llvm/TargetParser/Triple.h
@@ -115,7 +115,8 @@ public:
     renderscript32, // 32-bit RenderScript
     renderscript64, // 64-bit RenderScript
     ve,             // NEC SX-Aurora Vector Engine
-    LastArchType = ve
+    eclipse,        // Data General Nova/Eclipse (16-bit)
+    LastArchType = eclipse
   };
   enum SubArchType {
     NoSubArch,
diff --git a/llvm/lib/Target/Eclipse/CMakeLists.txt b/llvm/lib/Target/Eclipse/CMakeLists.txt
new file mode 100644
index 000000000..fa97a7a49
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/CMakeLists.txt
@@ -0,0 +1,43 @@
+add_llvm_component_group(Eclipse)
+
+set(LLVM_TARGET_DEFINITIONS Eclipse.td)
+
+tablegen(LLVM EclipseGenAsmWriter.inc -gen-asm-writer)
+tablegen(LLVM EclipseGenDAGISel.inc -gen-dag-isel)
+tablegen(LLVM EclipseGenInstrInfo.inc -gen-instr-info)
+tablegen(LLVM EclipseGenRegisterInfo.inc -gen-register-info)
+tablegen(LLVM EclipseGenSubtargetInfo.inc -gen-subtarget)
+
+add_public_tablegen_target(EclipseCommonTableGen)
+
+add_llvm_target(EclipseCodeGen
+  EclipseAsmPrinter.cpp
+  EclipseFrameLowering.cpp
+  EclipseISelDAGToDAG.cpp
+  EclipseISelLowering.cpp
+  EclipseInstrInfo.cpp
+  EclipseMCInstLower.cpp
+  EclipseRegisterInfo.cpp
+  EclipseSubtarget.cpp
+  EclipseTargetMachine.cpp
+
+  LINK_COMPONENTS
+  Analysis
+  AsmPrinter
+  CodeGen
+  CodeGenTypes
+  Core
+  EclipseDesc
+  EclipseInfo
+  MC
+  SelectionDAG
+  Support
+  Target
+  TargetParser
+
+  ADD_TO_COMPONENT
+  Eclipse
+  )
+
+add_subdirectory(MCTargetDesc)
+add_subdirectory(TargetInfo)
diff --git a/llvm/lib/Target/Eclipse/Eclipse.h b/llvm/lib/Target/Eclipse/Eclipse.h
new file mode 100644
index 000000000..252505f6d
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/Eclipse.h
@@ -0,0 +1,32 @@
+//===-- Eclipse.h - Top-level interface for Eclipse backend ---*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSE_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSE_H
+
+#include "llvm/CodeGen/SelectionDAGISel.h"
+#include "llvm/Pass.h"
+
+namespace llvm {
+class EclipseTargetMachine;
+class PassRegistry;
+class FunctionPass;
+
+class EclipseISelDAGToDAGPass : public SelectionDAGISelPass {
+public:
+  EclipseISelDAGToDAGPass(EclipseTargetMachine &TM);
+};
+
+FunctionPass *createEclipseISelDagLegacyPass(EclipseTargetMachine &TM);
+
+void initializeEclipseDAGToDAGISelLegacyPass(PassRegistry &);
+void initializeEclipseAsmPrinterPass(PassRegistry &);
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSE_H
diff --git a/llvm/lib/Target/Eclipse/Eclipse.td b/llvm/lib/Target/Eclipse/Eclipse.td
new file mode 100644
index 000000000..a0a3785f3
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/Eclipse.td
@@ -0,0 +1,51 @@
+//===- Eclipse.td - Describe the Eclipse Target Machine ---*- tablegen -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+include "llvm/Target/Target.td"
+
+include "EclipseRegisterInfo.td"
+include "EclipseInstrInfo.td"
+
+defm : RemapAllTargetPseudoPointerOperands<GPR>;
+
+def EclipseInstrInfo : InstrInfo;
+
+// The Eclipse S/140's "Extended Instruction Set" over the base Data
+// General Nova/Eclipse ISA — concretely, for this backend's purposes,
+// native IOR/XOR instructions. Confirmed empirically: `dgasm -t
+// eclipse_s140` accepts IOR/XOR, but `dgasm -t nova1`/`nova3`/`nova4`
+// reject both as "unrecognised instruction" — every other mnemonic this
+// backend emits (ADD/SUB/AND/COM/NEG/MOV/DSZ/ISZ/JMP/JSR/LDA/MOVL#/MOV#/
+// SUB#/DIV/MUL/device I/O) assembles unchanged on all four. See
+// EclipseISelLowering.cpp's PerformDAGCombine, which synthesizes OR/XOR
+// from COM/AND (De Morgan's) whenever this feature is absent, instead of
+// emitting IORrr/XORrr directly.
+def FeatureEIS : SubtargetFeature<"eis", "HasEIS", "true",
+  "Has the Eclipse S/140 Extended Instruction Set (native IOR/XOR)">;
+
+def : ProcessorModel<"generic", NoSchedModel, []>;
+def : ProcessorModel<"eclipse_s140", NoSchedModel, [FeatureEIS]>;
+// Base Data General Nova CPUs -- no FeatureEIS, matching dgasm's -t
+// nova1/nova3/nova4 (see FeatureEIS's comment above). All three are
+// modeled identically here: this backend doesn't yet distinguish any
+// finer-grained differences between them (e.g. nova1 vs nova3/nova4
+// hardware MUL/DIV availability -- see nova-llvm-backend's own
+// NOVA_PORTING_NOTES.md for what was and wasn't verified).
+def : ProcessorModel<"nova1", NoSchedModel, []>;
+def : ProcessorModel<"nova3", NoSchedModel, []>;
+def : ProcessorModel<"nova4", NoSchedModel, []>;
+
+def EclipseInstPrinter : AsmWriter {
+  string AsmWriterClassName = "InstPrinter";
+  bit isMCAsmWriter = 1;
+}
+
+def Eclipse : Target {
+  let InstructionSet = EclipseInstrInfo;
+  let AssemblyWriters = [EclipseInstPrinter];
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseAsmPrinter.cpp b/llvm/lib/Target/Eclipse/EclipseAsmPrinter.cpp
new file mode 100644
index 000000000..641e6941c
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseAsmPrinter.cpp
@@ -0,0 +1,720 @@
+//===-- EclipseAsmPrinter.cpp - Eclipse LLVM assembly writer ------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseAsmPrinter.h"
+#include "Eclipse.h"
+#include "EclipseISelLowering.h"
+#include "EclipseMCInstLower.h"
+#include "MCTargetDesc/EclipseInstPrinter.h"
+#include "MCTargetDesc/EclipseMCTargetDesc.h"
+#include "TargetInfo/EclipseTargetInfo.h"
+#include "llvm/ADT/STLFunctionalExtras.h"
+#include "llvm/CodeGen/MachineConstantPool.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstr.h"
+#include "llvm/IR/Constants.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/GlobalVariable.h"
+#include "llvm/IR/Module.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/MC/MCStreamer.h"
+#include "llvm/MC/MCSymbol.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/Support/raw_ostream.h"
+
+using namespace llvm;
+
+char EclipseAsmPrinter::ID = 0;
+
+INITIALIZE_PASS(EclipseAsmPrinter, "eclipse-asm-printer",
+                 "Eclipse Assembly Printer", false, false)
+
+// dgasm's real string-literal escaping (confirmed against a real dgasm
+// binary by the sibling eccc project — crates/eccc-codegen/src/
+// symbolic.rs's escape_dgasm_string). NUL termination is dgasm's own job
+// ("dgasm NUL-terminates `var` strings automatically"), not added here.
+static std::string escapeDgasmString(StringRef S) {
+  std::string Out;
+  for (unsigned char B : S) {
+    switch (B) {
+    case '\n': Out += "\\n"; break;
+    case '\t': Out += "\\t"; break;
+    case '\r': Out += "\\r"; break;
+    case '"': Out += "\\\""; break;
+    case '\\': Out += "\\\\"; break;
+    default:
+      if (B >= 0x20 && B <= 0x7e)
+        Out += static_cast<char>(B);
+      else
+        Out += ("\\" + Twine::utohexstr(B)).str(); // best-effort; see README.md
+    }
+  }
+  return Out;
+}
+
+static StringRef stripSlotSuffix(StringRef SlotName) {
+  return SlotName.ends_with("_SLOT") ? SlotName.drop_back(5) : SlotName;
+}
+
+bool EclipseAsmPrinter::doInitialization(Module &M) {
+  HasMain = M.getFunction("main") != nullptr;
+  if (HasMain)
+    addCallSlot("main_SLOT");
+
+  for (const Function &F : M) {
+    if (F.isDeclaration() || !F.hasFnAttribute("interrupt"))
+      continue;
+    if (!InterruptHandlerName.empty()) {
+      std::string Msg =
+          ("Eclipse backend: more than one __attribute__((interrupt)) "
+           "function in this module (\"" +
+           Twine(InterruptHandlerName) + "\" and \"" + F.getName() +
+           "\") -- real hardware has exactly one interrupt vector (memory "
+           "location 1), so only one handler is possible.")
+              .str();
+      report_fatal_error(StringRef(Msg));
+    }
+    InterruptHandlerName = std::string(F.getName());
+  }
+
+  return AsmPrinter::doInitialization(M);
+}
+
+void EclipseAsmPrinter::emitStartOfAsmFile(Module &M) {
+  OutStreamer->emitRawText(
+      "// Generated by the Eclipse LLVM backend (see README.md)");
+  OutStreamer->emitRawText(
+      "// Assemble with: dgasm -t eclipse_s140 -f simh -o out.simh <this file>");
+  if (!InterruptHandlerName.empty()) {
+    // Real hardware jumps indirect through memory location 1 on interrupt
+    // entry (see the Eclipse S/140 Programmer's Reference's Program Flow
+    // Interruption section, and this backend's RETI/INTEN/JMPABSI, which
+    // implement the matching return sequence) -- this fixed word has to
+    // hold the handler's address before anything else runs. Emitted here,
+    // ahead of even "org 0100", so reorder_asm.py's preamble detection
+    // (which takes *everything* through the first "var _scratch" line as
+    // fixed and untouched -- see its own header comment) carries it
+    // through unmolested, exactly like every hand-verified
+    // interrupt_testN.s test in this project's development history did by
+    // hand.
+    OutStreamer->emitRawText("org 1");
+    OutStreamer->emitRawText(
+        ("var VECTOR = " + Twine(InterruptHandlerName)).str());
+    OutStreamer->emitRawText("");
+  }
+  OutStreamer->emitRawText("org 0100"); // just a default; see README.md
+  OutStreamer->emitRawText("dev TTI = 010");
+  OutStreamer->emitRawText("dev TTO = 011");
+  OutStreamer->emitRawText("");
+  if (HasMain) {
+    OutStreamer->emitRawText("_start:");
+    OutStreamer->emitRawText("\tJSR @main_SLOT,0");
+    OutStreamer->emitRawText("\tHALT");
+    OutStreamer->emitRawText("");
+  }
+  OutStreamer->emitRawText("// --- fixed runtime state ---");
+  OutStreamer->emitRawText("var _SP = 020000");
+  OutStreamer->emitRawText("var _scratch = 0");
+  OutStreamer->emitRawText("");
+}
+
+void EclipseAsmPrinter::emitEndOfAsmFile(Module &M) {
+  OutStreamer->emitRawText("");
+  OutStreamer->emitRawText("// --- indirect call jump table ---");
+  for (const std::string &Slot : CallSlots)
+    OutStreamer->emitRawText(
+        ("var " + Twine(Slot) + " = " + stripSlotSuffix(Slot)).str());
+  if (NeedsIndirectCallSlot)
+    OutStreamer->emitRawText(
+        (Twine("var ") + EclipseIndirectCallSlotName + " = 0").str());
+
+  if (!AddrSlots.empty()) {
+    OutStreamer->emitRawText("");
+    OutStreamer->emitRawText("// --- global address slots (LEAGA) ---");
+    for (const std::string &Target : AddrSlots)
+      OutStreamer->emitRawText(
+          ("var " + Twine(Target) + "_PTR = " + Target).str());
+  }
+
+  if (!OffsetSlots.empty()) {
+    OutStreamer->emitRawText("");
+    OutStreamer->emitRawText("// --- LEAGA offset constants ---");
+    for (auto &Entry : OffsetSlots)
+      OutStreamer->emitRawText(
+          ("var " + Twine(Entry.first) + " = " + Twine(Entry.second)).str());
+  }
+}
+
+namespace {
+// One flattened Eclipse word from a (possibly nested) constant initializer
+// -- either a plain integer or a reference to another global/function's
+// address (the "&fred"/CallSlots shape emitGlobalVariable's GlobalValue
+// case already handles at the top level).
+struct FlatWord {
+  bool IsSymbol;
+  int64_t IntVal;
+  const GlobalValue *Sym;
+};
+} // namespace
+
+// How many Eclipse words (16-bit) a value of this type occupies, used only
+// to expand a ConstantAggregateZero's *nested* member (a zero-initialized
+// sub-struct/sub-array within an otherwise-nonzero aggregate, e.g. `struct
+// S2 v = {1, 2, 3, {4, 5}}` if `.s` had been left as its default `{0, 0}`
+// instead) into the right number of zero words -- ConstantAggregateZero
+// carries no explicit element list of its own to recurse into, unlike
+// every other Constant kind flattenConstant handles.
+static unsigned countScalarWords(Type *Ty) {
+  if (auto *IT = dyn_cast<IntegerType>(Ty))
+    return (IT->getBitWidth() + 15) / 16;
+  if (Ty->isPointerTy())
+    return 1;
+  if (auto *AT = dyn_cast<ArrayType>(Ty))
+    return AT->getNumElements() * countScalarWords(AT->getElementType());
+  if (auto *ST = dyn_cast<StructType>(Ty)) {
+    unsigned N = 0;
+    for (unsigned i = 0, e = ST->getNumElements(); i != e; ++i)
+      N += countScalarWords(ST->getElementType(i));
+    return N;
+  }
+  return 0; // unsupported leaf type (e.g. float/double) -- caller bails.
+}
+
+// Recursively flattens any (possibly nested) struct/array/scalar constant
+// into a flat, in-order sequence of Eclipse words -- e.g. `struct S2 v =
+// {1, 2, 3, {4, 5}}` (a struct with a nested union *and* a nested struct
+// member, see this function's own caller for why the previous flat-only
+// ConstantStruct handling didn't cover this) becomes the 5-word sequence
+// [1, 2, 3, 4, 5], addressed the same way emitArrayElements' flat array
+// case always has: relative to the *global's own base address* plus a
+// compile-time-known word offset, never by any individual word's own
+// (otherwise-arbitrary) symbol name. Returns false, flattening nothing,
+// the moment it hits a constant kind it doesn't understand, so the
+// caller can fall back to the generic "unsupported" placeholder exactly
+// as it did before this function existed, rather than silently emitting
+// a partial/misaligned word sequence.
+
+// Splits a raw bit pattern into NumWords Eclipse words, most-significant
+// first -- shared by ConstantInt (any width) and ConstantFP (always
+// exactly 2 words: this backend's `float`/`double` alias to the same
+// 32-bit soft-float IEEE-754 layout rt/eclipse_rt.c's sf_* routines
+// expect, see this function's own ConstantFP case below).
+static void appendBitsAsWords(const APInt &Val, unsigned NumWords,
+                               SmallVectorImpl<FlatWord> &Out) {
+  for (unsigned i = 0; i != NumWords; ++i) {
+    unsigned ShiftAmt = (NumWords - 1 - i) * 16;
+    int16_t Word =
+        static_cast<int16_t>(Val.lshr(ShiftAmt).trunc(16).getZExtValue());
+    Out.push_back({false, static_cast<int64_t>(Word), nullptr});
+  }
+}
+
+static bool flattenConstant(const Constant *C, SmallVectorImpl<FlatWord> &Out) {
+  if (const auto *CI = dyn_cast<ConstantInt>(C)) {
+    // Same multi-word (most-significant-first) splitting as a top-level
+    // wide scalar -- see emitGlobalVariable's own ConstantInt case for
+    // why a single `var` line can't represent more than 16 bits.
+    unsigned BitWidth = CI->getBitWidth();
+    appendBitsAsWords(CI->getValue(), (BitWidth + 15) / 16, Out);
+    return true;
+  }
+  if (const auto *CFP = dyn_cast<ConstantFP>(C)) {
+    // A `float`/`double` global's initializer (e.g. `double x = 100;` --
+    // `double` aliases `float`'s 32-bit soft-float layout on this
+    // backend, see the header comment on why) was never handled at all
+    // before this case existed: falling all the way through to the
+    // generic "unsupported" placeholder meant the global silently read
+    // back as 0.0 forever, regardless of its real initializer -- found
+    // by tracing `double x = 100; return x < 1;` (expected false/0,
+    // this backend's own real bug-free-by-then __ltsf2 confirmed
+    // correct on ordinary local/literal operands) returning true/1
+    // instead, which only happens if `x` were actually 0.0 (0 < 1 is
+    // true) rather than 100. `bitcastToAPInt` gives the exact 32-bit
+    // IEEE-754 bit pattern this rt's sf_bits()/sf_from_bits() (and every
+    // sf_* routine built on them) already assume for any runtime
+    // `float`/`double` value, so emitting it the same way a wide
+    // ConstantInt already is keeps every soft-float routine's own
+    // bit-pattern assumptions satisfied for a *constant* one too.
+    appendBitsAsWords(CFP->getValueAPF().bitcastToAPInt(), 2, Out);
+    return true;
+  }
+  if (const auto *GVal = dyn_cast<GlobalValue>(C)) {
+    Out.push_back({true, 0, GVal});
+    return true;
+  }
+  if (const auto *CS = dyn_cast<ConstantStruct>(C)) {
+    for (unsigned i = 0, e = CS->getNumOperands(); i != e; ++i)
+      if (!flattenConstant(CS->getOperand(i), Out))
+        return false;
+    return true;
+  }
+  if (const auto *CA = dyn_cast<ConstantArray>(C)) {
+    for (unsigned i = 0, e = CA->getNumOperands(); i != e; ++i)
+      if (!flattenConstant(CA->getOperand(i), Out))
+        return false;
+    return true;
+  }
+  if (const auto *CDA = dyn_cast<ConstantDataArray>(C)) {
+    for (unsigned i = 0, e = CDA->getNumElements(); i != e; ++i)
+      Out.push_back({false, CDA->getElementAsAPInt(i).getSExtValue(), nullptr});
+    return true;
+  }
+  if (isa<ConstantAggregateZero>(C)) {
+    unsigned N = countScalarWords(C->getType());
+    if (N == 0 && !C->getType()->isIntegerTy(0))
+      return false; // unrecognized leaf type somewhere in this subtree
+    for (unsigned i = 0; i != N; ++i)
+      Out.push_back({false, 0, nullptr});
+    return true;
+  }
+  return false;
+}
+
+void EclipseAsmPrinter::emitGlobalVariable(const GlobalVariable *GV) {
+  MCSymbol *Sym = getSymbol(GV);
+  if (!GV->hasInitializer()) {
+    OutStreamer->emitRawText(("var " + Twine(Sym->getName()) + " = 0").str());
+    return;
+  }
+  const Constant *Init = GV->getInitializer();
+
+  if (const auto *CDA = dyn_cast<ConstantDataArray>(Init)) {
+    if (CDA->isString()) {
+      StringRef S = CDA->getAsString();
+      S.consume_back(StringRef("\0", 1)); // dgasm adds its own terminator
+      OutStreamer->emitRawText(("var " + Twine(Sym->getName()) + " = \"" +
+                                 escapeDgasmString(S) + "\"")
+                                    .str());
+      return;
+    }
+  }
+
+  if (const auto *CI = dyn_cast<ConstantInt>(Init)) {
+    // A scalar initializer wider than one Eclipse word (e.g. a 32-bit
+    // `long`/`unsigned long`, this target's only scalar type besides i16)
+    // needs one `var` line *per word*, most-significant first — the same
+    // multi-word layout the `_off1`/"access word N" indirect-addressing
+    // scheme (see LEAGA's emitInstruction case and
+    // EclipseISelLowering.cpp's PerformDAGCombine) already assumes every
+    // >16-bit global has. A single `var NAME = <64-bit decimal value>`
+    // line here does NOT do that: dgasm's `var` only ever reserves and
+    // deposits *one* word, silently truncating the decimal value to its
+    // low 16 bits at parse time. This was a real, previously-undiscovered
+    // bug, not a hypothetical: confirmed by tracing a `static u32 g_a =
+    // 0x40400000UL;` global on eclipseemu all the way down to its actual
+    // deposited memory contents — both words came back 0x0000 (0x40400000
+    // truncated mod 2^16 happens to be exactly 0 for that value, which is
+    // what made it look, from the *load* side alone, like a "high word
+    // right, low word wrong" bug rather than "the global's storage was
+    // never really 32 bits wide in the first place"). Any 32-bit-wide
+    // global's arithmetic silently corrupts as a result, regardless of
+    // whether the *load* path is otherwise correct.
+    unsigned BitWidth = CI->getBitWidth();
+    if (BitWidth <= 16) {
+      OutStreamer->emitRawText(("var " + Twine(Sym->getName()) + " = " +
+                                 Twine(CI->getSExtValue()))
+                                    .str());
+      return;
+    }
+    APInt Val = CI->getValue();
+    unsigned NumWords = (BitWidth + 15) / 16;
+    for (unsigned i = 0; i != NumWords; ++i) {
+      unsigned ShiftAmt = (NumWords - 1 - i) * 16;
+      int16_t Word =
+          static_cast<int16_t>(Val.lshr(ShiftAmt).trunc(16).getZExtValue());
+      // Only the first (most-significant) word keeps the real symbol
+      // name — LEAGA/the `_PTR` slot always materializes *this* word's
+      // address, and every later word is only ever reached relative to
+      // it via the `_off1`-style mechanism, never addressed by its own
+      // name — same convention emitArrayElements below already uses for
+      // array elements past the first.
+      std::string Name = i == 0 ? Sym->getName().str()
+                                 : (Sym->getName() + "_word" + Twine(i)).str();
+      OutStreamer->emitRawText(
+          ("var " + Twine(Name) + " = " + Twine(static_cast<int64_t>(Word)))
+              .str());
+    }
+    return;
+  }
+
+  // Numeric array initializer (e.g. `static const unsigned int prog[] =
+  // {...}`) — every array element becomes its own `var` line, in order,
+  // so dgasm's own sequential address assignment (each `var` line takes
+  // the next word) naturally lays them out contiguously; C-level
+  // pointer/index arithmetic on the array (computed relative to the
+  // *first* element's address, which is this global's own symbol) then
+  // finds every later element correctly with no further backend support
+  // needed. Only the first element keeps the real symbol name — the
+  // rest just need *a* unique, never-referenced-by-name label so dgasm
+  // accepts the declaration; nothing ever addresses them by name.
+  //
+  // This was a real, silent bug before this comment existed: an array
+  // initializer that reached neither the ConstantInt nor the
+  // ConstantDataArray-isString() case above fell through to a single
+  // placeholder `var NAME = 0` word instead of the real data — so any
+  // array with more than one element silently lost everything past its
+  // first word, and indexing into it read whatever unrelated global
+  // happened to be laid out next. Found by tracing actual DOA/DOB
+  // values sent to a real device on eclipseemu and discovering the
+  // "array" data was actually a different global's string contents.
+  auto emitArrayElements = [&](unsigned NumElts,
+                                llvm::function_ref<int64_t(unsigned)> At) {
+    for (unsigned i = 0; i != NumElts; ++i) {
+      std::string Name = i == 0 ? Sym->getName().str()
+                                 : (Sym->getName() + "_elem" + Twine(i)).str();
+      OutStreamer->emitRawText(
+          ("var " + Twine(Name) + " = " + Twine(At(i))).str());
+    }
+  };
+
+  if (const auto *CDA = dyn_cast<ConstantDataArray>(Init)) {
+    // Reached only for the non-string (numeric) case now — isString()
+    // was already handled and returned above.
+    emitArrayElements(CDA->getNumElements(), [&](unsigned i) {
+      return CDA->getElementAsAPInt(i).getSExtValue();
+    });
+    return;
+  }
+
+  // A function-pointer (or any other global's-address) scalar
+  // initializer, e.g. `int (*f)(int) = &fred;` -- under this LLVM
+  // version's opaque pointers, `&fred` is just the Function itself used
+  // as the initializer (no bitcast/ptrtoint wrapper to see through), and
+  // a plain `var NAME = target` is exactly CallSlots' own "var
+  // foo_SLOT = foo" shape, just under this global's own name instead of
+  // a synthesized "_SLOT" one.
+  if (const auto *TargetGV = dyn_cast<GlobalValue>(Init)) {
+    OutStreamer->emitRawText(("var " + Twine(Sym->getName()) + " = " +
+                               Twine(getSymbol(TargetGV)->getName()))
+                                  .str());
+    return;
+  }
+
+  // A struct initializer whose members are themselves each a scalar
+  // (ConstantInt) or a global's address (GlobalValue) -- e.g. `struct S
+  // { int (*zerofunc)(); } s = { &zero };` -- and, more generally, any
+  // (possibly multiply-nested) struct/array/union combination, e.g.
+  // `struct S2 { int a, b; union {int c,d;}; struct S1 s; } v = {1, 2, 3,
+  // {4, 5}}` (a struct with both a nested union member *and* a nested
+  // struct member) or `S a[1] = {{1, {2, 3}}}` (an array of structs with
+  // their own nested array member) -- flattenConstant recurses through
+  // any combination of ConstantStruct/ConstantArray/ConstantDataArray/
+  // ConstantAggregateZero down to their scalar (ConstantInt/GlobalValue)
+  // leaves, in address order, so this handles every aggregate shape
+  // reaching this point uniformly rather than the previous flat-members-
+  // only ConstantStruct case and the numeric-ConstantArray case above
+  // each needing their own hand-rolled, nesting-blind logic. One `var`
+  // line per flattened word, same "only the first word keeps the real
+  // symbol name" convention as emitArrayElements above (nothing ever
+  // addresses a later word by its own name -- see that comment for why).
+  // Confirmed as a real, previously-silent bug, not just a missing
+  // feature: the old ConstantStruct case's `AllSimple` check saw `v`'s
+  // nested union/struct members and declined entirely, falling to the
+  // generic zero placeholder below -- but the *numeric ConstantArray*
+  // case above (now folded into this one too) had an even quieter
+  // version of the same gap, silently zeroing any nested sub-array
+  // element (e.g. a multi-dimensional `int arr[][3][5] = {...}`) while
+  // still reporting success for the array as a whole.
+  {
+    SmallVector<FlatWord, 16> Words;
+    if (flattenConstant(Init, Words)) {
+      for (unsigned i = 0, e = Words.size(); i != e; ++i) {
+        std::string Name = i == 0 ? Sym->getName().str()
+                                   : (Sym->getName() + "_elem" + Twine(i)).str();
+        std::string Value = Words[i].IsSymbol
+            ? getSymbol(Words[i].Sym)->getName().str()
+            : Twine(Words[i].IntVal).str();
+        OutStreamer->emitRawText(
+            ("var " + Twine(Name) + " = " + Twine(Value)).str());
+      }
+      return;
+    }
+  }
+
+  // Anything else beyond a simple i16 scalar, string literal, or the
+  // struct/array/union/scalar combinations flattenConstant handles isn't
+  // materialized yet (e.g. a float/double member) — see README.md "Known
+  // limitations".
+  OutStreamer->emitRawText(
+      ("var " + Twine(Sym->getName()) + " = 0 // TODO: unsupported initializer")
+          .str());
+}
+
+void EclipseAsmPrinter::emitConstantPool() {
+  const MachineConstantPool *MCP = MF->getConstantPool();
+  const std::vector<MachineConstantPoolEntry> &Entries = MCP->getConstants();
+  if (Entries.empty())
+    return;
+  for (unsigned i = 0, e = Entries.size(); i != e; ++i) {
+    const MachineConstantPoolEntry &E = Entries[i];
+    if (E.isMachineConstantPoolEntry())
+      continue; // not used by this backend
+    int64_t Value = 0;
+    if (const auto *CI = dyn_cast<ConstantInt>(E.Val.ConstVal))
+      Value = CI->getSExtValue();
+    OutStreamer->emitRawText(
+        ("var " + Twine(GetCPISymbol(i)->getName()) + " = " + Twine(Value))
+            .str());
+  }
+}
+
+namespace {
+// Which real skip-test instruction each conditional branch pseudo prints,
+// and whether it's the zero test (MOV#) or the sign test (MOVL#) — see
+// docs/CALLING_CONVENTION.md's verified idiom in the sibling eccc project.
+struct SkipTest {
+  const char *Mnemonic; // "MOV#" or "MOVL#"
+  const char *Skip;     // "SZR", "SNR", "SZC", "SNC"
+};
+
+// The printed sequence is "<test>,<skip>" then "JMP $dst": the *skip*
+// fires (and the JMP is *not* taken) exactly when the skip-condition
+// holds. So to make the JMP execute — i.e. actually branch — precisely
+// when this pseudo's own named condition holds, each one needs the
+// *opposite* skip test: BRZ (branch when val==0) must skip when val!=0
+// (SNR), not when val==0 (that would skip the JMP in exactly the case
+// BRZ is supposed to take it). Same inversion for the other three.
+SkipTest skipTestFor(unsigned Opcode) {
+  switch (Opcode) {
+  case Eclipse::BRZ:
+    return {"MOV#", "SNR"};
+  case Eclipse::BRNZ:
+    return {"MOV#", "SZR"};
+  case Eclipse::BGEZ:
+    return {"MOVL#", "SNC"};
+  case Eclipse::BLTZ:
+    return {"MOVL#", "SZC"};
+  // BGEU/BLTU aren't in this table: unlike the other four, they test the
+  // *native* carry from "SUB# rhs,lhs" directly, not a rotated copy of a
+  // single already-computed value — see emitInstruction's own BGEU/BLTU
+  // case, which prints "SUB#" (not this function's Mnemonic) against two
+  // distinct operands. Same skip letters apply, though (SNC/SZC), which
+  // is why they're documented alongside these here rather than
+  // separately: BGEU's "branch if no borrow occurred" is the same SNC
+  // used by BGEZ's "branch if sign bit clear", just fed by a different
+  // preceding instruction.
+  default:
+    llvm_unreachable("not a conditional branch");
+  }
+}
+} // namespace
+
+void EclipseAsmPrinter::emitInstruction(const MachineInstr *MI) {
+  switch (MI->getOpcode()) {
+  case Eclipse::BRZ:
+  case Eclipse::BRNZ:
+  case Eclipse::BGEZ:
+  case Eclipse::BLTZ: {
+    SkipTest T = skipTestFor(MI->getOpcode());
+    StringRef Reg = EclipseInstPrinter::getRegisterName(MI->getOperand(0).getReg());
+    OutStreamer->emitRawText(
+        (Twine("\t") + T.Mnemonic + " " + Reg + "," + Reg + "," + T.Skip).str());
+    OutStreamer->emitRawText(
+        (Twine("\tJMP ") + MI->getOperand(1).getMBB()->getSymbol()->getName())
+            .str());
+    return;
+  }
+  case Eclipse::BGEU:
+  case Eclipse::BLTU: {
+    // Bare "SUB# rhs,lhs,<skip>" (the original design here) turned out to
+    // be a genuine hardware bug in this lowering, not just a documentation
+    // slip: on real Nova/Eclipse ALU encoding, the carry field of a plain
+    // SUB (no Z/O/C modifier) is "none" — the carry it leaves behind is
+    // *whatever carry was already set* before the instruction executed,
+    // completely independent of lhs/rhs. Confirmed empirically against
+    // eclipseemu with a direct carry-in/carry-out truth table: bare SUB#'s
+    // carry-out equals its carry-in in all four (carry-in, lhs-vs-rhs)
+    // combinations tested. This is what caused the "same source, different
+    // compiled skip letter" contradiction — the residual carry differs by
+    // program, not the node type.
+    //
+    // The real fix needs a *forced* carry-in, i.e. "SUBZ" (the Z carry
+    // modifier). But SUBZ can't be combined with "#" (test mode / no
+    // writeback) — that specific combination decodes as an unrelated XOP
+    // instruction on this hardware/simulator (also confirmed empirically:
+    // "ADDZ#"/"SUBZ#" both mis-decode). So the comparison can't be done
+    // in a single non-clobbering instruction the way the signed BGEZ/BLTZ
+    // path does.
+    //
+    // Instead: borrow AC2 (the frame pointer) as scratch, exactly like the
+    // MUL/DIV pseudo-expansion already does elsewhere in this file/backend
+    // — save it to the software stack, copy lhs into it, SUBZ rhs into it
+    // (now carry is a clean, deterministic function of lhs/rhs only:
+    // confirmed via truth table that carry=1 iff lhs>=rhs, unsigned),
+    // restore AC2, then read that already-set carry with a *bare* "SUB#"
+    // self-test (confirmed pure carry passthrough, i.e. doesn't disturb
+    // the carry we just restored-past) purely to attach the skip
+    // modifier. DSZ/STA/LDA/ISZ (the save/restore sequence) were each
+    // confirmed not to disturb carry either.
+    // Skip-field semantics confirmed via a direct, isolated eclipseemu
+    // test (deposit carry explicitly, execute *only* the skip
+    // instruction, watch whether PC advances by one word or two): "SZC"
+    // fires (skips) when C=1, "SNC" fires when C=0 — the opposite of
+    // what the mnemonics suggest. Combined with the SUBZ truth table
+    // above (C=1 iff lhs>=rhs), BGEU needs "SNC" (skip — i.e. don't take
+    // the branch — when C=0/lhs<rhs) and BLTU needs "SZC" (skip when
+    // C=1/lhs>=rhs).
+    const char *Skip = MI->getOpcode() == Eclipse::BGEU ? "SNC" : "SZC";
+    StringRef Lhs = EclipseInstPrinter::getRegisterName(MI->getOperand(0).getReg());
+    StringRef Rhs = EclipseInstPrinter::getRegisterName(MI->getOperand(1).getReg());
+    OutStreamer->emitRawText("\tDSZ _SP,0");
+    OutStreamer->emitRawText("\tSTA 2,@_SP");
+    OutStreamer->emitRawText((Twine("\tMOV ") + Lhs + ",2").str());
+    OutStreamer->emitRawText((Twine("\tSUBZ ") + Rhs + ",2").str());
+    OutStreamer->emitRawText("\tLDA 2,@_SP");
+    OutStreamer->emitRawText("\tISZ _SP,0");
+    OutStreamer->emitRawText((Twine("\tSUB# 2,2,") + Skip).str());
+    OutStreamer->emitRawText(
+        (Twine("\tJMP ") + MI->getOperand(2).getMBB()->getSymbol()->getName())
+            .str());
+    return;
+  }
+  case Eclipse::CALL: {
+    StringRef Slot = MI->getOperand(0).getSymbolName();
+    // The shared indirect-call slot (see EclipseISelLowering.h) isn't an
+    // ordinary "<callee>_SLOT" pointing at a real function -- addCallSlot
+    // would declare it as `var __indirect_call_SLOT = __indirect_call_SLOT`
+    // (stripSlotSuffix leaves a name with no "_SLOT" suffix unchanged),
+    // a self-referential declaration for what needs to just be a plain
+    // writable zero-initialized word instead. Track it separately and
+    // declare it directly in emitEndOfAsmFile.
+    if (Slot == EclipseIndirectCallSlotName)
+      NeedsIndirectCallSlot = true;
+    else
+      addCallSlot(Slot);
+    break; // fall through to normal lowering for the JSR text itself
+  }
+  case Eclipse::LEAGA: {
+    StringRef Dst = EclipseInstPrinter::getRegisterName(MI->getOperand(0).getReg());
+    StringRef Target = getSymbol(MI->getOperand(1).getGlobal())->getName();
+    addAddrSlot(Target);
+    OutStreamer->emitRawText(
+        (Twine("\tLDA ") + Dst + "," + Target + "_PTR").str());
+
+    // A GlobalAddress operand's offset is real: `opt` folds a
+    // compile-time-constant `getelementptr(@global, N)` (from indexing
+    // an array with a literal, or one the optimizer proved constant)
+    // directly into this offset field rather than leaving it as a
+    // separate runtime ADD — confirmed by inspecting the IR this
+    // backend actually receives (`getelementptr inbounds nuw (i8, ptr
+    // @foo, i16 N)` baked into the load's pointer operand). Previously
+    // ignored entirely here: every LEAGA silently loaded the global's
+    // *base* address regardless of N, so indexed access anywhere past
+    // element 0 of an array read whatever unrelated data happened to
+    // follow it in memory — a real, confirmed-the-hard-way bug (traced
+    // actual DOA/DOB values sent to a real device on eclipseemu against
+    // the known-correct source data to find it).
+    //
+    // AC2 is temporarily repurposed as scratch to hold the offset
+    // constant for the ADD, saved/restored around it exactly like
+    // MULrr/UDIVrr's post-RA expansion already does for the hardware
+    // MUL/DIV operand convention (EclipseInstrInfo.cpp) — $dst is
+    // always AC0 or AC1 (LEAGA's register class excludes the reserved
+    // AC2/AC3), so this can never save/restore the wrong register.
+    //
+    // MI->getOperand(1).getOffset() is a *byte* offset (LLVM's
+    // GlobalAddress offsets always are), but this backend's addresses
+    // are word-granular (every Eclipse word is 2 of those DataLayout
+    // bytes — same reason EclipseFrameLowering::
+    // processFunctionBeforeFrameFinalized divides object sizes by 2).
+    // Adding the raw byte count here landed 2x too far — confirmed the
+    // hard way: reading at "base + 10" actually returned element 10's
+    // value, not element 5's, once traced against the real deposited
+    // memory addresses in a generated .simh.
+    //
+    // That "2 DataLayout bytes per word" rule only holds for globals
+    // this backend actually *packs* two-bytes-to-a-word — i.e. numeric
+    // array initializers, emitted one full word per element by
+    // emitGlobalVariable below regardless of the element's own
+    // DataLayout size (so 2 bytes worth of *offset* really does mean 1
+    // word of address). A string literal is emitted completely
+    // differently: dgasm's `var name = "..."` (no `packed` suffix, what
+    // emitGlobalVariable emits) reserves one full word *per character*
+    // (see dgasm's assembler.c, VARIABLE_STRING vs VARIABLE_PACKED_STRING)
+    // — so a byte offset into a string is already a word offset, and
+    // dividing it by 2 here truncates every *odd* byte offset down to
+    // its even neighbour's word (bytes 2 and 3 both landing on word 1,
+    // etc.), silently reading/writing the wrong character. Found via a
+    // local `char arr[5] = {'H','e','l','l','o'}` whose compiler-
+    // generated initializer copy (an unaligned `llvm.memcpy` from the
+    // string constant, expanded into byte-sized sub-loads by LLVM's
+    // generic unaligned-access lowering) corrupted exactly the odd-byte-
+    // offset accesses this truncation predicts.
+    int64_t Divisor = 2;
+    if (const auto *GV = dyn_cast<GlobalVariable>(MI->getOperand(1).getGlobal())) {
+      if (GV->hasInitializer()) {
+        if (const auto *CDA = dyn_cast<ConstantDataArray>(GV->getInitializer()))
+          if (CDA->isString())
+            Divisor = 1;
+      }
+    }
+    int64_t Offset = MI->getOperand(1).getOffset() / Divisor;
+    if (Offset != 0) {
+      std::string OffSym = addOffsetSlot(Target, Offset);
+      OutStreamer->emitRawText("\tDSZ _SP,0");
+      OutStreamer->emitRawText("\tSTA 2,@_SP");
+      OutStreamer->emitRawText(("\tLDA 2," + Twine(OffSym)).str());
+      OutStreamer->emitRawText((Twine("\tADD 2,") + Dst).str());
+      OutStreamer->emitRawText("\tLDA 2,@_SP");
+      OutStreamer->emitRawText("\tISZ _SP,0");
+    }
+    return;
+  }
+  default:
+    break;
+  }
+
+  EclipseMCInstLower Lower(OutContext, *this);
+  MCInst TmpInst;
+  Lower.Lower(MI, TmpInst);
+  EmitToStreamer(*OutStreamer, TmpInst);
+}
+
+void EclipseAsmPrinter::printOperand(const MachineInstr *MI, int OpNum,
+                                      raw_ostream &O) {
+  const MachineOperand &MO = MI->getOperand(OpNum);
+  switch (MO.getType()) {
+  case MachineOperand::MO_Register:
+    O << EclipseInstPrinter::getRegisterName(MO.getReg());
+    break;
+  case MachineOperand::MO_Immediate:
+    O << MO.getImm();
+    break;
+  case MachineOperand::MO_MachineBasicBlock:
+    MO.getMBB()->getSymbol()->print(O, MAI);
+    break;
+  case MachineOperand::MO_GlobalAddress:
+    getSymbol(MO.getGlobal())->print(O, MAI);
+    break;
+  case MachineOperand::MO_ExternalSymbol:
+    GetExternalSymbolSymbol(MO.getSymbolName())->print(O, MAI);
+    break;
+  default:
+    llvm_unreachable("unsupported inline-asm operand kind");
+  }
+}
+
+// No ExtraCode ('%a'/'%c'/'%n'/'%s'-style) modifiers are supported yet —
+// only plain %0/%1/... substitution, which is all a device-I/O
+// instruction's operands (an accumulator, a device code) need.
+bool EclipseAsmPrinter::PrintAsmOperand(const MachineInstr *MI, unsigned OpNo,
+                                         const char *ExtraCode,
+                                         raw_ostream &O) {
+  if (ExtraCode && ExtraCode[0])
+    return true; // unknown/unsupported modifier
+  printOperand(MI, OpNo, O);
+  return false;
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeEclipseAsmPrinter() {
+  RegisterAsmPrinter<EclipseAsmPrinter> X(getTheEclipseTarget());
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseAsmPrinter.h b/llvm/lib/Target/Eclipse/EclipseAsmPrinter.h
new file mode 100644
index 000000000..7da95bb55
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseAsmPrinter.h
@@ -0,0 +1,164 @@
+//===-- EclipseAsmPrinter.h - Eclipse LLVM assembly writer -----*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// Drives the module-level structure a real dgasm source file needs (an
+// `org`/`dev` preamble, an optional `_start`/HALT entry trampoline when
+// the module defines `main`, `var` declarations for globals/string
+// literals/constant-pool entries, and one `var <callee>_SLOT = <callee>`
+// per distinct call target for the indirect-call jump table) around the
+// per-function instruction text. See README.md and the sibling `eccc`
+// project's crates/eccc-codegen/src/symbolic.rs, which this mirrors.
+//
+// IMPORTANT — page-zero placement: all `var` declarations (constant
+// pool, globals, the jump table, `_SP`/`_scratch`) must physically end up
+// within the first ~200 words of the address space: `LDA`/`STA ac,addr`
+// default to *page-zero* (absolute, no index) addressing with only an
+// 8-bit displacement, and dgasm assigns addresses strictly in textual
+// source order starting at `org` (confirmed by reading its assembler.c
+// directly, not assumed). Growing function bodies can push a `var` line
+// emitted after them past address 255, which dgasm rejects.
+//
+// This AsmPrinter does *not* solve that itself (it would require
+// buffering and reordering essentially everything AsmPrinter emits,
+// including labels emitted by its own internal machinery, not just this
+// file's explicit emitRawText calls — real surgery for a marginal gain
+// over the alternative). Instead, the toolchain wrapper this project
+// ships (see the driver script) runs a small, separate text-level
+// reordering pass on `llc`'s output before handing it to dgasm: keep the
+// fixed org/dev/_start preamble first, then move every remaining `var `
+// line before every remaining non-`var` line, preserving relative order
+// within each group. Pure line-oriented text manipulation, easy to
+// verify independently of AsmPrinter's internals. Matches eccc's own
+// proven layout (docs/DESIGN.md / docs/CALLING_CONVENTION.md): small
+// page-zero data first, arbitrarily-large code after, reached only via
+// PC-relative jumps or through-the-jump-table calls, never direct
+// page-zero addressing.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEASMPRINTER_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEASMPRINTER_H
+
+#include "llvm/ADT/SmallVector.h"
+#include "llvm/CodeGen/AsmPrinter.h"
+#include <string>
+
+namespace llvm {
+
+class EclipseAsmPrinter : public AsmPrinter {
+  SmallVector<std::string, 8> CallSlots;
+  // Pointer-word slots for LEAGA (see EclipseInstrInfo.td's LEAGA comment):
+  // "var <name>_PTR = <name>" holds a global's *address* as a page-zero
+  // data word, the same indirection CallSlots already uses for function
+  // addresses. Stores the *target* name (e.g. ".str"); the printer
+  // appends "_PTR" itself when declaring and referencing the slot.
+  SmallVector<std::string, 8> AddrSlots;
+  // Constant page-zero words holding a nonzero LEAGA offset (see the
+  // LEAGA case in emitInstruction): `getelementptr(@global, N)` folded
+  // into a GlobalAddress operand's offset, for N != 0. This ISA has no
+  // immediate-operand ADD, so combining a compile-time-constant offset
+  // into a loaded pointer needs the offset materialized as its own data
+  // word first, the same reason every other "constant" in this backend
+  // goes through a var/constant-pool word (see EclipseInstrInfo.td's
+  // file header). Keyed by (target, offset) so the same offset into the
+  // same global is only declared once. This is a *separate* bookkeeping
+  // vector from AddrSlots (not reusing MF->getConstantPool()) because a
+  // constant pool entry added this late — during per-instruction text
+  // emission — would arrive after emitConstantPool() already ran for
+  // this function and never get its `var` line emitted at all; AddrSlots
+  // and CallSlots already solve exactly this ordering problem by
+  // collecting names during instruction emission and flushing them in
+  // emitEndOfAsmFile, so this follows the same proven pattern.
+  SmallVector<std::pair<std::string, int64_t>, 8> OffsetSlots;
+  bool HasMain = false;
+
+  // Set when this module contains at least one CALLSLOT_STORE (see
+  // EclipseISelLowering.cpp's LowerCall / EclipseInstrInfo.td) -- i.e. at
+  // least one genuinely indirect call (through a runtime-computed
+  // function pointer, not a direct call to a named function). Declaring
+  // the shared __indirect_call_SLOT word costs one more word of the
+  // tight 256-word page-zero budget (see this file's own header), so
+  // it's only emitted for a module that actually needs it, the same
+  // discipline print_float's split-out of printf follows.
+  bool NeedsIndirectCallSlot = false;
+
+  // Name of the module's `__attribute__((interrupt))` function, if any
+  // (computed once in doInitialization, before any function body is
+  // emitted, since emitStartOfAsmFile needs it for the vector directive
+  // it writes ahead of everything else). Empty means no interrupt handler
+  // in this module. Real hardware has exactly one interrupt vector
+  // (memory location 1), so more than one such function is a hard error
+  // (doInitialization), not a "pick one" situation.
+  std::string InterruptHandlerName;
+
+  void addCallSlot(StringRef Slot) {
+    if (!llvm::is_contained(CallSlots, Slot.str()))
+      CallSlots.push_back(Slot.str());
+  }
+
+  void addAddrSlot(StringRef Target) {
+    if (!llvm::is_contained(AddrSlots, Target.str()))
+      AddrSlots.push_back(Target.str());
+  }
+
+  // Returns the symbol name to use for `Target`'s Offset constant word,
+  // declaring it (once) if this is the first time this exact
+  // (Target, Offset) pair has been seen.
+  std::string addOffsetSlot(StringRef Target, int64_t Offset) {
+    std::string Name = (Target + "_off" + Twine(Offset)).str();
+    for (auto &Entry : OffsetSlots)
+      if (Entry.first == Name)
+        return Name;
+    OffsetSlots.emplace_back(Name, Offset);
+    return Name;
+  }
+
+  // NB: internal JMPs (conditional-branch trailing jumps and plain
+  // unconditional ones) stay direct/PC-relative here, not indirected
+  // through a page-zero slot the way CALL is. Unlike CALL — one slot per
+  // *function*, a naturally small set — a real program can have many
+  // dozens of basic blocks; giving every one a permanent page-zero slot
+  // was tried and overflowed the 256-word page-zero budget on nothing
+  // fancier than eclipse_rt.c's printf/scanf. Out-of-range jumps (dgasm:
+  // "Address out of range", displacement outside -128..127) are instead
+  // relaxed selectively, only where actually needed, by
+  // eclipse-toolchain/reorder_asm.py — it runs after llc, when real
+  // per-instruction addresses are computable, which this AsmPrinter
+  // (still mid-codegen, no real addresses yet) cannot do itself.
+
+public:
+  explicit EclipseAsmPrinter(TargetMachine &TM,
+                              std::unique_ptr<MCStreamer> Streamer)
+      : AsmPrinter(TM, std::move(Streamer), ID) {}
+
+  StringRef getPassName() const override { return "Eclipse Assembly Printer"; }
+
+  bool doInitialization(Module &M) override;
+  void emitStartOfAsmFile(Module &M) override;
+  void emitEndOfAsmFile(Module &M) override;
+  void emitInstruction(const MachineInstr *MI) override;
+  void emitGlobalVariable(const GlobalVariable *GV) override;
+  void emitConstantPool() override;
+
+  // Inline asm operand substitution (e.g. `asm volatile("DOAS %0,012" ::
+  // "r"(ch))` — the escape hatch for device I/O instructions, since those
+  // have no ordinary-C-semantics mapping; see README.md).
+  void printOperand(const MachineInstr *MI, int OpNum, raw_ostream &O);
+  bool PrintAsmOperand(const MachineInstr *MI, unsigned OpNo,
+                        const char *ExtraCode, raw_ostream &O) override;
+
+  // dgasm's grammar has no linkage-visibility directive (".globl" and
+  // friends are a GNU-assembler concept) — labels are just labels.
+  void emitLinkage(const GlobalValue *, MCSymbol *) const override {}
+
+  static char ID;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEASMPRINTER_H
diff --git a/llvm/lib/Target/Eclipse/EclipseFrameLowering.cpp b/llvm/lib/Target/Eclipse/EclipseFrameLowering.cpp
new file mode 100644
index 000000000..2515b7be0
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseFrameLowering.cpp
@@ -0,0 +1,143 @@
+//===-- EclipseFrameLowering.cpp - Eclipse Frame Information -------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseFrameLowering.h"
+#include "EclipseInstrInfo.h"
+#include "EclipseRegisterInfo.h"
+#include "EclipseSubtarget.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/RegisterScavenging.h"
+
+using namespace llvm;
+
+EclipseFrameLowering::EclipseFrameLowering(const EclipseSubtarget & /*STI*/)
+    : TargetFrameLowering(TargetFrameLowering::StackGrowsDown, Align(2), 0) {}
+
+// `_SP` is a plain page-zero memory word, not a register: manipulated with
+// ISZABS/DSZABS (ISZ/DSZ) plus single-level indirect LDABSI/STABSI — see
+// docs/ECLIPSE_ISA_NOTES.md in the sibling `eccc` project for why (the
+// hardware PSH/POP/SAVE/RTN stack instructions were tried and rejected as
+// needing an OS-managed environment this bare-metal target doesn't have).
+static const char *SPSym = "_SP";
+
+void EclipseFrameLowering::emitPrologue(MachineFunction &MF,
+                                         MachineBasicBlock &MBB) const {
+  MachineBasicBlock::iterator MBBI = MBB.begin();
+  DebugLoc DL;
+  const auto &TII =
+      *MF.getSubtarget<EclipseSubtarget>().getInstrInfo();
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+
+  // Push caller's FP (AC2).
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::DSZABS)).addExternalSymbol(SPSym);
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::STABSI))
+      .addReg(Eclipse::AC2)
+      .addExternalSymbol(SPSym);
+  // Push return address (AC3, set by the caller's JSR).
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::DSZABS)).addExternalSymbol(SPSym);
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::STABSI))
+      .addReg(Eclipse::AC3)
+      .addExternalSymbol(SPSym);
+  // FP := current stack top.
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::LDABS), Eclipse::AC2)
+      .addExternalSymbol(SPSym);
+  // Reserve locals + temps.
+  uint64_t Words = MFI.getStackSize();
+  for (uint64_t i = 0; i < Words; ++i)
+    BuildMI(MBB, MBBI, DL, TII.get(Eclipse::DSZABS)).addExternalSymbol(SPSym);
+}
+
+void EclipseFrameLowering::emitEpilogue(MachineFunction &MF,
+                                         MachineBasicBlock &MBB) const {
+  MachineBasicBlock::iterator MBBI = MBB.getFirstTerminator();
+  DebugLoc DL = MBBI != MBB.end() ? MBBI->getDebugLoc() : DebugLoc();
+  const auto &TII =
+      *MF.getSubtarget<EclipseSubtarget>().getInstrInfo();
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+
+  uint64_t Words = MFI.getStackSize();
+  for (uint64_t i = 0; i < Words; ++i)
+    BuildMI(MBB, MBBI, DL, TII.get(Eclipse::ISZABS)).addExternalSymbol(SPSym);
+
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::LDABSI), Eclipse::AC3)
+      .addExternalSymbol(SPSym);
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::ISZABS)).addExternalSymbol(SPSym);
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::LDABSI), Eclipse::AC2)
+      .addExternalSymbol(SPSym);
+  BuildMI(MBB, MBBI, DL, TII.get(Eclipse::ISZABS)).addExternalSymbol(SPSym);
+}
+
+MachineBasicBlock::iterator EclipseFrameLowering::eliminateCallFramePseudoInstr(
+    MachineFunction &MF, MachineBasicBlock &MBB,
+    MachineBasicBlock::iterator MI) const {
+  // ADJCALLSTACKDOWN/UP are always expanded earlier, during isel, by their
+  // hasCustomInserter handler (EclipseISelLowering.cpp) — nothing should
+  // reach PEI still holding one of these pseudos.
+  return MBB.erase(MI);
+}
+
+void EclipseFrameLowering::processFunctionBeforeFrameFinalized(
+    MachineFunction &MF, RegScavenger *RS) const {
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+
+  // EclipseRegisterInfo::requiresRegisterScavenging/
+  // requiresFrameIndexScavenging both unconditionally return true (see
+  // their comment) — LEAFI's post-regalloc expansion (eliminateFrameIndex)
+  // needs a genuine scratch physical register it can't just conjure, so
+  // PEI always builds a RegScavenger for this target. But a scavenger can
+  // only *use* a register that's already live by spilling its current
+  // occupant somewhere first, and it has no stack slot to spill into
+  // unless one is explicitly reserved and registered here — without this,
+  // any function with enough register pressure to actually need a
+  // mid-function scavenge (a local array plus a loop plus call-heavy code
+  // was enough to trigger it) crashed with "Cannot scavenge register
+  // without an emergency spill slot!" during PEI. One word-sized GPR slot
+  // is enough: nothing on this target ever needs to scavenge more than one
+  // register at a time.
+  int EmergencyFI = MFI.CreateStackObject(2, Align(2), /*isSpillSlot=*/true);
+  RS->addScavengingFrameIndex(EmergencyFI);
+
+  int64_t Offset = 0;
+  // Locals, temps, and register-allocator spill slots are all just more
+  // stack objects by this point — assign them cumulative negative
+  // FP-relative displacements exactly like the sibling `eccc` project's
+  // frame.rs does for its own temps/locals. Parameters are separate,
+  // *fixed* objects created with their final FP+2.. offset already baked
+  // in at LowerFormalArguments time (see EclipseISelLowering.cpp) and are
+  // skipped here.
+  for (int i = 0, e = MFI.getObjectIndexEnd(); i != e; ++i) {
+    if (MFI.isDeadObjectIndex(i) || MFI.isFixedObjectIndex(i))
+      continue;
+    // MachineFrameInfo object sizes are in real (8-bit) DataLayout bytes;
+    // every Eclipse word is 2 of those (EclipseTargetMachine's DataLayout
+    // is a standard 16-bit-pointer layout, not a "1 byte = 1 word" one —
+    // only the offsets *this backend itself* assigns, here and in
+    // LowerFormalArguments, are word-granular).
+    //
+    // NOTE: as of investigating a local-char-array miscompilation, this
+    // loop's offsets turned out to be moot in practice: PEI's own generic
+    // PEIImpl::calculateFrameObjectOffsets (llvm/lib/CodeGen/
+    // PrologEpilogInserter.cpp) runs immediately after
+    // processFunctionBeforeFrameFinalized and unconditionally overwrites
+    // every non-fixed, non-scavenging object's offset with its own
+    // byte-granular, alignment-respecting computation — confirmed by
+    // instrumenting both this loop and eliminateFrameIndex and finding
+    // completely different values for the same frame index. It happens
+    // to still produce non-overlapping (if 2x wasteful) slots for
+    // scalars, which is why nothing here was ever noticed before. Left
+    // as-is (harmless, and correct in spirit) since actually fixing the
+    // real problem means addressing PEI's overwrite directly rather than
+    // this loop — see DEBUGGING_NOTES.md.
+    int64_t Words = (MFI.getObjectSize(i) + 1) / 2;
+    Offset -= Words;
+    MFI.setObjectOffset(i, Offset);
+  }
+  MFI.setStackSize(static_cast<uint64_t>(-Offset));
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseFrameLowering.h b/llvm/lib/Target/Eclipse/EclipseFrameLowering.h
new file mode 100644
index 000000000..1002a0ccd
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseFrameLowering.h
@@ -0,0 +1,67 @@
+//===-- EclipseFrameLowering.h - Define frame lowering ---------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// Implements exactly the prologue/epilogue sequence verified (against a
+// real `eclipseemu` simulator) and documented in the sibling `eccc`
+// project's docs/CALLING_CONVENTION.md: push caller's FP (AC2) and the
+// JSR return address (AC3) onto the software stack, adopt the new stack
+// top as FP, reserve locals+temps; epilogue reverses it. Every function
+// uses this same sequence, including leaf functions — eccc documents that
+// as a deliberate v1 simplification (docs/LIMITATIONS.md) and this backend
+// keeps it for the same reason: correctness first, a leaf-function fast
+// path is follow-up work (see README.md).
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEFRAMELOWERING_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEFRAMELOWERING_H
+
+#include "llvm/CodeGen/TargetFrameLowering.h"
+
+namespace llvm {
+
+class EclipseSubtarget;
+
+class EclipseFrameLowering : public TargetFrameLowering {
+public:
+  explicit EclipseFrameLowering(const EclipseSubtarget &STI);
+
+  void emitPrologue(MachineFunction &MF, MachineBasicBlock &MBB) const override;
+  void emitEpilogue(MachineFunction &MF, MachineBasicBlock &MBB) const override;
+
+  bool hasFPImpl(const MachineFunction &MF) const override { return true; }
+
+  MachineBasicBlock::iterator
+  eliminateCallFramePseudoInstr(MachineFunction &MF, MachineBasicBlock &MBB,
+                                 MachineBasicBlock::iterator MI) const override;
+
+  void processFunctionBeforeFrameFinalized(
+      MachineFunction &MF, RegScavenger *RS = nullptr) const override;
+
+  // No assignCalleeSavedSpillSlots override: AC2/AC3 never appear in
+  // getCalleeSavedRegs (EclipseRegisterInfo.cpp) regardless of function
+  // kind, so they're already never subject to the generic CSR mechanism —
+  // saved/restored explicitly by emitPrologue/emitEpilogue instead. An
+  // earlier version of this class *did* override this to unconditionally
+  // `return true` (meaning "already handled, PEI shouldn't do anything
+  // more"), which was harmless back when the callee-saved list was always
+  // empty, but became a real bug once __attribute__((interrupt)) started
+  // making it non-empty for AC0/AC1: `return true` with no real per-entry
+  // frame-index assignment left every CalleeSavedInfo's frame index at its
+  // default, causing every CSR save/restore to collide onto the same stack
+  // slot as whatever unrelated object happened to already have that
+  // default index — verified against a real generated interrupt handler,
+  // where AC0's and AC1's CSR saves silently clobbered each other and a
+  // register-allocator temp, all sharing one slot. Falling through to the
+  // base class's default implementation makes PEI create one real,
+  // distinct stack object per saved register instead.
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEFRAMELOWERING_H
diff --git a/llvm/lib/Target/Eclipse/EclipseISelDAGToDAG.cpp b/llvm/lib/Target/Eclipse/EclipseISelDAGToDAG.cpp
new file mode 100644
index 000000000..a4880e3ab
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseISelDAGToDAG.cpp
@@ -0,0 +1,106 @@
+//===-- EclipseISelDAGToDAG.cpp - A dag to dag inst selector -------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// Most Eclipse addressing shapes are expressed as plain TableGen
+// instruction Patterns in EclipseInstrInfo.td (wrapped global/
+// constant-pool addresses -> LDABS/STABS, any other register-valued
+// pointer -> the general LDIND/STIND indirection). Frame-index addressing
+// (LDFI/STFI/LEAFI) is the one exception, handled here by hand instead —
+// like other backends that have to do this: a plain `Operand<i16>` isn't
+// specifically typed to bind a FrameIndex SDNode value, so table-driven
+// matching doesn't reliably select it.
+//
+//===----------------------------------------------------------------------===//
+
+#include "Eclipse.h"
+#include "EclipseTargetMachine.h"
+#include "llvm/CodeGen/SelectionDAGISel.h"
+#include "llvm/CodeGen/SelectionDAGNodes.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "eclipse-isel"
+#define PASS_NAME "Eclipse DAG->DAG Pattern Instruction Selection"
+
+namespace {
+
+class EclipseDAGToDAGISel : public SelectionDAGISel {
+public:
+  EclipseDAGToDAGISel() = delete;
+  explicit EclipseDAGToDAGISel(EclipseTargetMachine &TM)
+      : SelectionDAGISel(TM) {}
+
+private:
+#include "EclipseGenDAGISel.inc"
+
+  void Select(SDNode *N) override;
+};
+
+class EclipseDAGToDAGISelLegacy : public SelectionDAGISelLegacy {
+public:
+  static char ID;
+  explicit EclipseDAGToDAGISelLegacy(EclipseTargetMachine &TM)
+      : SelectionDAGISelLegacy(ID, std::make_unique<EclipseDAGToDAGISel>(TM)) {
+  }
+};
+
+} // namespace
+
+char EclipseDAGToDAGISelLegacy::ID = 0;
+
+INITIALIZE_PASS(EclipseDAGToDAGISelLegacy, DEBUG_TYPE, PASS_NAME, false, false)
+
+void EclipseDAGToDAGISel::Select(SDNode *Node) {
+  if (Node->isMachineOpcode()) {
+    Node->setNodeId(-1);
+    return;
+  }
+
+  switch (Node->getOpcode()) {
+  case ISD::FrameIndex: {
+    int FI = cast<FrameIndexSDNode>(Node)->getIndex();
+    SDValue TFI = CurDAG->getTargetFrameIndex(FI, MVT::i16);
+    CurDAG->SelectNodeTo(Node, Eclipse::LEAFI, MVT::i16, TFI);
+    return;
+  }
+  case ISD::LOAD: {
+    auto *LD = cast<LoadSDNode>(Node);
+    if (auto *FIN = dyn_cast<FrameIndexSDNode>(LD->getBasePtr())) {
+      SDValue TFI = CurDAG->getTargetFrameIndex(FIN->getIndex(), MVT::i16);
+      SDValue Ops[] = {TFI, LD->getChain()};
+      SDNode *MN =
+          CurDAG->SelectNodeTo(Node, Eclipse::LDFI, MVT::i16, MVT::Other, Ops);
+      CurDAG->setNodeMemRefs(cast<MachineSDNode>(MN), {LD->getMemOperand()});
+      return;
+    }
+    break;
+  }
+  case ISD::STORE: {
+    auto *ST = cast<StoreSDNode>(Node);
+    if (auto *FIN = dyn_cast<FrameIndexSDNode>(ST->getBasePtr())) {
+      SDValue TFI = CurDAG->getTargetFrameIndex(FIN->getIndex(), MVT::i16);
+      SDValue Ops[] = {ST->getValue(), TFI, ST->getChain()};
+      SDNode *MN = CurDAG->SelectNodeTo(Node, Eclipse::STFI, MVT::Other, Ops);
+      CurDAG->setNodeMemRefs(cast<MachineSDNode>(MN), {ST->getMemOperand()});
+      return;
+    }
+    break;
+  }
+  default:
+    break;
+  }
+
+  SelectCode(Node);
+}
+
+EclipseISelDAGToDAGPass::EclipseISelDAGToDAGPass(EclipseTargetMachine &TM)
+    : SelectionDAGISelPass(std::make_unique<EclipseDAGToDAGISel>(TM)) {}
+
+FunctionPass *llvm::createEclipseISelDagLegacyPass(EclipseTargetMachine &TM) {
+  return new EclipseDAGToDAGISelLegacy(TM);
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseISelLowering.cpp b/llvm/lib/Target/Eclipse/EclipseISelLowering.cpp
new file mode 100644
index 000000000..310a09104
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseISelLowering.cpp
@@ -0,0 +1,1462 @@
+//===-- EclipseISelLowering.cpp - Eclipse DAG Lowering Implementation ---===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseISelLowering.h"
+#include "EclipseMachineFunctionInfo.h"
+#include "EclipseSubtarget.h"
+#include "MCTargetDesc/EclipseMCTargetDesc.h"
+#include "llvm/CodeGen/CallingConvLower.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+#include "llvm/CodeGen/SelectionDAG.h"
+#include "llvm/IR/DiagnosticInfo.h"
+#include "llvm/IR/Function.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "eclipse-isel-lowering"
+
+// Every Eclipse word is 16 bits: one InputArg/OutputArg (after the common
+// SelectionDAG type legalizer has already split anything wider — e.g. an
+// i32 parameter arrives here as two separate i16 entries) occupies exactly
+// one stack word. See docs/CALLING_CONVENTION.md in the sibling `eccc`
+// project.
+static constexpr int64_t kWordBytes = 2;
+
+// See EclipseISelLowering.h's own comment on this declaration. Must end
+// in "_SLOT" (matching every other call slot's naming convention) --
+// reorder_asm.py's PZ_VAR_RE only recognizes that exact (case-sensitive)
+// suffix (plus "_PTR"/"_offN"/"CPI_") when deciding which `var` lines are
+// page-zero-required and must be moved to the front of the file; a name
+// that doesn't match gets left wherever llc happened to emit it, and on
+// any program of real size that's well past address 255 -- confirmed
+// empirically (dgasm's own "Address out of range" on the very first
+// attempt with a lowercase name).
+const char *const llvm::EclipseIndirectCallSlotName = "__indirect_call_SLOT";
+
+EclipseTargetLowering::EclipseTargetLowering(const TargetMachine &TM,
+                                              const EclipseSubtarget &STI)
+    : TargetLowering(TM, STI), Subtarget(STI) {
+  addRegisterClass(MVT::i16, &Eclipse::GPRRegClass);
+  computeRegisterProperties(STI.getRegisterInfo());
+
+  // `char` is real i8 IR (Clang doesn't allow customizing CHAR_BIT — see
+  // clang/lib/Basic/Targets/Eclipse.h), even though no i8 register class
+  // exists here: i8 values live in a full i16 register/word, matching
+  // eccc's own "char is stored one per 16-bit word" convention at the
+  // storage-layout level. Note this only covers *scalar* char (locals,
+  // parameters, single-value pointers): char *arrays* and structs mixing
+  // char with other types would need genuine byte-addressed packing,
+  // which — like eccc — this backend doesn't implement (no LDB/STB
+  // byte-pointer instructions; see README.md "Known limitations").
+  //
+  // Loads/stores: since every real memory access here is a full 16-bit
+  // word regardless of the IR's notional 8-bit type, an i8-extending
+  // load or truncating store is *exactly* the same physical instruction
+  // as a plain i16 one — LDFI/STFI (custom-selected in
+  // EclipseISelDAGToDAG.cpp, which is already extension-agnostic) and
+  // LDABS/STABS/LDIND/STIND (declarative patterns added below).
+  for (MVT VT : {MVT::i1, MVT::i8}) {
+    setLoadExtAction(ISD::EXTLOAD, MVT::i16, VT, Legal);
+    setLoadExtAction(ISD::SEXTLOAD, MVT::i16, VT, Legal);
+    setLoadExtAction(ISD::ZEXTLOAD, MVT::i16, VT, Legal);
+    setTruncStoreAction(MVT::i16, VT, Legal);
+  }
+  setOperationAction(ISD::SIGN_EXTEND_INREG, MVT::i8, Custom);
+  setOperationAction(ISD::SIGN_EXTEND_INREG, MVT::i1, Expand);
+
+  setOperationAction(ISD::SHL, MVT::i16, Custom);
+  setOperationAction(ISD::SRL, MVT::i16, Custom);
+  setOperationAction(ISD::SRA, MVT::i16, Custom);
+
+  // See LowerSDIVREM: the hardware DIV instruction is only ever driven
+  // unsigned (via UDIVrr/UREMrr); signed division is decomposed here.
+  setOperationAction(ISD::SDIV, MVT::i16, Custom);
+  setOperationAction(ISD::SREM, MVT::i16, Custom);
+
+  setBooleanContents(ZeroOrOneBooleanContent);
+  setStackPointerRegisterToSaveRestore(Register());
+  setMinFunctionAlignment(Align(1));
+
+  setOperationAction(ISD::GlobalAddress, MVT::i16, Custom);
+  setOperationAction(ISD::ExternalSymbol, MVT::i16, Custom);
+  setOperationAction(ISD::ConstantPool, MVT::i16, Custom);
+
+  setOperationAction(ISD::BR_CC, MVT::i16, Custom);
+  // Was `Expand` — relying on the generic legalizer to always eliminate
+  // BRCOND before instruction selection. That held for code shaped by
+  // this backend's own lowering, but broke once soft-float's more
+  // elaborate C (nested if/else-if/else over 32-bit comparisons, e.g.
+  // eclipse_rt.c's sf_add) gave the DAG combiner enough shape to
+  // rebuild a "Select feeding a brcond" pattern *after* the main
+  // BRCOND-Expand legalization pass had already run — confirmed
+  // empirically ("Cannot select: ... brcond ... Select ..."), not a
+  // theoretical concern. `Custom` (via LowerBRCOND, which just
+  // reduces it to the existing, already-robust BR_CC path: "branch if
+  // Cond != 0") gets re-invoked for *any* brcond node regardless of
+  // when it's introduced, since Custom-lowered results are always
+  // re-legalized — unlike Expand, which is a one-shot legalization-time
+  // rule with no such guarantee against post-legalize combines.
+  setOperationAction(ISD::BRCOND, MVT::i16, Custom);
+
+  // No jump-table (BR_JT) or computed-goto (BRIND) support: no pattern
+  // exists for either, and neither was ever given an explicit action
+  // here, so both defaulted to LegalizeAction's enum-zero "Legal" --
+  // which made TargetLowering::areJTsAllowed() (checking
+  // isOperationLegalOrCustom(BR_JT/BRIND, Other)) answer yes, letting
+  // SelectionDAGBuilder's switch-lowering heuristic pick a jump table
+  // for a sufficiently dense `switch` (e.g. Duff's device's 8
+  // consecutive `case 0..7:` labels) and crash at isel with no lowering
+  // for the BR_JT it built ("Cannot select: ch = br_jt ..., JumpTable
+  // ..."), confirmed empirically. Expand here isn't itself a working
+  // lowering for BR_JT (this target has no generic jump-table expansion
+  // implemented either) -- its actual effect is upstream, at
+  // areJTsAllowed()'s check, which now answers no and makes
+  // SelectionDAGBuilder fall back to its ordinary cascade-of-compares
+  // switch lowering instead, which this backend's already-working BR_CC
+  // path handles fine. Same "avoid the unimplemented lowering path
+  // entirely, via a threshold/action, rather than implement the whole
+  // feature" shape as getMaxStoresPerMemcpy's memcpy-libcall avoidance
+  // above.
+  setOperationAction(ISD::BR_JT, MVT::Other, Expand);
+  setOperationAction(ISD::BRIND, MVT::Other, Expand);
+
+  setOperationAction(ISD::SETCC, MVT::i16, Custom);
+  setOperationAction(ISD::SELECT, MVT::i16, Expand);
+  setOperationAction(ISD::SELECT_CC, MVT::i16, Custom);
+
+  // No hardware rotate, funnel shift, or widening-multiply result.
+  setOperationAction(ISD::ROTL, MVT::i16, Expand);
+  setOperationAction(ISD::ROTR, MVT::i16, Expand);
+  setOperationAction(ISD::MULHS, MVT::i16, Expand);
+  setOperationAction(ISD::MULHU, MVT::i16, Expand);
+  setOperationAction(ISD::SMUL_LOHI, MVT::i16, Expand);
+  setOperationAction(ISD::UMUL_LOHI, MVT::i16, Expand);
+
+  // The base Nova/Eclipse ISA has no immediate-operand instructions at all
+  // (see EclipseInstrInfo.td) — every constant is really the address of a
+  // page-zero data word, so a bare ISD::Constant needs to become a
+  // constant-pool load rather than staying a plain immediate.
+  setOperationAction(ISD::Constant, MVT::i16, Custom);
+
+  // Variadic calls: arguments are pushed right-to-left on the flat
+  // software stack (LowerCall's comment) with no register-based argument
+  // passing to complicate things, so va_start just needs to point past
+  // the last named parameter (LowerFormalArguments/LowerVASTART) — no
+  // register-save area to build, unlike e.g. x86-64's varargs ABI.
+  // ISD::VAARG itself needs no Custom handling: SelectionDAG's generic
+  // expansion (load the va_list pointer, load the value, store the
+  // pointer back incremented by the type's size) works unmodified here
+  // since va_list is a plain i16 pointer into this flat address space.
+  setOperationAction(ISD::VASTART, MVT::Other, Custom);
+  // Not Expand: the generic VAARG expansion inserts pointer-alignment
+  // rounding ("(ap+1) & ~1") for a real byte-addressed machine where a
+  // 2-byte-aligned type might start at an odd address. There's no such
+  // thing here — every address already refers to a whole word, so that
+  // rounding is not just unneeded but actively wrong (it can silently
+  // round the va_list pointer down to the *previous* word depending on
+  // the runtime address's parity). Custom-lowered instead with plain,
+  // unconditional word-stepping — see LowerVAARG.
+  setOperationAction(ISD::VAARG, MVT::Other, Custom);
+  // va_end is a no-op on this stack-based ABI (no resources to release).
+  setOperationAction(ISD::VAEND, MVT::Other, Expand);
+
+  // See PerformDAGCombine: a runtime array index into an indirectly-
+  // addressed ("large") global gets added, as a plain ADD, to the
+  // WRAPPER'd base pointer LEAGA materializes — and that ADD's other
+  // operand needs a word/byte-unit fixup this backend can't express any
+  // other way.
+  setTargetDAGCombine(ISD::ADD);
+
+  // Also see PerformDAGCombine: on a base-Nova subtarget (no FeatureEIS —
+  // see Eclipse.td), OR/XOR need synthesizing from COM/AND instead of the
+  // native IORrr/XORrr instructions, which dgasm rejects for those CPUs.
+  // Registered unconditionally (not gated on Subtarget.hasEIS() here) so
+  // eclipse_s140 pays for nothing extra: PerformDAGCombine itself bails
+  // immediately via Subtarget.hasEIS(), leaving IOR/XOR exactly as legal
+  // and unchanged as they were before this existed.
+  setTargetDAGCombine(ISD::OR);
+  setTargetDAGCombine(ISD::XOR);
+
+  // See PerformDAGCombine's own ISD::BRCOND case: catches a
+  // compile-time-constant branch condition that Custom-legalization's
+  // usual LegalizeDAG dispatch never reaches.
+  setTargetDAGCombine(ISD::BRCOND);
+
+  // Always inline memcpy/memmove/memset, never fall back to a `memcpy`/
+  // `memmove`/`memset` libcall: SelectionDAG::getMemcpy (etc.) only
+  // takes the libcall path once the store count needed to inline a
+  // given copy exceeds getMaxStoresPerMemcpy()'s default of 8 (4 with
+  // -Os) — e.g. a 10-element `int arr[10] = {...}` local initializer,
+  // which getOptimalMemOpType above sizes as ten i16 stores. LowerCall
+  // crashes on that libcall (confirmed empirically — see
+  // getOptimalMemOpType's comment above), so raise these limits well
+  // past anything this test suite's aggregates need instead of trying
+  // to make the libcall itself work.
+  MaxStoresPerMemcpy = MaxStoresPerMemcpyOptSize = 1024;
+  MaxStoresPerMemmove = MaxStoresPerMemmoveOptSize = 1024;
+  MaxStoresPerMemset = MaxStoresPerMemsetOptSize = 1024;
+
+  // f32 has no register class here (see this file's header — no
+  // addRegisterClass(MVT::f32, ...) above), so LLVM's type legalizer
+  // automatically "softens" every float op into an integer-typed libcall.
+  // On most in-tree targets that's all that's needed — the libcall *name*
+  // for each RTLIB::Libcall (e.g. RTLIB::ADD_F32) defaults to the usual
+  // compiler-rt symbol, resolved through a big per-architecture table
+  // (RuntimeLibcalls.inc's setTargetRuntimeLibcallSets) keyed off the
+  // Triple. "eclipse" isn't a recognized architecture in that generated
+  // table, so every RTLIB::LibcallImpl defaults to RTLIB::Unsupported for
+  // this target, and TargetLowering::makeLibCall hard-crashes
+  // ("unsupported library call operation") the first time softening
+  // needs one — confirmed empirically (this is exactly what happened
+  // before these lines were added). Fixed by explicitly wiring each
+  // RTLIB::Libcall this target's soft-float runtime (eclipse_rt.c)
+  // actually implements to its RTLIB::LibcallImpl (the compiler-rt-style
+  // symbol name) directly, bypassing the architecture table entirely.
+  //
+  // Only f32 (`float`) is wired up — `double` stays unimplemented (see
+  // eclipse_rt.c's soft-float section and the README's "no i32 runtime
+  // library" limitation: f64 softening would need genuine i64 support
+  // this backend doesn't have).
+  setLibcallImpl(RTLIB::ADD_F32, RTLIB::impl___addsf3);
+  setLibcallImpl(RTLIB::SUB_F32, RTLIB::impl___subsf3);
+  setLibcallImpl(RTLIB::MUL_F32, RTLIB::impl___mulsf3);
+  setLibcallImpl(RTLIB::DIV_F32, RTLIB::impl___divsf3);
+  // NOTE: these are the *three-way* (-1/0/1) compare libcalls -- the
+  // FCMP3_PRED_* family, not the plain OEQ_F32/UNE_F32/... boolean
+  // family. eclipse_rt.c's __eqsf2/__nesf2/__ltsf2/__lesf2/__gtsf2/__gesf2
+  // are all thin aliases of a single sf_cmp() returning -1/0/1 (the
+  // libgcc/compiler-rt tri-state convention), tested by the generic
+  // softenSetCCOperands against 0 with a per-predicate CondCode (e.g.
+  // SETEQ for OEQ, SETGT for OGT). Registering them under the boolean
+  // family instead makes softenSetCCOperands test the tri-state result
+  // with SETNE for every predicate (nonzero-means-true), which only
+  // happens to agree with the tri-state convention for UNE (and for the
+  // single true case of OGT/OLT where the result is +1/-1) -- every
+  // other predicate silently gets the wrong condition code.
+  setLibcallImpl(RTLIB::FCMP3_PRED_OEQ_F32, RTLIB::impl___eqsf2);
+  setLibcallImpl(RTLIB::FCMP3_PRED_UNE_F32, RTLIB::impl___nesf2);
+  setLibcallImpl(RTLIB::FCMP3_PRED_OLT_F32, RTLIB::impl___ltsf2);
+  setLibcallImpl(RTLIB::FCMP3_PRED_OLE_F32, RTLIB::impl___lesf2);
+  setLibcallImpl(RTLIB::FCMP3_PRED_OGT_F32, RTLIB::impl___gtsf2);
+  setLibcallImpl(RTLIB::FCMP3_PRED_OGE_F32, RTLIB::impl___gesf2);
+  // Ordered-comparison softening pairs each predicate with this — see
+  // eclipse_rt.c's __unordsf2 comment.
+  setLibcallImpl(RTLIB::UO_F32, RTLIB::impl___unordsf2);
+  setLibcallImpl(RTLIB::SINTTOFP_I32_F32, RTLIB::impl___floatsisf);
+  setLibcallImpl(RTLIB::UINTTOFP_I32_F32, RTLIB::impl___floatunsisf);
+  setLibcallImpl(RTLIB::FPTOSINT_F32_I32, RTLIB::impl___fixsfsi);
+  setLibcallImpl(RTLIB::FPTOUINT_F32_I32, RTLIB::impl___fixunssfsi);
+}
+
+std::pair<unsigned, const TargetRegisterClass *>
+EclipseTargetLowering::getRegForInlineAsmConstraint(
+    const TargetRegisterInfo *TRI, StringRef Constraint, MVT VT) const {
+  if (Constraint.size() == 1) {
+    switch (Constraint[0]) {
+    case 'r':
+      return std::make_pair(0U, &Eclipse::GPRRegClass);
+    default:
+      break;
+    }
+  }
+  return TargetLowering::getRegForInlineAsmConstraint(TRI, Constraint, VT);
+}
+
+const char *EclipseTargetLowering::getTargetNodeName(unsigned Opcode) const {
+  switch (static_cast<EclipseISD::NodeType>(Opcode)) {
+  case EclipseISD::WRAPPER:
+    return "EclipseISD::WRAPPER";
+  case EclipseISD::WORD_ADD:
+    return "EclipseISD::WORD_ADD";
+  case EclipseISD::HALVE:
+    return "EclipseISD::HALVE";
+  case EclipseISD::COM:
+    return "EclipseISD::COM";
+  case EclipseISD::CALL:
+    return "EclipseISD::CALL";
+  case EclipseISD::RET_FLAG:
+    return "EclipseISD::RET_FLAG";
+  case EclipseISD::RETI_FLAG:
+    return "EclipseISD::RETI_FLAG";
+  case EclipseISD::BRZ:
+    return "EclipseISD::BRZ";
+  case EclipseISD::BRNZ:
+    return "EclipseISD::BRNZ";
+  case EclipseISD::BGEZ:
+    return "EclipseISD::BGEZ";
+  case EclipseISD::BLTZ:
+    return "EclipseISD::BLTZ";
+  case EclipseISD::BGEU:
+    return "EclipseISD::BGEU";
+  case EclipseISD::BLTU:
+    return "EclipseISD::BLTU";
+  default:
+    return nullptr;
+  }
+}
+
+//===----------------------------------------------------------------------===//
+// LowerOperation
+//===----------------------------------------------------------------------===//
+
+SDValue EclipseTargetLowering::LowerOperation(SDValue Op,
+                                               SelectionDAG &DAG) const {
+  switch (Op.getOpcode()) {
+  case ISD::GlobalAddress:
+    return LowerGlobalAddress(Op, DAG);
+  case ISD::ExternalSymbol:
+    return LowerExternalSymbol(Op, DAG);
+  case ISD::ConstantPool:
+    return LowerConstantPool(Op, DAG);
+  case ISD::Constant: {
+    // Route through the constant pool: see the ISD::Constant comment in
+    // the constructor above.
+    SDLoc dl(Op);
+    auto *C = cast<ConstantSDNode>(Op);
+    SDValue CP = DAG.getConstantPool(
+        ConstantInt::get(*DAG.getContext(), C->getAPIntValue()), MVT::i16);
+    return LowerConstantPool(CP, DAG);
+  }
+  case ISD::BR_CC:
+    return LowerBR_CC(Op, DAG);
+  case ISD::BRCOND:
+    return LowerBRCOND(Op, DAG);
+  case ISD::SELECT_CC:
+    return LowerSELECT_CC(Op, DAG);
+  case ISD::SETCC:
+    return LowerSETCC(Op, DAG);
+  case ISD::SIGN_EXTEND_INREG:
+    return LowerSIGN_EXTEND_INREG(Op, DAG);
+  case ISD::SHL:
+  case ISD::SRL:
+  case ISD::SRA:
+    return LowerShift(Op, DAG);
+  case ISD::SDIV:
+  case ISD::SREM:
+    return LowerSDIVREM(Op, DAG);
+  case ISD::VASTART:
+    return LowerVASTART(Op, DAG);
+  case ISD::VAARG:
+    return LowerVAARG(Op, DAG);
+  default:
+    report_fatal_error("unimplemented Eclipse LowerOperation for " +
+                        Twine(Op.getOpcode()));
+  }
+}
+
+SDValue EclipseTargetLowering::LowerGlobalAddress(SDValue Op,
+                                                   SelectionDAG &DAG) const {
+  const auto *N = cast<GlobalAddressSDNode>(Op);
+  SDLoc dl(Op);
+  SDValue Addr =
+      DAG.getTargetGlobalAddress(N->getGlobal(), dl, MVT::i16, N->getOffset());
+  return DAG.getNode(EclipseISD::WRAPPER, dl, MVT::i16, Addr);
+}
+
+SDValue EclipseTargetLowering::LowerExternalSymbol(SDValue Op,
+                                                    SelectionDAG &DAG) const {
+  const auto *N = cast<ExternalSymbolSDNode>(Op);
+  SDLoc dl(Op);
+  SDValue Sym = DAG.getTargetExternalSymbol(N->getSymbol(), MVT::i16);
+  return DAG.getNode(EclipseISD::WRAPPER, dl, MVT::i16, Sym);
+}
+
+// A "large" (indirectly-addressed) global's real address lives in a
+// page-zero _PTR word; LEAGA (see EclipseAsmPrinter.cpp's emitInstruction)
+// loads that word verbatim, so the value a WRAPPER node produces is
+// *word*-granular. A compile-time-constant GEP offset into such a global
+// gets folded straight into the GlobalAddressSDNode's own offset field by
+// LowerGlobalAddress above — EclipseAsmPrinter's LEAGA case already knows
+// to divide that by 2 before turning it into a word-address addend.
+//
+// A *runtime* (non-constant) index can't be folded that way: it shows up
+// here as an ordinary ADD combining the WRAPPER'd base pointer with some
+// separately-computed offset SDValue — and that offset was computed by
+// perfectly standard, target-independent GEP lowering, which always
+// scales a runtime array index by the element type's DataLayout size in
+// *bytes* (2, for the only element types this backend's array/pointer
+// access actually supports — i16, and i32 which legalizes into a pair of
+// i16 words). Added directly to a word-granular base pointer, that byte
+// offset lands exactly twice as far as it should (e.g. index 4 resolves
+// to element 8) — the same unit mismatch as the constant-offset case
+// above, just reaching this backend through generic arithmetic instead of
+// through GlobalAddressSDNode's offset field, so it can't be fixed in the
+// same place.
+//
+// This can't be fixed in LDIND's expansion either (EmitInstrWithCustomInserter
+// / emitIndirectMem): a computed address reaching LDIND might just as
+// well have come from an already-word-granular frame-index computation
+// (eliminateFrameIndex already divides object sizes by 2), and LDIND has
+// no way to tell those two origins apart.
+//
+// So: catch it here, at the one point where a WRAPPER'd base and a
+// (necessarily byte-scaled) runtime offset are still visibly distinct
+// operands of the same ADD, and halve the offset before they combine.
+// The rewritten node is built with the distinct EclipseISD::WORD_ADD
+// opcode rather than a plain ISD::ADD specifically so it can never match
+// this same `N->getOpcode() == ISD::ADD` check again when DAGCombiner
+// revisits it — an earlier version returned a plain ADD guarded by an
+// "is the offset already SRL-by-1" heuristic instead, which triggered an
+// infinite DAGCombine loop (llc hung indefinitely) once DAGCombiner's own
+// canonicalization re-shaped the node between visits.
+//
+// The halving itself uses EclipseISD::HALVE, not a plain ISD::SRL/UDIV,
+// for the same reason: a bare `DAG.getNode(ISD::SRL, ...)` here was tried
+// first and produced its own infinite loop, empirically confirmed via
+// `llc -debug-only=dagcombine` — LowerShift Custom-lowers SRL-by-1 into
+// UDIV(x,2) (see below), and DAGCombiner's own standard "udiv by a
+// power-of-two constant -> shift" fold immediately turns that back into
+// SRL-by-1; since SRL is only ever Custom here (never strictly Legal),
+// every generated SRL re-enters LowerShift and the two folds ping-pong
+// forever. HALVE selects straight to UDIVrr (see EclipseInstrInfo.td)
+// and is structurally invisible to both of those generic combine rules.
+// Synthesizes OR/XOR from EclipseISD::COM ("not") and AND (De Morgan's:
+// a|b = ~(~a & ~b); a^b = (a & ~b) | (~a & b)) for base-Nova subtargets,
+// which have neither instruction natively. Building this as a *tree* of
+// EclipseISD::COM/ISD::AND/ISD::OR nodes here, rather than emitting real
+// machine instructions directly, is deliberate: DAGCombiner revisits
+// every node this creates, so an intermediate OR produced while
+// synthesizing XOR gets synthesized in turn by this same function on its
+// next visit.
+//
+// The "not" pieces use the distinct EclipseISD::COM opcode rather than a
+// plain `xor(x, -1)` — see that opcode's comment in EclipseISelLowering.h
+// for the infinite DAGCombine ping-pong a plain XOR produced empirically
+// (generic De Morgan's/double-negation folds recognize
+// `not(and(not(a),not(b)))` as `or(a,b)` and rewrite it straight back,
+// re-triggering this same combine forever).
+//
+// The one case this must never rewrite is XOR against an all-ones
+// constant — that *is* a genuine `~x`, and needs to keep matching
+// COMrr's own `(not GPR:$src)` pattern (still a plain `xor(x,-1)`)
+// directly rather than going through EclipseISD::COM at all.
+static SDValue synthesizeOrXor(SDNode *N, SelectionDAG &DAG) {
+  SDLoc dl(N);
+  SDValue A = N->getOperand(0);
+  SDValue B = N->getOperand(1);
+
+  auto isAllOnes = [](SDValue V) {
+    auto *C = dyn_cast<ConstantSDNode>(V);
+    return C && C->isAllOnes();
+  };
+  if (N->getOpcode() == ISD::XOR && (isAllOnes(A) || isAllOnes(B)))
+    return SDValue();
+
+  auto NotOf = [&](SDValue V) {
+    return DAG.getNode(EclipseISD::COM, dl, MVT::i16, V);
+  };
+
+  if (N->getOpcode() == ISD::OR) {
+    SDValue AndNotAB =
+        DAG.getNode(ISD::AND, dl, MVT::i16, NotOf(A), NotOf(B));
+    return NotOf(AndNotAB);
+  }
+
+  // ISD::XOR, neither operand the all-ones constant: a^b = (a&~b)|(~a&b).
+  SDValue T1 = DAG.getNode(ISD::AND, dl, MVT::i16, A, NotOf(B));
+  SDValue T2 = DAG.getNode(ISD::AND, dl, MVT::i16, NotOf(A), B);
+  return DAG.getNode(ISD::OR, dl, MVT::i16, T1, T2);
+}
+
+SDValue EclipseTargetLowering::PerformDAGCombine(SDNode *N,
+                                                  DAGCombinerInfo &DCI) const {
+  // A raw ISD::BRCOND surviving all the way to isel is a genuine
+  // Cannot-select crash on this target (confirmed empirically: "Cannot
+  // select: ch = brcond ..., EclipseISD::WRAPPER TargetConstantPool...").
+  // BRCOND is Custom (see the constructor's own comment on why), so the
+  // ordinary path is LowerBRCOND -- but that Custom hook is only ever
+  // reached via LegalizeDAG's per-node dispatch, which for a *compile-time-
+  // constant* condition (e.g. `do { ... } while(1)`'s literal
+  // `br i1 true, ...` -- the i1 constant gets promoted straight to a
+  // legal i16 by DAGTypeLegalizer's own boolean-promotion logic, via a
+  // dedicated PromoteIntOp_BRCOND-style path that replaces BRCOND's
+  // operand directly) never re-enters LegalizeOp for the *parent* BRCOND
+  // node the way an ordinary Custom-lowered result does -- confirmed
+  // empirically by instrumenting LowerBRCOND itself: it's called zero
+  // times for exactly this shape, while LowerBR_CC (used by every
+  // ordinary `if (icmp ...)`, which SelectionDAGBuilder builds as BR_CC
+  // directly, never touching BRCOND at all) fires normally. DAGCombine's
+  // own target-hook dispatch (this function) is a completely independent
+  // trigger from LegalizeDAG's Custom dispatch, invoked for every node of
+  // a registered opcode regardless of how that node arrived here, so it
+  // catches exactly the case Custom-lowering's dispatch missed. Reuses
+  // LowerBRCOND's own Chain/Cond/Dest -> BR_CC reduction unchanged --
+  // this is not a new lowering, just a second path to the same one.
+  // Gated to isAfterLegalizeDAG(): firing any earlier (e.g. during type
+  // legalization, which is when a compile-time-constant i1 condition
+  // actually gets promoted to i16 -- see the long comment above) hits
+  // this combine before the DAG is fully type/operation-legal, and
+  // LowerBRCOND's own DAG.getNode(BR_CC,...)/LowerBR_CC construction
+  // isn't safe to run against a not-yet-legal DAG -- confirmed
+  // empirically ("unimplemented Eclipse LowerOperation for <N>", a
+  // *different* crash, from calling this combine unconditionally).
+  if (N->getOpcode() == ISD::BRCOND && DCI.isAfterLegalizeDAG())
+    return LowerBRCOND(SDValue(N, 0), DCI.DAG);
+
+  if ((N->getOpcode() == ISD::OR || N->getOpcode() == ISD::XOR) &&
+      N->getValueType(0) == MVT::i16 && !Subtarget.hasEIS())
+    return synthesizeOrXor(N, DCI.DAG);
+
+  if (N->getOpcode() != ISD::ADD || N->getValueType(0) != MVT::i16)
+    return SDValue();
+
+  SDValue N0 = N->getOperand(0);
+  SDValue N1 = N->getOperand(1);
+
+  // An i8 (char) access needs none of this function's /2 correction at
+  // all: this backend already stores one char per full word (see this
+  // file's header comment and dgasm's VARIABLE_STRING format), so a byte
+  // index into a char array is *already* word-granular the moment GEP's
+  // generic lowering scales it by the element's DataLayout size (1 byte
+  // for i8, not the 2 the rest of this function exists to correct for
+  // i16 elements). Checked once, up front, rather than per Base-shape
+  // branch below (WrappedGlobal/FrameIndex/loaded-pointer all share the
+  // identical hazard whenever N directly feeds an i8 load/store) --
+  // applying the /2 fix meant for i16 elements to an already-correct i8
+  // offset introduces a wrong halving where none was needed. Confirmed
+  // empirically on two different Base shapes: `char *s = "abcdef";
+  // s[1]` (a loaded-pointer base) read 'a' (s[0]) instead of 'b', and
+  // `char s[8] = "abcdef"; int i = 1; s[i]` (a FrameIndex/local-array
+  // base) read 'a' instead of 'b' too -- the same truncating-integer-
+  // division symptom (1/2=0) either way.
+  if (llvm::any_of(N->users(), [&](SDNode *User) {
+        if (auto *LD = dyn_cast<LoadSDNode>(User))
+          return LD->getBasePtr().getNode() == N &&
+                 LD->getMemoryVT() == MVT::i8;
+        if (auto *ST = dyn_cast<StoreSDNode>(User))
+          return ST->getBasePtr().getNode() == N &&
+                 ST->getMemoryVT() == MVT::i8;
+        return false;
+      }))
+    return SDValue();
+
+  auto IsWrappedGlobal = [](SDValue V) {
+    return V.getOpcode() == EclipseISD::WRAPPER &&
+           V.getOperand(0).getOpcode() == ISD::TargetGlobalAddress;
+  };
+  // Constants routed through the constant pool (see the ISD::Constant
+  // comment in the constructor) arrive here as WRAPPER(ConstantPool), not
+  // a raw ConstantSDNode — checked separately below alongside the direct
+  // ConstantSDNode case, since it isn't certain at this combine's point
+  // in the pipeline which form a small constant offset like the
+  // multi-word-global "access word N" case actually takes.
+  auto IsWrappedConstant = [](SDValue V) {
+    return V.getOpcode() == EclipseISD::WRAPPER &&
+           V.getOperand(0).getOpcode() == ISD::TargetConstantPool;
+  };
+
+  SDValue Base, Offset;
+  bool BaseIsFrameIndex = false;
+  if (IsWrappedGlobal(N0)) {
+    Base = N0;
+    Offset = N1;
+  } else if (IsWrappedGlobal(N1)) {
+    Base = N1;
+    Offset = N0;
+  } else if (N0.getOpcode() == ISD::FrameIndex) {
+    Base = N0;
+    Offset = N1;
+    BaseIsFrameIndex = true;
+  } else if (N1.getOpcode() == ISD::FrameIndex) {
+    Base = N1;
+    Offset = N0;
+    BaseIsFrameIndex = true;
+  } else if ((N0.getOpcode() == ISD::LOAD) != (N1.getOpcode() == ISD::LOAD)) {
+    // Neither operand is a bare FrameIndex or WRAPPER'd global, but
+    // exactly one is a fresh LOAD -- plausibly a pointer *value* read
+    // back from its own stack slot (e.g. a `const T arr[]` parameter, or
+    // any other local pointer variable: this backend reloads such values
+    // from memory at every use rather than keeping them live in a
+    // register across statement boundaries). Such a loaded pointer is
+    // exactly as word-granular as a global's or a local array's address
+    // — confirmed empirically: a runtime index into an array reached via
+    // a pointer *parameter* (as opposed to a locally-declared array,
+    // already covered by the FrameIndex case above) landed on element
+    // 2*i instead of element i, the identical symptom as the two bugs
+    // documented above this function, just reached through a base
+    // pointer shape neither existing case recognized.
+    //
+    // The one-operand-is-a-LOAD shape alone isn't a safe enough signal by
+    // itself, though: plenty of perfectly ordinary integer arithmetic
+    // adds two loaded values together with no pointer involved at all
+    // (e.g. `a[i] = b[j] + c[k]`), and halving one side of that would
+    // silently corrupt it. What actually distinguishes the runtime-GEP
+    // case is what this ADD's *result* feeds: pointer arithmetic is
+    // consumed directly as a LOAD/STORE's base-pointer operand, while
+    // ordinary arithmetic never is. EclipseISelDAGToDAG.cpp's ISD::LOAD/
+    // ISD::STORE handling confirms this ADD is still exactly that base-
+    // pointer operand at the point this combine runs — a non-FrameIndex
+    // base falls through to the generic LDIND/STIND pattern completely
+    // unchanged, so gating on that usage is both necessary and sufficient
+    // to catch this case without misfiring on unrelated arithmetic that
+    // never becomes an address.
+    bool FeedsMemoryAddress = llvm::any_of(N->users(), [&](SDNode *User) {
+      if (auto *LD = dyn_cast<LoadSDNode>(User))
+        return LD->getBasePtr().getNode() == N;
+      if (auto *ST = dyn_cast<StoreSDNode>(User))
+        return ST->getBasePtr().getNode() == N;
+      return false;
+    });
+    if (!FeedsMemoryAddress)
+      return SDValue();
+    if (N0.getOpcode() == ISD::LOAD) {
+      Base = N0;
+      Offset = N1;
+    } else {
+      Base = N1;
+      Offset = N0;
+    }
+    // A loaded pointer value has no analogous "already halved, embedded
+    // in the node" mechanism the way a global's LEAGA/offset-slot
+    // printing does (see the comment below) — treat it like the
+    // FrameIndex case: a compile-time-constant Offset here (e.g.
+    // `arr[3]` through a pointer parameter) still needs the same /2 fix,
+    // not just the runtime-index case.
+    BaseIsFrameIndex = true;
+  } else {
+    return SDValue();
+  }
+
+  // A compile-time-constant Offset paired with a *global* Base here is
+  // *not* the runtime-GEP case this combine exists for — it's the generic
+  // type legalizer's own "access word N of a value that didn't fit in one
+  // register" address computation (e.g. splitting a 32-bit load/store on
+  // a >16-bit global into two i16 halves reaches the second one via
+  // `base + 1`), which is already word-granular. Confirmed empirically as
+  // a real, previously-undiscovered bug: halving that constant-1 offset
+  // (0 after integer division) silently redirected the "high word" access
+  // onto the same address as the low word, corrupting every 32-bit
+  // arithmetic op whose operand round-tripped through such a global.
+  // Constant offsets that *do* need the byte->word fix (a real GEP into
+  // an array of i16/i32) are already folded directly into
+  // GlobalAddressSDNode's own offset field by LowerGlobalAddress (see
+  // this function's header comment) and never reach here as a separate
+  // ADD operand at all — so excluding constants here only excludes cases
+  // that were never this combine's intended target in the first place.
+  //
+  // A FrameIndex Base is a *different* situation, confirmed via a second,
+  // independent repro (two file-scope `u32` globals added together,
+  // tracked down with `llc -debug-only=isel`/hand-tracing the emitted
+  // assembly): the type legalizer's "access word N" address computation
+  // for a *local* multi-word value (e.g. the second/low word of a 32-bit
+  // stack slot, reached whenever the frame index doesn't reduce to a bare
+  // FrameIndexSDNode base pointer that EclipseISelDAGToDAG's custom
+  // ISD::LOAD/ISD::STORE handling can absorb directly) has no analogous
+  // "already-divided, embedded in the node" mechanism the way
+  // LowerGlobalAddress/LEAGA's offset-slot printing does for globals —
+  // it reaches here as a genuinely still-byte-granular ADD(FrameIndex,
+  // Constant) needing the same /2 fix as the runtime-GEP case. Leaving it
+  // unhalved (as the global-only guard below did) computes the wrong
+  // word address for that second word (e.g. base+2 instead of base+1),
+  // silently reading/writing one word further than intended — this is
+  // exactly the corruption the multi-word-global repro traced back to
+  // once the global-load path itself was ruled out (verified correct by
+  // hand against the emitted LEAGA/offset-slot sequence).
+  if (!BaseIsFrameIndex &&
+      (isa<ConstantSDNode>(Offset) || IsWrappedConstant(Offset)))
+    return SDValue();
+
+  SelectionDAG &DAG = DCI.DAG;
+  SDLoc dl(N);
+
+  // A byte-scaled index of exactly 2 (an i16 element) commonly reaches
+  // here not as a MUL-by-constant but as a *self-add* (`X+X`) — LowerShift
+  // custom-lowers `X << 1` that way (see LowerShift's comment above), and
+  // GEP's own generic lowering will sometimes produce the multiply
+  // pre-strength-reduced into a shift too. Emitting a real runtime HALVE
+  // (an actual DIV instruction, see below) to undo an addend that's
+  // *already* visibly just `X+X` is pure waste — a real division for a
+  // value that's algebraically just X again — and on a program with many
+  // such array accesses the extra instructions are enough to push a
+  // branch target past dgasm's 0-255 page-relative JMP range (confirmed
+  // empirically: this was silently bloating every runtime GEP through a
+  // loaded pointer parameter enough to break an otherwise-fine program).
+  // Recognize the shape directly and skip the round trip.
+  if (Offset.getOpcode() == ISD::ADD &&
+      Offset.getOperand(0) == Offset.getOperand(1)) {
+    SDValue Halved = Offset.getOperand(0);
+    return DAG.getNode(EclipseISD::WORD_ADD, dl, MVT::i16, Base, Halved);
+  }
+
+  SDValue TwoCP = DAG.getConstantPool(
+      ConstantInt::get(*DAG.getContext(), APInt(16, 2)), MVT::i16);
+  SDValue Two = LowerConstantPool(TwoCP, DAG);
+  SDValue Halved = DAG.getNode(EclipseISD::HALVE, dl, MVT::i16, Offset, Two);
+  return DAG.getNode(EclipseISD::WORD_ADD, dl, MVT::i16, Base, Halved);
+}
+
+// No shift/rotate-with-writeback hardware is exposed yet (see README.md —
+// the base ISA's single-bit rotate-through-carry could build one, but
+// that's real, separate scope), so sign_extend_inreg from i8 — the ABI's
+// "truncate a char return value to 8 bits, then sign-extend back" step —
+// is done with the classic bit trick using only instructions this
+// backend already has: ((x & 0xFF) ^ 0x80) - 0x80.
+SDValue EclipseTargetLowering::LowerSIGN_EXTEND_INREG(SDValue Op,
+                                                       SelectionDAG &DAG) const {
+  SDLoc dl(Op);
+  EVT ExtVT = cast<VTSDNode>(Op.getOperand(1))->getVT();
+  assert(ExtVT == MVT::i8 && "unexpected sign_extend_inreg source width");
+  SDValue X = Op.getOperand(0);
+  SDValue Masked =
+      DAG.getNode(ISD::AND, dl, MVT::i16, X, DAG.getConstant(0xFF, dl, MVT::i16));
+  SDValue Flipped = DAG.getNode(ISD::XOR, dl, MVT::i16, Masked,
+                                 DAG.getConstant(0x80, dl, MVT::i16));
+  return DAG.getNode(ISD::SUB, dl, MVT::i16, Flipped,
+                      DAG.getConstant(0x80, dl, MVT::i16));
+}
+
+SDValue EclipseTargetLowering::LowerShift(SDValue Op, SelectionDAG &DAG) const {
+  SDLoc dl(Op);
+  SDValue X = Op.getOperand(0);
+  auto *AmtNode = dyn_cast<ConstantSDNode>(Op.getOperand(1));
+  if (!AmtNode)
+    report_fatal_error(
+        "Eclipse backend only supports shifts by a compile-time constant "
+        "amount (no variable-count shift/rotate hardware is exposed yet — "
+        "see README.md)");
+  uint64_t Amt = AmtNode->getZExtValue();
+
+  if (Op.getOpcode() == ISD::SHL) {
+    // Self-add, chained: each iteration's two operands are both just the
+    // previous iteration's fresh result — no operand needs to survive
+    // *across* iterations, so this doesn't hit the bug documented below.
+    SDValue Result = X;
+    for (uint64_t I = 0; I < Amt && I < 16; ++I)
+      Result = DAG.getNode(ISD::ADD, dl, MVT::i16, Result, Result);
+    return Result;
+  }
+
+  // SRL/SRA used to divide by 2, Amt times, chaining Result through a
+  // *shared* "Two" divisor operand across iterations — mathematically
+  // fine (x >>u N == x / 2^N, and arithmetic right shift is floor
+  // division by 2^N for any N, not just N=1, so the same truncating-
+  // division rounding correction below applies regardless of how big the
+  // divisor is), but it triggered a real, separate backend bug: UDIVrr's
+  // post-RA expansion (EclipseInstrInfo.cpp) always routes the dividend
+  // and quotient through AC1, physically overwriting whatever was there
+  // — including "Two" itself, if the register allocator had (reasonably,
+  // from its own view — UDIVrr's declared operands don't say AC1 gets
+  // clobbered beyond $dst) chosen to keep the shared divisor live in AC1
+  // across the chain for reuse. Confirmed with eclipseemu: a chained
+  // x/8 (three x/2 steps, from 8's power-of-two strength reduction)
+  // computed the *second* step's divisor as the *first* step's quotient
+  // instead of 2, corrupting every value from index 1 of a `for` loop
+  // digit-extraction onward. A single divide by the *full* 2^Amt divisor
+  // sidesteps this rather than fixing UDIVrr's register-allocation
+  // interaction directly — this project already tried marking UDIVrr's
+  // implicit AC0/AC1 clobber explicitly once (see EclipseInstrInfo.td's
+  // MUL/DIV comment) and that broke allocation outright ("ran out of
+  // registers"), so this is the lower-risk fix.
+  if (Amt >= 16) {
+    if (Op.getOpcode() == ISD::SRL)
+      return DAG.getConstant(0, dl, MVT::i16);
+    // SRA of a full-width-or-wider amount: all-sign-bit fill, i.e. 0 or
+    // -1 depending on X's sign — built from SETCC-then-negate rather
+    // than a fresh SELECT_CC, since 0/1 -> 0/-1 is just "0 minus the
+    // boolean".
+    SDValue Zero = DAG.getConstant(0, dl, MVT::i16);
+    SDValue Neg = DAG.getSetCC(dl, MVT::i16, X, Zero, ISD::SETLT);
+    return DAG.getNode(ISD::SUB, dl, MVT::i16, Zero, Neg);
+  }
+
+  SDValue Divisor = DAG.getConstant(uint64_t(1) << Amt, dl, MVT::i16);
+  // EclipseISD::HALVE, not a plain ISD::UDIV: DAGCombiner's generic "udiv
+  // by a power-of-two constant -> shift" fold (DAGCombiner.cpp's
+  // visitUDIVLike) is unconditional -- it doesn't check whether SRL is
+  // actually cheap/Legal here before firing -- so a plain UDIV(X, 2^Amt)
+  // gets folded straight back into SRL(X, Amt), which this function's own
+  // Custom lowering (since SRL is only ever Custom, never strictly Legal)
+  // re-expands right back into UDIV(X, 2^Amt), forever. This is the same
+  // ping-pong EclipseISD::HALVE's own comment (below, and this file's
+  // header) already documents fixing for the ADD-combine's array-offset
+  // halving; that fix never covered *this* SRL path, which independently
+  // hits the identical generic fold — confirmed by reaching this exact
+  // node for a plain `unsigned x; x = x / 2u;` (a genuine runtime UDIV
+  // with a power-of-2 constant divisor, so DAGCombiner's fold applies
+  // regardless of isIntDivCheap, unlike the SDIV case isIntDivCheap
+  // below fixes).
+  if (Op.getOpcode() == ISD::SRL)
+    return DAG.getNode(EclipseISD::HALVE, dl, MVT::i16, X, Divisor);
+
+  assert(Op.getOpcode() == ISD::SRA && "not a shift opcode");
+  SDValue Zero = DAG.getConstant(0, dl, MVT::i16);
+  SDValue Q = DAG.getNode(ISD::SDIV, dl, MVT::i16, X, Divisor);
+  SDValue R = DAG.getNode(ISD::SREM, dl, MVT::i16, X, Divisor);
+  SDValue Neg = DAG.getSetCC(dl, MVT::i16, X, Zero, ISD::SETLT);
+  SDValue RNZ = DAG.getSetCC(dl, MVT::i16, R, Zero, ISD::SETNE);
+  SDValue Adj = DAG.getNode(ISD::AND, dl, MVT::i16, Neg, RNZ);
+  return DAG.getNode(ISD::SUB, dl, MVT::i16, Q, Adj);
+}
+
+SDValue EclipseTargetLowering::LowerSDIVREM(SDValue Op, SelectionDAG &DAG) const {
+  bool WantRemainder = Op.getOpcode() == ISD::SREM;
+  SDLoc dl(Op);
+  SDValue LHS = Op.getOperand(0);
+  SDValue RHS = Op.getOperand(1);
+  SDValue Zero = DAG.getConstant(0, dl, MVT::i16);
+
+  auto AbsVal = [&](SDValue X) {
+    SDValue NegX = DAG.getNode(ISD::SUB, dl, MVT::i16, Zero, X);
+    return DAG.getNode(ISD::SELECT_CC, dl, MVT::i16, X, Zero, NegX, X,
+                        DAG.getCondCode(ISD::SETLT));
+  };
+  SDValue AbsLHS = AbsVal(LHS);
+  SDValue AbsRHS = AbsVal(RHS);
+
+  if (WantRemainder) {
+    SDValue URem = DAG.getNode(ISD::UREM, dl, MVT::i16, AbsLHS, AbsRHS);
+    // C's `%` result takes the sign of the dividend.
+    SDValue NegRem = DAG.getNode(ISD::SUB, dl, MVT::i16, Zero, URem);
+    return DAG.getNode(ISD::SELECT_CC, dl, MVT::i16, LHS, Zero, NegRem, URem,
+                        DAG.getCondCode(ISD::SETLT));
+  }
+
+  // EclipseISD::HALVE, not a plain ISD::UDIV -- see LowerShift's SRL case
+  // for the full explanation of the ping-pong a plain UDIV risks here.
+  // AbsRHS is a genuine runtime SDValue at this point (not yet known to
+  // be constant), but DAGCombiner can later fold it down to a
+  // compile-time constant (AbsVal(2) trivially simplifies: 2 is not < 0,
+  // so SELECT_CC picks the unchanged "2" operand) -- and *if* that
+  // constant happens to be a power of 2, a plain UDIV here would be
+  // exposed to the identical generic "udiv by pow2 -> shift" fold as the
+  // SRL case, ping-ponging against LowerShift's own SRL lowering.
+  // Preemptive, not itself the fix for any specific confirmed bug here:
+  // the actual `x = x / 2` miscompile traced to a *different* mechanism
+  // (see isIntDivCheap's own comment in the header) that bypasses
+  // LowerSDIVREM entirely via DAGCombiner's own SDIV-by-pow2 expansion,
+  // so this UQuot node was never actually reached by that specific
+  // symptom -- but the same UDIV/SRL round-trip hazard applies here in
+  // principle, exactly as it's confirmed to for the SRL case above.
+  SDValue UQuot = DAG.getNode(EclipseISD::HALVE, dl, MVT::i16, AbsLHS, AbsRHS);
+  SDValue NegQuot = DAG.getNode(ISD::SUB, dl, MVT::i16, Zero, UQuot);
+  // Quotient is negative iff exactly one of LHS/RHS was negative.
+  SDValue LhsNeg = DAG.getSetCC(dl, MVT::i16, LHS, Zero, ISD::SETLT);
+  SDValue RhsNeg = DAG.getSetCC(dl, MVT::i16, RHS, Zero, ISD::SETLT);
+  SDValue QuotNeg = DAG.getNode(ISD::XOR, dl, MVT::i16, LhsNeg, RhsNeg);
+  return DAG.getNode(ISD::SELECT_CC, dl, MVT::i16, QuotNeg, Zero, NegQuot,
+                      UQuot, DAG.getCondCode(ISD::SETNE));
+}
+
+SDValue EclipseTargetLowering::LowerConstantPool(SDValue Op,
+                                                  SelectionDAG &DAG) const {
+  const auto *N = cast<ConstantPoolSDNode>(Op);
+  SDLoc dl(Op);
+  SDValue CP = DAG.getTargetConstantPool(N->getConstVal(), MVT::i16,
+                                          N->getAlign(), N->getOffset());
+  return DAG.getNode(EclipseISD::WRAPPER, dl, MVT::i16, CP);
+}
+
+//===----------------------------------------------------------------------===//
+// Comparisons / branches: everything reduces to a subtraction plus one of
+// the skip-test conditional branches. See docs/CALLING_CONVENTION.md's
+// verified idiom in the sibling eccc project.
+//
+// Unsigned comparisons do NOT reuse the signed sign-test path (BGEZ/BLTZ)
+// — an earlier version did, matching eccc's own documented limitation,
+// but that's a genuine bug here, not just an acceptable simplification:
+// BGEZ/BLTZ test the *sign bit* of a separately-computed SUB result,
+// which disagrees with the true unsigned relation whenever the
+// difference's sign bit doesn't match (e.g. 65535u >= 8u is true, but
+// 65535-8 = 65527 has its sign bit set). Confirmed via eclipseemu:
+// printf("%o", val) for any unsigned val >= 32768 silently printed
+// garbage as a result, since the hand-rolled print_octal's recursive
+// `if (val >= 8)` never took its true branch. BGEU/BLTU (see
+// EclipseISD::BGEU's comment) test the native carry from "SUB#
+// rhs,lhs" directly instead.
+//===----------------------------------------------------------------------===//
+
+static bool isUnsignedCC(ISD::CondCode CC) {
+  switch (CC) {
+  case ISD::SETULT:
+  case ISD::SETUGE:
+  case ISD::SETUGT:
+  case ISD::SETULE:
+    return true;
+  default:
+    return false;
+  }
+}
+
+static EclipseISD::NodeType canonBranchOpcode(ISD::CondCode CC, SDValue &LHS,
+                                               SDValue &RHS) {
+  switch (CC) {
+  case ISD::SETEQ:
+    return EclipseISD::BRZ;
+  case ISD::SETNE:
+    return EclipseISD::BRNZ;
+  case ISD::SETLT:
+    return EclipseISD::BLTZ;
+  case ISD::SETULT:
+    return EclipseISD::BLTU;
+  case ISD::SETGE:
+    return EclipseISD::BGEZ;
+  case ISD::SETUGE:
+    return EclipseISD::BGEU;
+  case ISD::SETGT:
+    std::swap(LHS, RHS);
+    return EclipseISD::BLTZ;
+  case ISD::SETUGT:
+    std::swap(LHS, RHS);
+    return EclipseISD::BLTU;
+  case ISD::SETLE:
+    std::swap(LHS, RHS);
+    return EclipseISD::BGEZ;
+  case ISD::SETULE:
+    std::swap(LHS, RHS);
+    return EclipseISD::BGEU;
+  default:
+    llvm_unreachable("unsupported Eclipse condition code");
+  }
+}
+
+static unsigned canonSelectOpcode(ISD::CondCode CC, SDValue &LHS,
+                                   SDValue &RHS) {
+  switch (CC) {
+  case ISD::SETEQ:
+    return Eclipse::BRZ;
+  case ISD::SETNE:
+    return Eclipse::BRNZ;
+  case ISD::SETLT:
+    return Eclipse::BLTZ;
+  case ISD::SETULT:
+    return Eclipse::BLTU;
+  case ISD::SETGE:
+    return Eclipse::BGEZ;
+  case ISD::SETUGE:
+    return Eclipse::BGEU;
+  case ISD::SETGT:
+    std::swap(LHS, RHS);
+    return Eclipse::BLTZ;
+  case ISD::SETUGT:
+    std::swap(LHS, RHS);
+    return Eclipse::BLTU;
+  case ISD::SETLE:
+    std::swap(LHS, RHS);
+    return Eclipse::BGEZ;
+  case ISD::SETULE:
+    std::swap(LHS, RHS);
+    return Eclipse::BGEU;
+  default:
+    llvm_unreachable("unsupported Eclipse condition code");
+  }
+}
+
+SDValue EclipseTargetLowering::LowerBR_CC(SDValue Op, SelectionDAG &DAG) const {
+  SDLoc dl(Op);
+  SDValue Chain = Op.getOperand(0);
+  ISD::CondCode CC = cast<CondCodeSDNode>(Op.getOperand(1))->get();
+  SDValue LHS = Op.getOperand(2);
+  SDValue RHS = Op.getOperand(3);
+  SDValue Dest = Op.getOperand(4);
+
+  bool Unsigned = isUnsignedCC(CC);
+  EclipseISD::NodeType NodeOpc = canonBranchOpcode(CC, LHS, RHS);
+  if (Unsigned)
+    return DAG.getNode(NodeOpc, dl, MVT::Other, Chain, LHS, RHS, Dest);
+
+  SDValue Cmp = DAG.getNode(ISD::SUB, dl, MVT::i16, LHS, RHS);
+  return DAG.getNode(NodeOpc, dl, MVT::Other, Chain, Cmp, Dest);
+}
+
+// See the ISD::BRCOND setOperationAction comment in the constructor for
+// why this exists as its own Custom lowering rather than relying on
+// generic Expand: reduces `brcond Chain, Cond, Dest` to the equivalent
+// `br_cc Chain, SETNE, Cond, 0, Dest` and hands it to the already-robust
+// LowerBR_CC, rather than duplicating that logic.
+SDValue EclipseTargetLowering::LowerBRCOND(SDValue Op, SelectionDAG &DAG) const {
+  SDLoc dl(Op);
+  SDValue Chain = Op.getOperand(0);
+  SDValue Cond = Op.getOperand(1);
+  SDValue Dest = Op.getOperand(2);
+  SDValue Zero = DAG.getConstant(0, dl, MVT::i16);
+  SDValue BRCC =
+      DAG.getNode(ISD::BR_CC, dl, MVT::Other, Chain,
+                  DAG.getCondCode(ISD::SETNE), Cond, Zero, Dest);
+  return LowerBR_CC(BRCC, DAG);
+}
+
+SDValue EclipseTargetLowering::LowerSELECT_CC(SDValue Op,
+                                               SelectionDAG &DAG) const {
+  SDLoc dl(Op);
+  SDValue LHS = Op.getOperand(0);
+  SDValue RHS = Op.getOperand(1);
+  SDValue True = Op.getOperand(2);
+  SDValue False = Op.getOperand(3);
+  ISD::CondCode CC = cast<CondCodeSDNode>(Op.getOperand(4))->get();
+
+  bool Unsigned = isUnsignedCC(CC);
+  unsigned CondOpc = canonSelectOpcode(CC, LHS, RHS);
+  if (Unsigned) {
+    SDValue Ops[] = {LHS, RHS, DAG.getTargetConstant(CondOpc, dl, MVT::i16),
+                      True, False};
+    return SDValue(DAG.getMachineNode(Eclipse::SelectU, dl, MVT::i16, Ops), 0);
+  }
+  SDValue Cmp = DAG.getNode(ISD::SUB, dl, MVT::i16, LHS, RHS);
+  SDValue Ops[] = {Cmp, DAG.getTargetConstant(CondOpc, dl, MVT::i16), True,
+                    False};
+  return SDValue(DAG.getMachineNode(Eclipse::Select, dl, MVT::i16, Ops), 0);
+}
+
+SDValue EclipseTargetLowering::LowerSETCC(SDValue Op, SelectionDAG &DAG) const {
+  SDLoc dl(Op);
+  SDValue LHS = Op.getOperand(0);
+  SDValue RHS = Op.getOperand(1);
+  ISD::CondCode CC = cast<CondCodeSDNode>(Op.getOperand(2))->get();
+
+  bool Unsigned = isUnsignedCC(CC);
+  unsigned CondOpc = canonSelectOpcode(CC, LHS, RHS);
+  SDValue One = DAG.getConstant(1, dl, MVT::i16);
+  SDValue Zero = DAG.getConstant(0, dl, MVT::i16);
+  if (Unsigned) {
+    SDValue Ops[] = {LHS, RHS, DAG.getTargetConstant(CondOpc, dl, MVT::i16),
+                      One, Zero};
+    return SDValue(DAG.getMachineNode(Eclipse::SelectU, dl, MVT::i16, Ops), 0);
+  }
+  SDValue Cmp = DAG.getNode(ISD::SUB, dl, MVT::i16, LHS, RHS);
+  SDValue Ops[] = {Cmp, DAG.getTargetConstant(CondOpc, dl, MVT::i16), One,
+                    Zero};
+  return SDValue(DAG.getMachineNode(Eclipse::Select, dl, MVT::i16, Ops), 0);
+}
+
+//===----------------------------------------------------------------------===//
+// Calling convention. Every argument is pushed on the software stack,
+// right-to-left, one word each (the common SelectionDAG type legalizer has
+// already split any wider-than-i16 argument into multiple i16 pieces by
+// the time LowerCall/LowerFormalArguments see them — see the file
+// header). No argument or return-value registers, matching eccc's own
+// convention exactly: see docs/CALLING_CONVENTION.md.
+//===----------------------------------------------------------------------===//
+
+SDValue EclipseTargetLowering::LowerFormalArguments(
+    SDValue Chain, CallingConv::ID /*CallConv*/, bool IsVarArg,
+    const SmallVectorImpl<ISD::InputArg> &Ins, const SDLoc &dl,
+    SelectionDAG &DAG, SmallVectorImpl<SDValue> &InVals) const {
+  MachineFunction &MF = DAG.getMachineFunction();
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+
+  for (unsigned i = 0, e = Ins.size(); i != e; ++i) {
+    // FP+2 = first parameter (pushed last by the caller, so shallowest);
+    // FP+0/FP+1 are the callee's own saved return address / caller FP.
+    int FI = MFI.CreateFixedObject(kWordBytes, /*SPOffset*/ 2 + i,
+                                    /*IsImmutable*/ true);
+    SDValue FIN = DAG.getFrameIndex(FI, MVT::i16);
+    InVals.push_back(DAG.getLoad(MVT::i16, dl, Chain, FIN,
+                                  MachinePointerInfo::getFixedStack(MF, FI)));
+  }
+
+  if (IsVarArg) {
+    // The "..." arguments are pushed right-to-left same as any named
+    // one (LowerCall), so they simply continue immediately after the
+    // last named parameter at FP+2+Ins.size() — no distinct varargs
+    // save area to build. va_start (LowerVASTART) just needs this frame
+    // index's address.
+    int FI = MFI.CreateFixedObject(kWordBytes, /*SPOffset*/ 2 + Ins.size(),
+                                    /*IsImmutable*/ true);
+    MF.getInfo<EclipseMachineFunctionInfo>()->setVarArgsFrameIndex(FI);
+  }
+
+  return Chain;
+}
+
+SDValue EclipseTargetLowering::LowerVASTART(SDValue Op,
+                                             SelectionDAG &DAG) const {
+  MachineFunction &MF = DAG.getMachineFunction();
+  auto *FuncInfo = MF.getInfo<EclipseMachineFunctionInfo>();
+  SDLoc dl(Op);
+
+  // Same mechanism as `&local`: a bare FrameIndex value gets custom-
+  // selected to LEAFI (EclipseISelDAGToDAG.cpp), materializing the
+  // varargs area's real address into a register.
+  SDValue FrameIndex =
+      DAG.getFrameIndex(FuncInfo->getVarArgsFrameIndex(), MVT::i16);
+  const Value *SV = cast<SrcValueSDNode>(Op.getOperand(2))->getValue();
+  return DAG.getStore(Op.getOperand(0), dl, FrameIndex, Op.getOperand(1),
+                       MachinePointerInfo(SV));
+}
+
+SDValue EclipseTargetLowering::LowerVAARG(SDValue Op, SelectionDAG &DAG) const {
+  SDNode *Node = Op.getNode();
+  SDValue Chain = Node->getOperand(0);
+  SDValue VAListPtr = Node->getOperand(1);
+  EVT VT = Node->getValueType(0);
+  SDLoc dl(Node);
+
+  // va_list itself is just a plain i16 pointer variable (see
+  // LowerVASTART) — load its current value, ...
+  SDValue VAList =
+      DAG.getLoad(MVT::i16, dl, Chain, VAListPtr, MachinePointerInfo());
+  // ... read the argument from wherever it points (no alignment
+  // adjustment: see the setOperationAction comment above), ...
+  SDValue Result =
+      DAG.getLoad(VT, dl, VAList.getValue(1), VAList, MachinePointerInfo());
+  // ... and advance it by exactly one word for the next va_arg call.
+  // Plain `1`, not kWordBytes(2): ordinary pointer arithmetic elsewhere
+  // in this backend (e.g. `s++` in eclipse_rt.c's puts()) already
+  // resolves an i16 element's GEP scale to 1 real word, not 2 — kWordBytes
+  // is a MachineFrameInfo *byte-size* convention (CreateFixedObject),
+  // unrelated to this runtime-pointer increment. Using kWordBytes here
+  // silently advanced two words per va_arg instead of one.
+  SDValue NextPtr = DAG.getNode(ISD::ADD, dl, MVT::i16, VAList,
+                                 DAG.getConstant(1, dl, MVT::i16));
+  SDValue Chain2 = DAG.getStore(Result.getValue(1), dl, NextPtr, VAListPtr,
+                                 MachinePointerInfo());
+
+  SDValue Ops[2] = {Result, Chain2};
+  return DAG.getMergeValues(Ops, dl);
+}
+
+SDValue
+EclipseTargetLowering::LowerCall(TargetLowering::CallLoweringInfo &CLI,
+                                  SmallVectorImpl<SDValue> &InVals) const {
+  SelectionDAG &DAG = CLI.DAG;
+  SDLoc &dl = CLI.DL;
+  SmallVectorImpl<ISD::OutputArg> &Outs = CLI.Outs;
+  SmallVectorImpl<SDValue> &OutVals = CLI.OutVals;
+  SmallVectorImpl<ISD::InputArg> &Ins = CLI.Ins;
+  SDValue Chain = CLI.Chain;
+  SDValue Callee = CLI.Callee;
+  CLI.IsTailCall = false; // no tail-call support
+
+  // Calling a variadic function needs no special handling here: every
+  // argument, named or "...", is pushed the same way (right-to-left,
+  // below) — the callee's LowerFormalArguments is what distinguishes
+  // named parameters from the varargs area.
+
+  unsigned NumWords = Outs.size();
+  // 0, not NumWords: PUSH (emitPushPop) already reserves its own word via
+  // its own DSZABS before storing — ADJCALLSTACKDOWN reserving NumWords
+  // *again* here double-decrements _SP per argument. Only the "up" side
+  // needs NumWords, to release what PUSH actually reserved. A real,
+  // empirically-found bug: with both sides at NumWords, _SP leaked one
+  // word per pushed argument on every call, invisible in single-call-
+  // depth tests but corrupting AC2/AC3 (frame pointer / return address)
+  // after enough nested/looped calls — e.g. `puts()` calling `putchar()`
+  // in its own loop leaked enough words to return through a garbage AC3.
+  Chain = DAG.getCALLSEQ_START(Chain, 0, 0, dl);
+
+  // Push right-to-left (last parameter pushed first) — see
+  // docs/CALLING_CONVENTION.md.
+  SDValue InGlue;
+  SmallVector<SDValue, 8> PushChains;
+  for (unsigned i = NumWords; i != 0; --i) {
+    SDValue Arg = OutVals[i - 1];
+    SDValue Ops[] = {Arg, Chain}; // value operand(s) first, chain last
+    Chain = SDValue(DAG.getMachineNode(Eclipse::PUSH, dl, MVT::Other, Ops), 0);
+  }
+
+  // Indirect through a page-zero jump-table slot named "<callee>_SLOT" —
+  // see EclipseInstrInfo.td's CALL def and docs/CALLING_CONVENTION.md.
+  // Always an ExternalSymbol operand (even for a real GlobalValue callee):
+  // "<name>_SLOT" isn't itself a GlobalValue, just a compiler-synthesized
+  // data symbol EclipseAsmPrinter emits alongside it.
+  std::string SlotName;
+  if (auto *G = dyn_cast<GlobalAddressSDNode>(Callee))
+    SlotName = (G->getGlobal()->getName() + "_SLOT").str();
+  else if (auto *E = dyn_cast<ExternalSymbolSDNode>(Callee))
+    SlotName = (Twine(E->getSymbol()) + "_SLOT").str();
+  else {
+    // A genuinely indirect call: Callee is a runtime value (already
+    // legalized down to something that will occupy a GPR by the time
+    // this reaches ISel — a loaded function pointer, the result of
+    // another call, etc.), not a symbol CALL's own Pat can build a
+    // "<callee>_SLOT" name from. Store the computed address into ONE
+    // shared, dedicated slot at runtime instead (CALLSLOT_STORE, see
+    // EclipseInstrInfo.td), then fall through to the exact same
+    // CALL/"JSR @slot,0" mechanism every direct call already uses, just
+    // targeting that fixed slot's name rather than a per-function one.
+    SDValue Ops[] = {Callee, Chain};
+    Chain = SDValue(
+        DAG.getMachineNode(Eclipse::CALLSLOT_STORE, dl, MVT::Other, Ops), 0);
+    SlotName = EclipseIndirectCallSlotName;
+  }
+  const char *PersistentName =
+      DAG.getMachineFunction().createExternalSymbolName(SlotName);
+  Callee = DAG.getTargetExternalSymbol(PersistentName, MVT::i16);
+
+  SDVTList NodeTys = DAG.getVTList(MVT::Other, MVT::Glue);
+  SmallVector<SDValue, 4> CallOps{Chain, Callee};
+  Chain = DAG.getNode(EclipseISD::CALL, dl, NodeTys, CallOps);
+  InGlue = Chain.getValue(1);
+
+  Chain = DAG.getCALLSEQ_END(Chain, NumWords, 0, InGlue, dl);
+  InGlue = Chain.getValue(1);
+
+  // Return value (if any) comes back in AC0 (single word) or AC0:AC1 (a
+  // 2-word/32-bit value, e.g. `long` or soft-float `float` — both
+  // registers are free at this exact point, nothing else can be live
+  // yet, so a register pair needs no spill/reload machinery) — see
+  // docs/CALLING_CONVENTION.md and LowerReturn's matching half of this.
+  if (!Ins.empty()) {
+    SDValue Val0 =
+        DAG.getCopyFromReg(Chain, dl, Eclipse::AC0, MVT::i16, InGlue);
+    Chain = Val0.getValue(1);
+    InVals.push_back(Val0.getValue(0));
+
+    if (Ins.size() == 2) {
+      SDValue Val1 = DAG.getCopyFromReg(Chain, dl, Eclipse::AC1, MVT::i16,
+                                         Val0.getValue(2));
+      Chain = Val1.getValue(1);
+      InVals.push_back(Val1.getValue(0));
+    } else if (Ins.size() > 2) {
+      report_fatal_error("Eclipse backend only supports single- or "
+                          "double-word (i16/32-bit) return values today");
+    }
+  }
+  return Chain;
+}
+
+SDValue EclipseTargetLowering::LowerReturn(
+    SDValue Chain, CallingConv::ID /*CallConv*/, bool /*IsVarArg*/,
+    const SmallVectorImpl<ISD::OutputArg> &Outs,
+    const SmallVectorImpl<SDValue> &OutVals, const SDLoc &dl,
+    SelectionDAG &DAG) const {
+  // Returning from a variadic function is no different from any other —
+  // only the parameter list is variadic, not the return path.
+  //
+  // 2-word (32-bit) returns go out in AC0:AC1 — the register-pair
+  // counterpart to LowerCall's read-back. Confirmed empirically (not
+  // just assumed) that OutVals[0]/OutVals[1] arrive in the same order
+  // the type legalizer's own big-endian ("E" in computeDataLayout) i32
+  // splitting produces, i.e. this file doesn't choose that order itself.
+  if (Outs.size() > 2)
+    report_fatal_error("Eclipse backend only supports single- or "
+                        "double-word (i16/32-bit) return values today");
+
+  if (DAG.getMachineFunction().getFunction().hasFnAttribute("interrupt")) {
+    // Clang's Sema already rejects a non-void __attribute__((interrupt))
+    // function, so Outs is empty here — hardware interrupt entry has no
+    // return-value slot to fill anyway.
+    assert(Outs.empty() && "interrupt handler must be void");
+    return DAG.getNode(EclipseISD::RETI_FLAG, dl, MVT::Other, Chain);
+  }
+
+  SDValue Glue;
+  SmallVector<SDValue, 4> RetOps{Chain};
+  if (!OutVals.empty()) {
+    Chain = DAG.getCopyToReg(Chain, dl, Eclipse::AC0, OutVals[0], Glue);
+    Glue = Chain.getValue(1);
+    RetOps.push_back(DAG.getRegister(Eclipse::AC0, MVT::i16));
+
+    if (OutVals.size() == 2) {
+      Chain = DAG.getCopyToReg(Chain, dl, Eclipse::AC1, OutVals[1], Glue);
+      Glue = Chain.getValue(1);
+      RetOps.push_back(DAG.getRegister(Eclipse::AC1, MVT::i16));
+    }
+    RetOps[0] = Chain;
+  }
+  if (Glue.getNode())
+    RetOps.push_back(Glue);
+
+  return DAG.getNode(EclipseISD::RET_FLAG, dl, MVT::Other, RetOps);
+}
+
+//===----------------------------------------------------------------------===//
+// Custom-inserted pseudo expansion: everything that needs more than one
+// real instruction, or fixed-physical-register plumbing the DAG can't
+// express directly, is a Pseudo (see EclipseInstrInfo.td) expanded here
+// into real MachineInstrs after instruction selection.
+//===----------------------------------------------------------------------===//
+
+MachineBasicBlock *
+EclipseTargetLowering::EmitInstrWithCustomInserter(MachineInstr &MI,
+                                                    MachineBasicBlock *MBB) const {
+  switch (MI.getOpcode()) {
+  case Eclipse::Select:
+    return emitSelectCC(MI, MBB);
+  case Eclipse::SelectU:
+    return emitSelectCCU(MI, MBB);
+  case Eclipse::PUSH:
+    return emitPushPop(MI, MBB, /*IsPush*/ true);
+  case Eclipse::POP:
+    return emitPushPop(MI, MBB, /*IsPush*/ false);
+  case Eclipse::ADJCALLSTACKDOWN:
+    return emitAdjCallStack(MI, MBB, /*IsDown*/ true);
+  case Eclipse::ADJCALLSTACKUP:
+    return emitAdjCallStack(MI, MBB, /*IsDown*/ false);
+  // MULrr/UDIVrr/UREMrr are handled by EclipseInstrInfo::expandPostRAPseudo
+  // instead, not here — see EclipseInstrInfo.td's comment on why they
+  // need to expand *after* register allocation.
+  case Eclipse::LDIND:
+    return emitIndirectMem(MI, MBB, /*IsLoad*/ true);
+  case Eclipse::STIND:
+    return emitIndirectMem(MI, MBB, /*IsLoad*/ false);
+  case Eclipse::CALLSLOT_STORE:
+    return emitIndirectCallSlotStore(MI, MBB);
+  default:
+    llvm_unreachable("unexpected custom-inserted Eclipse pseudo");
+  }
+}
+
+// Expands the `Select` pseudo into the classic two-successor diamond: test
+// the comparison value with the chosen skip-branch, copy the false operand
+// straight through on the fallthrough edge, copy the true operand in a new
+// block, and PHI the two together. Standard technique for targets with no
+// predicated/conditional-move instruction — see e.g. how SPARC/Mips
+// implement `Select` pseudos the same way.
+MachineBasicBlock *EclipseTargetLowering::emitSelectCC(MachineInstr &MI,
+                                                        MachineBasicBlock *MBB) const {
+  const auto &TII = *MBB->getParent()->getSubtarget<EclipseSubtarget>().getInstrInfo();
+  DebugLoc DL = MI.getDebugLoc();
+
+  Register Dst = MI.getOperand(0).getReg();
+  Register CmpVal = MI.getOperand(1).getReg();
+  unsigned CondOpc = MI.getOperand(2).getImm();
+  Register TrueV = MI.getOperand(3).getReg();
+  Register FalseV = MI.getOperand(4).getReg();
+
+  MachineFunction *F = MBB->getParent();
+  MachineBasicBlock *TrueMBB = F->CreateMachineBasicBlock(MBB->getBasicBlock());
+  MachineBasicBlock *MergeMBB = F->CreateMachineBasicBlock(MBB->getBasicBlock());
+  MachineFunction::iterator It = ++MBB->getIterator();
+  F->insert(It, TrueMBB);
+  F->insert(It, MergeMBB);
+
+  MergeMBB->splice(MergeMBB->begin(), MBB, std::next(MI.getIterator()), MBB->end());
+  MergeMBB->transferSuccessorsAndUpdatePHIs(MBB);
+
+  MBB->addSuccessor(TrueMBB);
+  MBB->addSuccessor(MergeMBB);
+  BuildMI(MBB, DL, TII.get(CondOpc)).addReg(CmpVal).addMBB(TrueMBB);
+  BuildMI(MBB, DL, TII.get(Eclipse::JMP)).addMBB(MergeMBB);
+
+  TrueMBB->addSuccessor(MergeMBB);
+  BuildMI(TrueMBB, DL, TII.get(Eclipse::JMP)).addMBB(MergeMBB);
+
+  BuildMI(*MergeMBB, MergeMBB->begin(), DL, TII.get(TargetOpcode::PHI), Dst)
+      .addReg(FalseV)
+      .addMBB(MBB)
+      .addReg(TrueV)
+      .addMBB(TrueMBB);
+
+  MI.eraseFromParent();
+  return MergeMBB;
+}
+
+// Same diamond shape as emitSelectCC, but CondOpc is BGEU/BLTU, which take
+// two live operands (lhs, rhs) rather than one pre-computed comparison
+// value — see EclipseISD::BGEU's comment in EclipseISelLowering.h.
+MachineBasicBlock *EclipseTargetLowering::emitSelectCCU(MachineInstr &MI,
+                                                         MachineBasicBlock *MBB) const {
+  const auto &TII = *MBB->getParent()->getSubtarget<EclipseSubtarget>().getInstrInfo();
+  DebugLoc DL = MI.getDebugLoc();
+
+  Register Dst = MI.getOperand(0).getReg();
+  Register Lhs = MI.getOperand(1).getReg();
+  Register Rhs = MI.getOperand(2).getReg();
+  unsigned CondOpc = MI.getOperand(3).getImm();
+  Register TrueV = MI.getOperand(4).getReg();
+  Register FalseV = MI.getOperand(5).getReg();
+
+  MachineFunction *F = MBB->getParent();
+  MachineBasicBlock *TrueMBB = F->CreateMachineBasicBlock(MBB->getBasicBlock());
+  MachineBasicBlock *MergeMBB = F->CreateMachineBasicBlock(MBB->getBasicBlock());
+  MachineFunction::iterator It = ++MBB->getIterator();
+  F->insert(It, TrueMBB);
+  F->insert(It, MergeMBB);
+
+  MergeMBB->splice(MergeMBB->begin(), MBB, std::next(MI.getIterator()), MBB->end());
+  MergeMBB->transferSuccessorsAndUpdatePHIs(MBB);
+
+  MBB->addSuccessor(TrueMBB);
+  MBB->addSuccessor(MergeMBB);
+  BuildMI(MBB, DL, TII.get(CondOpc)).addReg(Lhs).addReg(Rhs).addMBB(TrueMBB);
+  BuildMI(MBB, DL, TII.get(Eclipse::JMP)).addMBB(MergeMBB);
+
+  TrueMBB->addSuccessor(MergeMBB);
+  BuildMI(TrueMBB, DL, TII.get(Eclipse::JMP)).addMBB(MergeMBB);
+
+  BuildMI(*MergeMBB, MergeMBB->begin(), DL, TII.get(TargetOpcode::PHI), Dst)
+      .addReg(FalseV)
+      .addMBB(MBB)
+      .addReg(TrueV)
+      .addMBB(TrueMBB);
+
+  MI.eraseFromParent();
+  return MergeMBB;
+}
+
+MachineBasicBlock *EclipseTargetLowering::emitPushPop(MachineInstr &MI,
+                                                       MachineBasicBlock *MBB,
+                                                       bool IsPush) const {
+  const auto &TII = *MBB->getParent()->getSubtarget<EclipseSubtarget>().getInstrInfo();
+  DebugLoc DL = MI.getDebugLoc();
+  if (IsPush) {
+    Register Src = MI.getOperand(0).getReg();
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::DSZABS)).addExternalSymbol("_SP");
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::STABSI))
+        .addReg(Src)
+        .addExternalSymbol("_SP");
+  } else {
+    Register Dst = MI.getOperand(0).getReg();
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::LDABSI), Dst).addExternalSymbol("_SP");
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::ISZABS)).addExternalSymbol("_SP");
+  }
+  MI.eraseFromParent();
+  return MBB;
+}
+
+MachineBasicBlock *EclipseTargetLowering::emitAdjCallStack(MachineInstr &MI,
+                                                            MachineBasicBlock *MBB,
+                                                            bool IsDown) const {
+  const auto &TII = *MBB->getParent()->getSubtarget<EclipseSubtarget>().getInstrInfo();
+  DebugLoc DL = MI.getDebugLoc();
+  int64_t Words = MI.getOperand(0).getImm();
+  unsigned Opc = IsDown ? Eclipse::DSZABS : Eclipse::ISZABS;
+  for (int64_t i = 0; i < Words; ++i)
+    BuildMI(*MBB, MI, DL, TII.get(Opc)).addExternalSymbol("_SP");
+  MI.eraseFromParent();
+  return MBB;
+}
+
+// MUL/UDIV/UREM are expanded post-register-allocation instead — see
+// EclipseInstrInfo::expandPostRAPseudo and EclipseInstrInfo.td's comment
+// on why (this pseudo temporarily repurposes AC2, our frame pointer, and
+// that's only safe once nothing else can be scheduled or spilled into
+// the middle of it — a real, verified-the-hard-way bug when this used to
+// be a pre-RA custom-insertion here instead).
+
+MachineBasicBlock *EclipseTargetLowering::emitIndirectMem(MachineInstr &MI,
+                                                           MachineBasicBlock *MBB,
+                                                           bool IsLoad) const {
+  const auto &TII = *MBB->getParent()->getSubtarget<EclipseSubtarget>().getInstrInfo();
+  DebugLoc DL = MI.getDebugLoc();
+  if (IsLoad) {
+    Register Dst = MI.getOperand(0).getReg();
+    Register Addr = MI.getOperand(1).getReg();
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::STABS)).addReg(Addr).addExternalSymbol("_scratch");
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::LDABSI), Dst).addExternalSymbol("_scratch");
+  } else {
+    Register Src = MI.getOperand(0).getReg();
+    Register Addr = MI.getOperand(1).getReg();
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::STABS)).addReg(Addr).addExternalSymbol("_scratch");
+    BuildMI(*MBB, MI, DL, TII.get(Eclipse::STABSI)).addReg(Src).addExternalSymbol("_scratch");
+  }
+  MI.eraseFromParent();
+  return MBB;
+}
+
+// Store the runtime callee address (already in a GPR by now) directly
+// into the shared indirect-call slot -- a plain absolute store (STABS,
+// "STA $src,$addr"), not the indirect STABSI emitIndirectMem uses for
+// `_scratch`: the slot IS the destination here, not a pointer to one.
+// See EclipseInstrInfo.td's CALLSLOT_STORE comment and LowerCall.
+MachineBasicBlock *
+EclipseTargetLowering::emitIndirectCallSlotStore(MachineInstr &MI,
+                                                  MachineBasicBlock *MBB) const {
+  const auto &TII = *MBB->getParent()->getSubtarget<EclipseSubtarget>().getInstrInfo();
+  DebugLoc DL = MI.getDebugLoc();
+  Register Src = MI.getOperand(0).getReg();
+  BuildMI(*MBB, MI, DL, TII.get(Eclipse::STABS))
+      .addReg(Src)
+      .addExternalSymbol(EclipseIndirectCallSlotName);
+  MI.eraseFromParent();
+  return MBB;
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseISelLowering.h b/llvm/lib/Target/Eclipse/EclipseISelLowering.h
new file mode 100644
index 000000000..2164defca
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseISelLowering.h
@@ -0,0 +1,326 @@
+//===-- EclipseISelLowering.h - Eclipse DAG Lowering Interface -*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// i16 is the only legal scalar type (see EclipseRegisterInfo.td / README.md
+// — this deliberately mirrors the sibling `eccc` project's own "char is
+// stored one per 16-bit word" simplification). i32 and float ops are never
+// given a register class, so LLVM's generic type legalizer handles them on
+// its own: i32 add/sub/and/or/xor/shift/compare decompose automatically
+// into pairs of legal i16 ops (its default "expand" behavior for an
+// oversized integer type), and i32 multiply/divide plus all float
+// arithmetic fall back to its default runtime-libcall behavior. This
+// backend does not hand-encode i32 carry propagation or Eclipse FPU
+// opcodes — see README.md "Known limitations" for why (nothing here would
+// be empirically verified against real hardware the way eccc's ISA facts
+// are, and a wrong hand-rolled encoding is worse than an honest libcall).
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEISELLOWERING_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEISELLOWERING_H
+
+#include "llvm/CodeGen/TargetLowering.h"
+
+namespace llvm {
+
+class EclipseSubtarget;
+
+namespace EclipseISD {
+enum NodeType {
+  FIRST_NUMBER = ISD::BUILTIN_OP_END,
+  WRAPPER,  // Wraps a GlobalAddress/ExternalSymbol/ConstantPool address so
+            // it can be used as an LDABS/STABS/CALL operand (the base
+            // Nova/Eclipse ISA has no immediate-operand instructions at
+            // all, hence always going through an addressed data word).
+  WORD_ADD, // Emitted only by PerformDAGCombine, in place of a plain
+            // ISD::ADD, for "WRAPPER'd base pointer + already word-scaled
+            // runtime offset". Deliberately a distinct opcode rather than
+            // a plain ADD: selects to the exact same ADDrr instruction
+            // (see EclipseInstrInfo.td), but being structurally unable to
+            // match PerformDAGCombine's own `N->getOpcode() == ISD::ADD`
+            // check guarantees the combine can never re-fire on its own
+            // output — an earlier version used a plain ADD with a
+            // heuristic "already halved" check instead, which produced an
+            // infinite DAGCombine loop.
+  HALVE,    // Unsigned divide-by-2 (dividend, pre-materialized constant 2),
+            // used only by PerformDAGCombine's WORD_ADD offset. A generic
+            // ISD::SRL/ISD::UDIV was tried first and produced its own
+            // infinite loop: LowerShift Custom-lowers SRL-by-1 into
+            // UDIV(x,2), and DAGCombiner's own standard "udiv by power of
+            // two -> shift" fold immediately turns that back into an
+            // SRL-by-1 — forever, since SRL is only Custom (never
+            // strictly Legal) so each generated SRL re-enters LowerShift.
+            // HALVE selects straight to UDIVrr (see EclipseInstrInfo.td)
+            // and is invisible to both of those generic rules.
+  COM,      // Emitted only by PerformDAGCombine's OR/XOR synthesis for
+            // base-Nova subtargets (no FeatureEIS — Eclipse.td), standing
+            // in for `xor(x, -1)` (bitwise NOT). Deliberately a distinct
+            // opcode rather than a plain ISD::XOR: generic DAGCombiner
+            // folds recognize `not(and(not(a), not(b)))` as De Morgan's
+            // `or(a, b)` and rewrite it straight back — which, since that
+            // *is* the original node PerformDAGCombine was trying to
+            // eliminate, re-triggers this combine, which resynthesizes
+            // the same NOT/AND/NOT shape, which gets refolded again,
+            // forever. Confirmed empirically (`llc` spinning at 100% CPU,
+            // never terminating) before switching to this distinct
+            // opcode — the same ping-pong class of bug WORD_ADD/HALVE
+            // above exist to avoid, just for OR/XOR instead of runtime
+            // GEP addressing. Selects straight to the existing COMrr
+            // instruction (EclipseInstrInfo.td), whose own `(not
+            // GPR:$src)` pattern (still a plain ISD::XOR-with-(-1)) is
+            // left completely untouched for genuine `~x` source
+            // expressions.
+  CALL,
+  RET_FLAG,
+  RETI_FLAG,
+  BRZ,
+  BRNZ,
+  BGEZ,
+  BLTZ,
+  BGEU,     // Unsigned >=. Deliberately *not* the same lowering path as
+            // BGEZ: BGEZ tests the *sign bit* of a separately-computed
+            // SUB result (rotated into carry via MOVL#), which is a
+            // signed ">=0" test — correct for signed comparisons, but
+            // wrong for unsigned ones whenever the true (unsigned)
+            // relation disagrees with the sign of the difference (e.g.
+            // 65535u >= 8u is true, but 65535-8 = 65527 has its sign bit
+            // set, so a sign-bit test says "false"). This was a real,
+            // confirmed bug — canonBranchOpcode used to route SETUGE to
+            // BGEZ, and print_octal("%o", val) for any val >= 32768
+            // silently produced garbage as a result (traced with
+            // eclipseemu: the recursive digit-extraction branch simply
+            // never taken). BGEU/BLTU instead test the *native* carry
+            // produced by "SUB# rhs,lhs" directly (no separate rotate) —
+            // see EclipseAsmPrinter.cpp's emission and dgnsdk's
+            // dgnasm/language.md ("Subtract... if this operation
+            // produces a carry value of 1, then invert the Carry bit" —
+            // the standard two's-complement subtract-via-add identity,
+            // confirmed against eclipseemu). Needs both LHS and RHS as
+            // live operands (unlike BGEZ/BLTZ's single pre-computed
+            // value), since the carry has to be tested at the moment of
+            // subtraction, not recovered later from a stored result.
+  BLTU,     // Unsigned <. See BGEU.
+};
+} // namespace EclipseISD
+
+class EclipseTargetLowering : public TargetLowering {
+public:
+  EclipseTargetLowering(const TargetMachine &TM, const EclipseSubtarget &STI);
+
+  SDValue LowerOperation(SDValue Op, SelectionDAG &DAG) const override;
+
+  // Tells DAGCombiner's generic sdiv/srem-by-constant-power-of-2
+  // strength-reduction (DAGCombiner.cpp's visitSDIVLike/buildOptimizedSREM,
+  // via TargetLowering::BuildSDIVPow2/BuildSREMPow2's own isIntDivCheap
+  // check) that this target's real DIV instruction is cheap enough not to
+  // bother -- so it leaves the original SDIV/SREM node alone for
+  // LowerSDIVREM to Custom-lower into one hardware DIV, instead of
+  // expanding it into a chain of SRA/SRL/ADD/SELECT nodes. That chain
+  // isn't just wasteful here: for a divisor of exactly 2, the expansion's
+  // own first step needs `SRA(x, 15)` (sign-bit replication, BitWidth-1),
+  // and this backend's LowerShift turns *any* SRA-by-N into a genuine
+  // "divide by 2^N" — so `SRA(x, 15)` became a real runtime divide by
+  // 2^15 truncated to i16 (DAG.getConstant(uint64_t(1)<<15, ..., i16),
+  // i.e. -32768), immediately followed by the expansion's *second*
+  // SRA(_, 1) (the real halving step) as a *separate* DIV — two
+  // back-to-back hardware DIVs sharing this target's tiny two-GPR
+  // register file, which silently produced a wrong quotient. Confirmed
+  // empirically: `x = x / 2` (x=10, a plain compile-time-constant
+  // divisor) computed 0 instead of 5, traced all the way back to this
+  // exact expansion via the wrong CPI0_1 = -32768 lands materialized. A
+  // single real DIV doesn't hit whatever the two-DIV interaction bug is
+  // (not separately root-caused; sidestepped instead, matching the
+  // existing "single divide by the full 2^Amt divisor" precedent for
+  // this backend's own chained-shift lowering, see LowerShift's header
+  // comment) and it's strictly fewer instructions besides.
+  bool isIntDivCheap(EVT VT, AttributeList Attr) const override {
+    return true;
+  }
+
+  // Fixes up runtime (non-compile-time-constant) array/pointer offsets
+  // added to a WRAPPER'd GlobalAddress base — see the .cpp for why this
+  // can't be handled in LowerGlobalAddress itself (the offset doesn't
+  // exist yet at that point) nor in LDIND's expansion (which can't tell a
+  // global-array-index address apart from an already-word-granular
+  // frame-index address).
+  SDValue PerformDAGCombine(SDNode *N, DAGCombinerInfo &DCI) const override;
+
+  MachineBasicBlock *
+  EmitInstrWithCustomInserter(MachineInstr &MI,
+                               MachineBasicBlock *MBB) const override;
+
+  const char *getTargetNodeName(unsigned Opcode) const override;
+
+  // memcpy/memmove/memset lowering must never coalesce two *unaligned*
+  // bytes into one wider (i16) load/store: unlike a real byte-addressed
+  // machine, this target never actually packs two bytes into one word —
+  // every i8 gets its own full word (see this file's header comment and
+  // dgasm's VARIABLE_STRING format) *unless* the data is genuinely
+  // 2-byte-aligned i16 content (a numeric array/struct field, where 2
+  // DataLayout bytes really do mean 1 word — see EclipseAsmPrinter.cpp's
+  // emitGlobalVariable, one `var` line per numeric element). The generic
+  // default (EVT::Other) picks i16 chunks for anything long enough to
+  // look profitable regardless of alignment, then — for a 1-byte-aligned
+  // source (e.g. a `char arr[N] = {...}` initializer copied from a
+  // string constant) — expands each into two i8 sub-accesses combined
+  // via shift+or, as if reading from a genuinely packed 2-bytes-per-word
+  // source. It isn't, so the combined value is meaningless, and storing
+  // it back out corrupts whichever byte doesn't survive the round trip.
+  // Only fall back to i8 (one access per byte, matching what this target
+  // can actually do) when the *generic* logic couldn't safely assume
+  // 2-byte alignment either — an earlier version forced i8
+  // unconditionally, which broke ordinary aligned array copies (e.g.
+  // `int arr[] = {1,2,3,4,5};`) by pushing them over
+  // getMaxStoresPerMemcpy's chunk-count limit into a `memcpy` libcall
+  // this freestanding target has no way to lower (confirmed via a real
+  // `LowerCall` crash tracing straight back to SelectionDAG::getMemcpy).
+  EVT getOptimalMemOpType(LLVMContext &Context, const MemOp &Op,
+                           const AttributeList &FuncAttributes) const override {
+    return Op.isAligned(Align(2)) ? MVT::Other : MVT::i8;
+  }
+
+  // Same word-vs-byte confusion as getOptimalMemOpType above, hitting a
+  // different generic transform: TargetLowering::SimplifySetCC's "shorten
+  // the load feeding `(and load, const) == 0`" combine (TargetLowering.cpp,
+  // the `(LHS is '(and load, const)' ...` block) and DAGCombiner's sibling
+  // ReduceLoadWidth/ReduceLoadOpStoreWidth combines. Given `if (b & (1L <<
+  // 27))` on a 32-bit local `b`, this generic combine sees that only bit 27
+  // is ever demanded, notices all of the mask's set bits fall inside a
+  // single *byte* of the 32-bit value, and rewrites the whole `and load,
+  // const` into a narrower i8 load at that byte's offset plus a
+  // correspondingly right-shifted 8-bit mask (here: an i8 load at byte
+  // offset 0 ANDed with 0x08, standing in for the original `and (i32 load),
+  // 0x08000000`). That rewrite is only sound on hardware where an i8 load
+  // at a nonzero/interior byte offset actually extracts *that specific
+  // byte*, right-justified — true on essentially every byte-addressable
+  // target, but not this one: Eclipse has no LDB/STB (see this file's
+  // header comment on `char` storage), so EclipseISelDAGToDAG's i8-extload
+  // selection just loads the *containing 16-bit word verbatim*, unshifted.
+  // Declaring i8 EXTLOAD-from-i16 "Legal" above is correct only for loads
+  // that were already i8 in the IR (a real `char`, stored one-per-word, top
+  // byte always zero — no actual narrowing happens); it does not hold for
+  // an i8 load *synthesized* by narrowing a wider (e.g. i32) load down to
+  // one byte of a multi-word value, which is exactly what these combines
+  // do. Left unguarded, the synthesized load returns the whole word
+  // (e.g. 2048 = 0x0800) while the combine's precomputed mask (0x08)
+  // targets the low 8 bits it *assumed* the load would land in — so
+  // `AND(2048, 8)` is always 0 regardless of the true value of `b`,
+  // silently mis-evaluating the branch condition. Confirmed directly via
+  // `llc -debug-only=isel` on `if (b & (1L << 27)) b -= 1L << 28;`: the
+  // pre-legalization combine rewrote `and i32 %load, 134217728` into `and
+  // i8 %narrowload, 8`, and the emitted LDFI loaded the unshifted high word
+  // — the branch was silently never taken for *any* value of `b`, which
+  // from the C level looked like "the if-branch's store to `b` gets
+  // discarded" (see EclipseFrameLowering.cpp / PerformDAGCombine's WORD_ADD
+  // for the two sibling wide-value-addressing bugs this was first suspected
+  // to be a third instance of — it isn't; nothing downstream of the branch
+  // is wrong, the branch condition itself is computed wrong). Vetoing only
+  // i8 here leaves every width Eclipse actually supports untouched: i16
+  // reductions, and reducing a >16-bit load down to one of its already
+  // word-aligned i16 halves (handled correctly elsewhere by the type
+  // legalizer's own multi-word splitting), both still fire normally.
+  bool shouldReduceLoadWidth(
+      SDNode *Load, ISD::LoadExtType ExtTy, EVT NewVT,
+      std::optional<unsigned> ByteOffset = std::nullopt) const override {
+    if (NewVT == MVT::i8)
+      return false;
+    return TargetLowering::shouldReduceLoadWidth(Load, ExtTy, NewVT,
+                                                  ByteOffset);
+  }
+
+  // Inline asm: the escape hatch for device I/O instructions (SKPDN,
+  // SKPBN, DIAC, DOAS, NIO/NIOS/NIOC/NIOP), which have no ordinary-C
+  // mapping — see README.md. Only the generic "r" (any GPR) constraint is
+  // supported; there's no target-specific constraint letter yet.
+  std::pair<unsigned, const TargetRegisterClass *>
+  getRegForInlineAsmConstraint(const TargetRegisterInfo *TRI,
+                                StringRef Constraint, MVT VT) const override;
+
+private:
+  // Needed by PerformDAGCombine to check hasEIS() (Extended Instruction
+  // Set — native IOR/XOR) — false for the base-Nova subtargets added
+  // alongside eclipse_s140, see Eclipse.td. A forward declaration is
+  // enough here since only a reference is stored; EclipseSubtarget.h is
+  // pulled in by the .cpp where its methods actually get called.
+  const EclipseSubtarget &Subtarget;
+
+  SDValue LowerGlobalAddress(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerExternalSymbol(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerConstantPool(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerBR_CC(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerBRCOND(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerSELECT_CC(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerSETCC(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerVASTART(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerVAARG(SDValue Op, SelectionDAG &DAG) const;
+  SDValue LowerSIGN_EXTEND_INREG(SDValue Op, SelectionDAG &DAG) const;
+
+  // No shift/rotate-with-writeback hardware is exposed yet (see
+  // EclipseInstrInfo.td's file header) — constant-count shifts are built
+  // from primitives already verified elsewhere in this backend: `x << 1`
+  // is `x + x`; `x >>u 1` is unsigned-divide-by-2; `x >>s 1` is
+  // signed-divide-by-2 with a floor-rounding correction (truncating
+  // division rounds toward zero, arithmetic shift must round toward
+  // -infinity — they differ for negative odd values). Variable-count
+  // shifts aren't supported yet (see README.md).
+  SDValue LowerShift(SDValue Op, SelectionDAG &DAG) const;
+
+  // The hardware DIV instruction's dividend is unsigned (see
+  // EclipseInstrInfo.td's MUL/DIV comment): a genuinely negative,
+  // sign-primed dividend was tried and is real backend-level complexity
+  // for no benefit, since it decomposes cleanly here instead — signed
+  // sdiv/srem reduce to unsigned division on abs(LHS)/abs(RHS) plus a
+  // conditional negate, built from SELECT_CC (already implemented and
+  // proven via comparisons/branches).
+  SDValue LowerSDIVREM(SDValue Op, SelectionDAG &DAG) const;
+
+  SDValue LowerFormalArguments(SDValue Chain, CallingConv::ID CallConv,
+                                bool IsVarArg,
+                                const SmallVectorImpl<ISD::InputArg> &Ins,
+                                const SDLoc &DL, SelectionDAG &DAG,
+                                SmallVectorImpl<SDValue> &InVals) const override;
+
+  SDValue LowerCall(TargetLowering::CallLoweringInfo &CLI,
+                     SmallVectorImpl<SDValue> &InVals) const override;
+
+  SDValue LowerReturn(SDValue Chain, CallingConv::ID CallConv, bool IsVarArg,
+                       const SmallVectorImpl<ISD::OutputArg> &Outs,
+                       const SmallVectorImpl<SDValue> &OutVals,
+                       const SDLoc &DL, SelectionDAG &DAG) const override;
+
+  MachineBasicBlock *emitSelectCC(MachineInstr &MI,
+                                   MachineBasicBlock *MBB) const;
+  // Same diamond-expansion shape as emitSelectCC, for the SelectU pseudo
+  // (unsigned SETCC/SELECT_CC) — kept separate because it branches on
+  // BGEU/BLTU (two live GPR operands) rather than a single pre-computed
+  // comparison value; see EclipseISD::BGEU's comment.
+  MachineBasicBlock *emitSelectCCU(MachineInstr &MI,
+                                    MachineBasicBlock *MBB) const;
+  MachineBasicBlock *emitPushPop(MachineInstr &MI, MachineBasicBlock *MBB,
+                                  bool IsPush) const;
+  MachineBasicBlock *emitAdjCallStack(MachineInstr &MI,
+                                       MachineBasicBlock *MBB,
+                                       bool IsDown) const;
+  MachineBasicBlock *emitIndirectMem(MachineInstr &MI, MachineBasicBlock *MBB,
+                                      bool IsLoad) const;
+  // Expands CALLSLOT_STORE -- see EclipseInstrInfo.td's own comment and
+  // LowerCall's indirect-call branch.
+  MachineBasicBlock *emitIndirectCallSlotStore(MachineInstr &MI,
+                                                MachineBasicBlock *MBB) const;
+};
+
+// Shared by EclipseISelLowering.cpp (LowerCall / the CALLSLOT_STORE
+// expansion) and EclipseAsmPrinter.cpp (deciding whether to declare it,
+// and what name the CALL case's slot-tracking should skip auto-declaring
+// like an ordinary "<callee>_SLOT") -- one definition, not two string
+// literals that could silently drift apart.
+extern const char *const EclipseIndirectCallSlotName;
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEISELLOWERING_H
diff --git a/llvm/lib/Target/Eclipse/EclipseInstrFormats.td b/llvm/lib/Target/Eclipse/EclipseInstrFormats.td
new file mode 100644
index 000000000..c7b0873e1
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseInstrFormats.td
@@ -0,0 +1,37 @@
+//===- EclipseInstrFormats.td - Eclipse Instruction Formats *- tablegen -*-=//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This backend never emits machine code directly (see README.md: like the
+// sibling `eccc` project, it treats Nova/Eclipse *assembly text* as the
+// ground truth and leaves bit-level encoding to a real assembler). So these
+// aren't real bit-layout Instruction encodings, just enough structure for
+// TableGen's InstrInfo/AsmWriter/DAGISel generators; `Size` and the (unused)
+// `Inst`/`outs` bit patterns below are nominal.
+//
+//===----------------------------------------------------------------------===//
+
+class EclipseInst<dag outs, dag ins, string asmstr, list<dag> pattern>
+    : Instruction {
+  let Namespace = "Eclipse";
+  dag OutOperandList = outs;
+  dag InOperandList = ins;
+  let AsmString = asmstr;
+  let Pattern = pattern;
+  field bits<16> Inst;
+  field bits<16> SoftFail = 0;
+  let Size = 2; // nominal word count; real emission is textual, see above
+}
+
+// A pseudo-instruction: exists only in MachineInstr form, either expanded by
+// a custom inserter (EmitInstrWithCustomInserter) into real EclipseInsts, or
+// printed directly by EclipseAsmPrinter as more than one line of real text
+// (e.g. the skip-then-jump conditional branches).
+class EclipsePseudo<dag outs, dag ins, string asmstr, list<dag> pattern>
+    : EclipseInst<outs, ins, asmstr, pattern> {
+  let isPseudo = 1;
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseInstrInfo.cpp b/llvm/lib/Target/Eclipse/EclipseInstrInfo.cpp
new file mode 100644
index 000000000..a140e17a2
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseInstrInfo.cpp
@@ -0,0 +1,309 @@
+//===-- EclipseInstrInfo.cpp - Eclipse Instruction Information ----------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseInstrInfo.h"
+#include "EclipseSubtarget.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+
+#define GET_INSTRINFO_CTOR_DTOR
+#include "EclipseGenInstrInfo.inc"
+
+using namespace llvm;
+
+EclipseInstrInfo::EclipseInstrInfo(const EclipseSubtarget &STI)
+    : EclipseGenInstrInfo(STI, RegisterInfo, Eclipse::ADJCALLSTACKDOWN,
+                           Eclipse::ADJCALLSTACKUP),
+      RegisterInfo() {}
+
+void EclipseInstrInfo::copyPhysReg(MachineBasicBlock &MBB,
+                                    MachineBasicBlock::iterator I,
+                                    const DebugLoc &DL, Register DestReg,
+                                    Register SrcReg, bool KillSrc,
+                                    bool /*RenamableDest*/,
+                                    bool /*RenamableSrc*/) const {
+  BuildMI(MBB, I, DL, get(Eclipse::MOVrr), DestReg)
+      .addReg(SrcReg, getKillRegState(KillSrc));
+}
+
+void EclipseInstrInfo::storeRegToStackSlot(
+    MachineBasicBlock &MBB, MachineBasicBlock::iterator I, Register SrcReg,
+    bool IsKill, int FrameIndex, const TargetRegisterClass * /*RC*/,
+    Register /*VReg*/, MachineInstr::MIFlag Flags) const {
+  DebugLoc DL = I != MBB.end() ? I->getDebugLoc() : DebugLoc();
+  BuildMI(MBB, I, DL, get(Eclipse::STFI))
+      .addReg(SrcReg, getKillRegState(IsKill))
+      .addFrameIndex(FrameIndex)
+      .setMIFlags(Flags);
+}
+
+void EclipseInstrInfo::loadRegFromStackSlot(
+    MachineBasicBlock &MBB, MachineBasicBlock::iterator I, Register DestReg,
+    int FrameIndex, const TargetRegisterClass * /*RC*/, Register /*VReg*/,
+    unsigned /*SubReg*/, MachineInstr::MIFlag Flags) const {
+  DebugLoc DL = I != MBB.end() ? I->getDebugLoc() : DebugLoc();
+  BuildMI(MBB, I, DL, get(Eclipse::LDFI), DestReg)
+      .addFrameIndex(FrameIndex)
+      .setMIFlags(Flags);
+}
+
+static bool isCondBranchOpcode(unsigned Opc) {
+  switch (Opc) {
+  case Eclipse::BRZ:
+  case Eclipse::BRNZ:
+  case Eclipse::BGEZ:
+  case Eclipse::BLTZ:
+    return true;
+  default:
+    return false;
+  }
+}
+
+bool EclipseInstrInfo::analyzeBranch(MachineBasicBlock &MBB,
+                                      MachineBasicBlock *&TBB,
+                                      MachineBasicBlock *&FBB,
+                                      SmallVectorImpl<MachineOperand> &Cond,
+                                      bool /*AllowModify*/) const {
+  MachineBasicBlock::iterator I = MBB.end();
+  if (I == MBB.begin())
+    return false;
+  --I;
+  while (I->isDebugInstr()) {
+    if (I == MBB.begin())
+      return false;
+    --I;
+  }
+  if (!I->isTerminator())
+    return false;
+
+  MachineInstr *LastInst = &*I;
+  if (I == MBB.begin() || !std::prev(I)->isTerminator()) {
+    if (LastInst->getOpcode() == Eclipse::JMP) {
+      TBB = LastInst->getOperand(0).getMBB();
+      return false;
+    }
+    if (isCondBranchOpcode(LastInst->getOpcode())) {
+      TBB = LastInst->getOperand(1).getMBB();
+      Cond.push_back(MachineOperand::CreateImm(LastInst->getOpcode()));
+      Cond.push_back(LastInst->getOperand(0));
+      return false;
+    }
+    return true; // Unanalyzable (e.g. RET).
+  }
+
+  MachineInstr *SecondLastInst = &*std::prev(I);
+  if (isCondBranchOpcode(SecondLastInst->getOpcode()) &&
+      LastInst->getOpcode() == Eclipse::JMP) {
+    TBB = SecondLastInst->getOperand(1).getMBB();
+    Cond.push_back(MachineOperand::CreateImm(SecondLastInst->getOpcode()));
+    Cond.push_back(SecondLastInst->getOperand(0));
+    FBB = LastInst->getOperand(0).getMBB();
+    return false;
+  }
+
+  return true; // Unanalyzable pair.
+}
+
+unsigned EclipseInstrInfo::removeBranch(MachineBasicBlock &MBB,
+                                         int *BytesRemoved) const {
+  assert(!BytesRemoved && "code size not tracked");
+  MachineBasicBlock::iterator I = MBB.end();
+  unsigned Count = 0;
+  while (I != MBB.begin()) {
+    --I;
+    if (I->isDebugInstr())
+      continue;
+    if (I->getOpcode() != Eclipse::JMP && !isCondBranchOpcode(I->getOpcode()))
+      break;
+    I->eraseFromParent();
+    I = MBB.end();
+    ++Count;
+  }
+  return Count;
+}
+
+unsigned EclipseInstrInfo::insertBranch(MachineBasicBlock &MBB,
+                                         MachineBasicBlock *TBB,
+                                         MachineBasicBlock *FBB,
+                                         ArrayRef<MachineOperand> Cond,
+                                         const DebugLoc &DL,
+                                         int *BytesAdded) const {
+  assert(!BytesAdded && "code size not tracked");
+  assert(Cond.size() == 2 || Cond.empty());
+
+  if (Cond.empty()) {
+    assert(!FBB && "unconditional branch with multiple successors");
+    BuildMI(&MBB, DL, get(Eclipse::JMP)).addMBB(TBB);
+    return 1;
+  }
+
+  unsigned CondOpc = Cond[0].getImm();
+  BuildMI(&MBB, DL, get(CondOpc)).add(Cond[1]).addMBB(TBB);
+  if (!FBB)
+    return 1;
+  BuildMI(&MBB, DL, get(Eclipse::JMP)).addMBB(FBB);
+  return 2;
+}
+
+bool EclipseInstrInfo::reverseBranchCondition(
+    SmallVectorImpl<MachineOperand> &Cond) const {
+  assert(Cond.size() == 2);
+  switch (Cond[0].getImm()) {
+  case Eclipse::BRZ:
+    Cond[0].setImm(Eclipse::BRNZ);
+    return false;
+  case Eclipse::BRNZ:
+    Cond[0].setImm(Eclipse::BRZ);
+    return false;
+  case Eclipse::BGEZ:
+    Cond[0].setImm(Eclipse::BLTZ);
+    return false;
+  case Eclipse::BLTZ:
+    Cond[0].setImm(Eclipse::BGEZ);
+    return false;
+  default:
+    llvm_unreachable("unknown Eclipse conditional branch opcode");
+  }
+}
+
+// MUL/DIV are zero-operand hardware instructions operating implicitly on
+// AC0:AC1:AC2 (see docs/ECLIPSE_ISA_NOTES.md — "MUL is a
+// multiply-*accumulate*" — in the sibling eccc project, and
+// EclipseInstrInfo.td's MUL/DIV comment). AC2 is our frame pointer, so it
+// has to be saved and restored around this — via the software stack
+// (`_SP`), exactly like eccc's func.rs, *not* a spare register, since
+// that's what stays valid (absolute/indirect addressing, not
+// frame-relative) throughout the window where AC2 doesn't mean "FP".
+//
+// This expansion deliberately runs post-RA (see the .td comment): MI's
+// operands are already physical registers by this point, and — the
+// entire reason this is a post-RA expansion rather than
+// EmitInstrWithCustomInserter — nothing else can be scheduled or
+// spilled into the middle of it anymore.
+static const char *SPSym = "_SP";
+
+bool EclipseInstrInfo::expandPostRAPseudo(MachineInstr &MI) const {
+  unsigned Opc = MI.getOpcode();
+
+  if (Opc == Eclipse::RETI) {
+    // Interrupt return: NIOS 077 (re-enable interrupts) immediately
+    // followed by JMP @0 (resume via the PC hardware saved at memory
+    // location 0). Expanded here, post-RA, so nothing can be scheduled or
+    // spilled between the two — see this pseudo's .td comment.
+    MachineBasicBlock &MBB = *MI.getParent();
+    DebugLoc DL = MI.getDebugLoc();
+    BuildMI(MBB, MI, DL, get(Eclipse::INTEN));
+    BuildMI(MBB, MI, DL, get(Eclipse::JMPABSI)).addImm(0);
+    MI.eraseFromParent();
+    return true;
+  }
+
+  if (Opc != Eclipse::MULrr && Opc != Eclipse::UDIVrr &&
+      Opc != Eclipse::UREMrr)
+    return false;
+
+  MachineBasicBlock &MBB = *MI.getParent();
+  DebugLoc DL = MI.getDebugLoc();
+  Register Dst = MI.getOperand(0).getReg();
+  Register LHS = MI.getOperand(1).getReg();
+  Register RHS = MI.getOperand(2).getReg();
+
+  BuildMI(MBB, MI, DL, get(Eclipse::DSZABS)).addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::STABSI))
+      .addReg(Eclipse::AC2)
+      .addExternalSymbol(SPSym);
+
+  // Also unconditionally save AC0 and AC1, exactly like AC2 just above —
+  // NOT redundant with $dst/$lhs/$rhs already being GPR operands. The
+  // register allocator sizes this pseudo's demand from its *declared*
+  // operands/Defs (see the .td's own long comment on why AC0/AC1 aren't
+  // *also* listed there), reasoning that $lhs/$rhs (both drawn from the
+  // only two allocatable registers) already account for the whole GPR
+  // file. That reasoning silently breaks whenever $lhs and $rhs get
+  // allocated to the *same* physical register (e.g. two occurrences of
+  // the identical constant "2" -- HALVE(Offset, Two) where a struct
+  // field's byte offset happens to equal the halving divisor -- CSE'd
+  // down to one node, one register): from the allocator's point of view
+  // only ONE register is touched by this instruction's operands, so it's
+  // free to keep some other, unrelated value alive in the *other* GPR
+  // across it — but the real hardware sequence below unconditionally
+  // zeros AC0 and loads AC1 regardless of which register(s) $lhs/$rhs
+  // actually occupy, silently destroying that other live value. Confirmed
+  // empirically (ct00018: a loaded struct-pointer value in AC0, live
+  // across a HALVE(2, 2) computing a field's word offset in AC1, was
+  // gone by the time the pointer and offset needed combining). Save/
+  // restore here — entirely within this one already-post-RA expansion,
+  // touching neither the .td's Defs list nor the allocator's own
+  // decisions — sidesteps this without repeating the "declaring AC0/AC1
+  // as Defs double-counts register demand and allocation fails outright"
+  // failure the .td comment already documents from a previous attempt at
+  // the more obvious-looking fix.
+  BuildMI(MBB, MI, DL, get(Eclipse::DSZABS)).addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::STABSI))
+      .addReg(Eclipse::AC0)
+      .addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::DSZABS)).addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::STABSI))
+      .addReg(Eclipse::AC1)
+      .addExternalSymbol(SPSym);
+
+  // NB: must be real MOVrr, not TargetOpcode::COPY — these are inserted
+  // *before* the loop's current position in ExpandPostRAPseudos, so a
+  // generic COPY placed here would never get revisited for lowering and
+  // would reach the asm printer as a bare, encoding-less pseudo.
+  //
+  // NB: RHS must be copied to AC2 *before* LHS is copied to AC1. Only
+  // AC0/AC1 are allocatable GPRs, so {LHS, RHS} both live in {AC0, AC1} —
+  // copying LHS into AC1 first can clobber RHS's source register before
+  // it's been read, if RHS happened to be allocated to AC1. AC2 is never
+  // LHS or RHS's register (it's the reserved frame pointer, not
+  // GPR-allocatable), so writing it first is always safe.
+  BuildMI(MBB, MI, DL, get(Eclipse::MOVrr), Eclipse::AC2).addReg(RHS);
+  BuildMI(MBB, MI, DL, get(Eclipse::MOVrr), Eclipse::AC1).addReg(LHS);
+  // Zero AC0 unconditionally (self-subtract: correct regardless of AC0's
+  // prior value) — primes MUL's accumulator and DIV's dividend high word.
+  BuildMI(MBB, MI, DL, get(Eclipse::SUBrr), Eclipse::AC0)
+      .addReg(Eclipse::AC0)
+      .addReg(Eclipse::AC0);
+
+  if (Opc == Eclipse::MULrr) {
+    BuildMI(MBB, MI, DL, get(Eclipse::MUL));
+    BuildMI(MBB, MI, DL, get(Eclipse::MOVrr), Dst).addReg(Eclipse::AC1);
+  } else {
+    BuildMI(MBB, MI, DL, get(Eclipse::DIV));
+    // DIV: AC0 := remainder, AC1 := quotient (docs/ECLIPSE_ISA_NOTES.md).
+    Register Result = (Opc == Eclipse::UDIVrr) ? Eclipse::AC1 : Eclipse::AC0;
+    BuildMI(MBB, MI, DL, get(Eclipse::MOVrr), Dst).addReg(Result);
+  }
+
+  // Restore AC1 then AC0 (reverse of the push order above) -- except
+  // whichever of the two *is* Dst: Dst was just given the real answer by
+  // the MOVrr right above, and popping the pre-instruction value back
+  // into it here would immediately overwrite that answer with garbage,
+  // so skip the LDABSI itself (not just its effect) when Dst is that
+  // register. Still advance `_SP` past the unused saved slot either way
+  // (ISZABS alone) so the stack stays balanced regardless of which
+  // register Dst turned out to be.
+  if (Dst != Eclipse::AC1)
+    BuildMI(MBB, MI, DL, get(Eclipse::LDABSI), Eclipse::AC1)
+        .addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::ISZABS)).addExternalSymbol(SPSym);
+
+  if (Dst != Eclipse::AC0)
+    BuildMI(MBB, MI, DL, get(Eclipse::LDABSI), Eclipse::AC0)
+        .addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::ISZABS)).addExternalSymbol(SPSym);
+
+  BuildMI(MBB, MI, DL, get(Eclipse::LDABSI), Eclipse::AC2)
+      .addExternalSymbol(SPSym);
+  BuildMI(MBB, MI, DL, get(Eclipse::ISZABS)).addExternalSymbol(SPSym);
+
+  MI.eraseFromParent();
+  return true;
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseInstrInfo.h b/llvm/lib/Target/Eclipse/EclipseInstrInfo.h
new file mode 100644
index 000000000..4caee1b13
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseInstrInfo.h
@@ -0,0 +1,72 @@
+//===-- EclipseInstrInfo.h - Eclipse Instruction Information ---*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEINSTRINFO_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEINSTRINFO_H
+
+#include "EclipseRegisterInfo.h"
+#include "MCTargetDesc/EclipseMCTargetDesc.h"
+#include "llvm/CodeGen/TargetInstrInfo.h"
+
+#define GET_INSTRINFO_HEADER
+#include "EclipseGenInstrInfo.inc"
+
+namespace llvm {
+
+class EclipseSubtarget;
+
+class EclipseInstrInfo : public EclipseGenInstrInfo {
+  const EclipseRegisterInfo RegisterInfo;
+
+public:
+  explicit EclipseInstrInfo(const EclipseSubtarget &STI);
+
+  const EclipseRegisterInfo &getRegisterInfo() const { return RegisterInfo; }
+
+  void copyPhysReg(MachineBasicBlock &MBB, MachineBasicBlock::iterator I,
+                    const DebugLoc &DL, Register DestReg, Register SrcReg,
+                    bool KillSrc, bool RenamableDest = false,
+                    bool RenamableSrc = false) const override;
+
+  void storeRegToStackSlot(
+      MachineBasicBlock &MBB, MachineBasicBlock::iterator I, Register SrcReg,
+      bool IsKill, int FrameIndex, const TargetRegisterClass *RC,
+      Register VReg,
+      MachineInstr::MIFlag Flags = MachineInstr::NoFlags) const override;
+
+  void loadRegFromStackSlot(
+      MachineBasicBlock &MBB, MachineBasicBlock::iterator I, Register DestReg,
+      int FrameIndex, const TargetRegisterClass *RC, Register VReg,
+      unsigned SubReg = 0,
+      MachineInstr::MIFlag Flags = MachineInstr::NoFlags) const override;
+
+  bool analyzeBranch(MachineBasicBlock &MBB, MachineBasicBlock *&TBB,
+                      MachineBasicBlock *&FBB,
+                      SmallVectorImpl<MachineOperand> &Cond,
+                      bool AllowModify) const override;
+
+  unsigned removeBranch(MachineBasicBlock &MBB,
+                         int *BytesRemoved = nullptr) const override;
+
+  unsigned insertBranch(MachineBasicBlock &MBB, MachineBasicBlock *TBB,
+                         MachineBasicBlock *FBB,
+                         ArrayRef<MachineOperand> Cond, const DebugLoc &DL,
+                         int *BytesAdded = nullptr) const override;
+
+  bool reverseBranchCondition(
+      SmallVectorImpl<MachineOperand> &Cond) const override;
+
+  // Expands MULrr/UDIVrr/UREMrr — see EclipseInstrInfo.td's comment on
+  // why this has to happen post-RA rather than via
+  // EmitInstrWithCustomInserter.
+  bool expandPostRAPseudo(MachineInstr &MI) const override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEINSTRINFO_H
diff --git a/llvm/lib/Target/Eclipse/EclipseInstrInfo.td b/llvm/lib/Target/Eclipse/EclipseInstrInfo.td
new file mode 100644
index 000000000..4068cd2a5
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseInstrInfo.td
@@ -0,0 +1,483 @@
+//===- EclipseInstrInfo.td - Eclipse Instruction defs ------*- tablegen -*-=//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// Deliberately small instruction set: only what's needed to lower i16
+// scalar/pointer C-like code, matching exactly what the sibling `eccc`
+// project's hand-written codegen already proved correct against a real
+// `eclipseemu` simulator (see docs/ECLIPSE_ISA_NOTES.md and
+// docs/CALLING_CONVENTION.md there). i32 and float arithmetic are lowered
+// to runtime library calls (see EclipseISelLowering.cpp) rather than
+// inline multi-word instruction sequences, specifically because this
+// backend won't fabricate unverified carry/FPU opcode encodings the way
+// eccc refused to (see README.md).
+//
+//===----------------------------------------------------------------------===//
+
+include "EclipseInstrFormats.td"
+
+//===----------------------------------------------------------------------===//
+// Target-specific SDNodes
+//===----------------------------------------------------------------------===//
+
+def SDT_EclipseWrapper : SDTypeProfile<1, 1, [SDTCisSameAs<0, 1>,
+                                               SDTCisPtrTy<0>]>;
+def EclipseWrapper : SDNode<"EclipseISD::WRAPPER", SDT_EclipseWrapper>;
+
+// Emitted only by EclipseTargetLowering::PerformDAGCombine, in place of a
+// plain `add`, once it has already halved a runtime GEP byte-offset to
+// match a WRAPPER'd (LEAGA-materialized) base pointer's word granularity
+// — see that function's comment. Semantically just a register add, hence
+// the identical Pat below reusing ADDrr; the distinct SDNode only exists
+// so PerformDAGCombine's own `ISD::ADD`-keyed check can never match its
+// own output again.
+def SDT_EclipseWordAdd : SDTypeProfile<1, 2, [SDTCisSameAs<0, 1>,
+                                               SDTCisSameAs<0, 2>,
+                                               SDTCisVT<0, i16>]>;
+def EclipseWordAdd : SDNode<"EclipseISD::WORD_ADD", SDT_EclipseWordAdd>;
+
+// Also emitted only by PerformDAGCombine, to unsigned-divide-by-2 the
+// runtime offset WORD_ADD above consumes. Deliberately *not* expressed as
+// plain `srl`/`udiv` — see PerformDAGCombine's comment for the infinite
+// DAGCombine loop that produced empirically (LowerShift's Custom SRL-by-1
+// lowering and DAGCombiner's own udiv-by-power-of-2-to-shift fold
+// re-triggering each other forever). Selects straight to the existing,
+// already-proven UDIVrr pseudo.
+def SDT_EclipseHalve : SDTypeProfile<1, 2, [SDTCisSameAs<0, 1>,
+                                             SDTCisSameAs<0, 2>,
+                                             SDTCisVT<0, i16>]>;
+def EclipseHalve : SDNode<"EclipseISD::HALVE", SDT_EclipseHalve>;
+
+// Emitted only by PerformDAGCombine's OR/XOR synthesis for base-Nova
+// subtargets (no FeatureEIS — see Eclipse.td), standing in for `xor(x,
+// -1)` (bitwise NOT) without actually being one — see
+// EclipseISelLowering.h's EclipseISD::COM comment for the infinite
+// DAGCombine loop a real ISD::XOR produced here. The Pat immediately
+// below COMrr's own definition maps this to the identical instruction.
+def SDT_EclipseCom : SDTypeProfile<1, 1, [SDTCisSameAs<0, 1>,
+                                           SDTCisVT<0, i16>]>;
+def EclipseCom : SDNode<"EclipseISD::COM", SDT_EclipseCom>;
+
+def SDT_EclipseCall : SDTypeProfile<0, -1, [SDTCisVT<0, i16>]>;
+def EclipseCall : SDNode<"EclipseISD::CALL", SDT_EclipseCall,
+                          [SDNPHasChain, SDNPOutGlue, SDNPOptInGlue,
+                           SDNPVariadic]>;
+
+def EclipseRet : SDNode<"EclipseISD::RET_FLAG", SDTNone,
+                         [SDNPHasChain, SDNPOptInGlue, SDNPVariadic]>;
+
+// Return from an `__attribute__((interrupt))` handler — always void, so
+// unlike EclipseRet there's no variadic return-value operand to carry.
+def EclipseRetI : SDNode<"EclipseISD::RETI_FLAG", SDTNone, [SDNPHasChain]>;
+
+def SDT_EclipseCallSeqStart : SDCallSeqStart<[SDTCisVT<0, i16>,
+                                               SDTCisVT<1, i16>]>;
+def SDT_EclipseCallSeqEnd : SDCallSeqEnd<[SDTCisVT<0, i16>, SDTCisVT<1, i16>]>;
+def EclipseCallSeqStart : SDNode<"ISD::CALLSEQ_START", SDT_EclipseCallSeqStart,
+                                  [SDNPHasChain, SDNPOutGlue]>;
+def EclipseCallSeqEnd : SDNode<"ISD::CALLSEQ_END", SDT_EclipseCallSeqEnd,
+                                [SDNPHasChain, SDNPOptInGlue, SDNPOutGlue]>;
+
+def SDT_EclipseBrcond : SDTypeProfile<0, 2, [SDTCisVT<0, i16>,
+                                              SDTCisVT<1, OtherVT>]>;
+def EclipseBrz  : SDNode<"EclipseISD::BRZ",  SDT_EclipseBrcond, [SDNPHasChain]>;
+def EclipseBrnz : SDNode<"EclipseISD::BRNZ", SDT_EclipseBrcond, [SDNPHasChain]>;
+def EclipseBgez : SDNode<"EclipseISD::BGEZ", SDT_EclipseBrcond, [SDNPHasChain]>;
+def EclipseBltz : SDNode<"EclipseISD::BLTZ", SDT_EclipseBrcond, [SDNPHasChain]>;
+
+// Unsigned branch variants: unlike BRZ/BRNZ/BGEZ/BLTZ, these take *two*
+// live GPR operands (not one pre-computed comparison value) because the
+// carry they test has to come directly off "SUB# rhs,lhs" at the moment
+// of subtraction — see EclipseISD::BGEU's comment in EclipseISelLowering.h.
+def SDT_EclipseBrcondU : SDTypeProfile<0, 3, [SDTCisVT<0, i16>,
+                                               SDTCisVT<1, i16>,
+                                               SDTCisVT<2, OtherVT>]>;
+def EclipseBgeu : SDNode<"EclipseISD::BGEU", SDT_EclipseBrcondU, [SDNPHasChain]>;
+def EclipseBltu : SDNode<"EclipseISD::BLTU", SDT_EclipseBrcondU, [SDNPHasChain]>;
+
+//===----------------------------------------------------------------------===//
+// Operands
+//===----------------------------------------------------------------------===//
+
+def brtarget : Operand<OtherVT>;
+def calltarget : Operand<i16>;
+
+// `i16imm` (a plain 16-bit immediate operand) is already defined by
+// target-independent Target.td — reused as-is here. There are no
+// immediate-operand instructions on the base Nova/Eclipse ISA at all (the
+// reason eccc built a deduplicated constant pool instead) — every
+// "constant" this backend needs is really the *address* of a page-zero
+// data word, but the operand class itself is just a plain 16-bit value.
+
+//===----------------------------------------------------------------------===//
+// Instruction Pattern Stuff
+//===----------------------------------------------------------------------===//
+
+def imm16 : ImmLeaf<i16, [{ return true; }]>;
+
+//===----------------------------------------------------------------------===//
+// Data movement / ALC (accumulator-accumulator) instructions
+//
+// Nova/Eclipse ALC instructions are printed "OP src,dst" and (except MOV,
+// NEG, COM, which just overwrite dst) accumulate into dst: dst := dst OP
+// src. Confirmed empirically in docs/CALLING_CONVENTION.md ("ADD 0,1 means
+// AC1 += AC0").
+//===----------------------------------------------------------------------===//
+
+let isAsCheapAsAMove = 1 in
+def MOVrr : EclipseInst<(outs GPR:$dst), (ins GPR:$src), "MOV $src,$dst", []>;
+
+class ALCrr<string mnem, SDNode op>
+    : EclipseInst<(outs GPR:$dst), (ins GPR:$src, GPR:$dstin), mnem#" $src,$dst",
+                  [(set GPR:$dst, (op GPR:$dstin, GPR:$src))]> {
+  let Constraints = "$dst = $dstin";
+}
+
+def ADDrr : ALCrr<"ADD", add>;
+def : Pat<(EclipseWordAdd GPR:$dstin, GPR:$src), (ADDrr GPR:$src, GPR:$dstin)>;
+def SUBrr : ALCrr<"SUB", sub>;
+def ANDrr : ALCrr<"AND", and>;
+def IORrr : ALCrr<"IOR", or>;
+def XORrr : ALCrr<"XOR", xor>;
+
+def NEGrr : EclipseInst<(outs GPR:$dst), (ins GPR:$src), "NEG $src,$dst",
+                         [(set GPR:$dst, (ineg GPR:$src))]>;
+def COMrr : EclipseInst<(outs GPR:$dst), (ins GPR:$src), "COM $src,$dst",
+                         [(set GPR:$dst, (not GPR:$src))]>;
+def : Pat<(EclipseCom GPR:$src), (COMrr GPR:$src)>;
+
+//===----------------------------------------------------------------------===//
+// Memory: frame-relative (AC2/FP-relative) and absolute (page-zero)
+//===----------------------------------------------------------------------===//
+
+// No declarative Pattern here: a bare `frameindex` operand needs custom
+// selection (EclipseISelDAGToDAG.cpp's Select()), not table-driven
+// pattern matching — matching real-target practice (neither MSP430 nor
+// AVR bind `frameindex` directly in a .td Pattern either).
+let mayLoad = 1 in
+def LDFI : EclipseInst<(outs GPR:$dst), (ins i16imm:$off), "LDA $dst,$off,2",
+                        []>;
+let mayStore = 1 in
+def STFI : EclipseInst<(outs), (ins GPR:$src, i16imm:$off), "STA $src,$off,2",
+                        []>;
+
+// Materializes the *address* FP+off as a value (not a load) — needed for
+// `&local`, arrays, and any pointer arithmetic rooted at a local/param.
+// There's no load-effective-address instruction on this ISA (no immediate
+// operands at all — see the file header), so this is a pseudo, expanded in
+// EclipseRegisterInfo::eliminateFrameIndex (the one place the frame index's
+// resolved word offset is actually known) into: copy FP, materialize the
+// offset via a fresh constant-pool entry, add it in.
+def LEAFI : EclipsePseudo<(outs GPR:$dst), (ins i16imm:$fi), "# LEAFI", []>;
+
+// LDABS/STABS have NO Pattern for a *global* (tglobaladdr) load/store,
+// deliberately: see the "page-zero indirect data addressing" comment
+// below LEAGA's definition for why a global's actual content is never
+// addressed directly (only tconstpool — genuinely single-word values —
+// still uses these two directly, just below). A bare LDABS/STABS
+// definition still exists because LEAFI's frame-index materialization
+// and other internal uses reference the opcodes directly.
+def LDABS : EclipseInst<(outs GPR:$dst), (ins i16imm:$addr), "LDA $dst,$addr", []>;
+def STABS : EclipseInst<(outs), (ins GPR:$src, i16imm:$addr), "STA $src,$addr", []>;
+
+// A constant-pool wrapper used *as a value* (not explicitly `load`-wrapped)
+// is exactly what materializing an integer constant looks like — a
+// constant-pool entry only ever exists to be loaded as a value in the
+// first place (see the ISD::Constant handling in EclipseISelLowering.cpp),
+// unlike a global's address, so this is the same LDABS instruction.
+def : Pat<(i16 (EclipseWrapper tconstpool:$cp)), (LDABS tconstpool:$cp)>;
+
+// Page-zero indirect data addressing. `LDA/STA ac,addr` only has an
+// 8-bit displacement (0-255), and this backend originally put every
+// global's/string's *actual content* there directly — meaning a single
+// 256-word budget was shared by the entire program's data, not just its
+// pointers. A program pulling in a modest combination of runtime-library
+// functions (confirmed empirically: printf + string.h + ctype.h +
+// stdlib.h together) could exceed that outright. Since indirect
+// addressing ("LDA ac,@addr") has no such range limit — the *pointer*
+// lives in page-zero, but what it points to can be anywhere — globals
+// are now reached that way universally: a global's wrapper used *as a
+// value* (bare, not `load`-wrapped, e.g. a string literal or array
+// decaying to a pointer) materializes its address via LEAGA, exactly as
+// before; but now the *load-wrapped* case (LDABS's old direct-content
+// Pattern, removed above) is deliberately left unhandled here too, so
+// SelectionDAG's ordinary bottom-up matching falls back to composing two
+// independently-proven pieces: LEAGA (this address materialization) feeds
+// its result register into LDIND/STIND's *existing*, unrelated
+// register-pointer-dereference pattern (used for `*p`-style C pointer
+// dereference via the shared `_scratch` word) — no new Pat needed for
+// that composition to work. Every global access now costs one extra
+// instruction (materialize the address) plus one extra memory read
+// (through `_scratch`) versus the old direct scheme, but the page-zero
+// cost of a global drops from "one word per word of content" to exactly
+// one pointer word, regardless of size — reorder_asm.py places the
+// pointer slot in page-zero and the actual content wherever it
+// naturally falls (no longer reordered to the front).
+//
+// tconstpool is deliberately NOT changed to this scheme (see the Pat
+// just above): a constant-pool entry is already exactly one word, so
+// indirecting it would only add overhead with no page-zero savings.
+def LEAGA : EclipseInst<(outs GPR:$dst), (ins i16imm:$addr), "# LEAGA", []>;
+def : Pat<(i16 (EclipseWrapper tglobaladdr:$addr)), (LEAGA tglobaladdr:$addr)>;
+
+// Page-zero *indirect* load/store: "LDA/STA ac,@addr" — one extra level of
+// indirection through whatever address is stored at the (page-zero) word
+// `addr`. Used for the software-stack push/pop (indirect through `_SP`)
+// and, with a shared fixed scratch word, for general pointer dereference —
+// see docs/ECLIPSE_ISA_NOTES.md's verified push/pop sequence and
+// DESIGN.md's "pointer-dereference scratch word".
+def LDABSI : EclipseInst<(outs GPR:$dst), (ins i16imm:$addr), "LDA $dst,@$addr", []>;
+def STABSI : EclipseInst<(outs), (ins GPR:$src, i16imm:$addr), "STA $src,@$addr", []>;
+
+// Jump indirect through a fixed absolute address — same "@" indirect
+// addressing LDABSI/STABSI use, just for control flow. Used with $addr=0
+// for the interrupt-return sequence below: hardware saves the interrupted
+// PC into memory location 0 on interrupt entry, so "return" means jumping
+// indirect through that fixed cell (see docs/ECLIPSE_ISA_NOTES.md and the
+// Eclipse S/140 Programmer's Reference's Program Flow Interruption
+// section — empirically verified end-to-end in hand-written assembly
+// before this pseudo existed).
+let isBranch = 1, isTerminator = 1, isBarrier = 1, isIndirectBranch = 1 in
+def JMPABSI : EclipseInst<(outs), (ins i16imm:$addr), "JMP @$addr", []>;
+
+// ISZ/DSZ are flow-class in dgasm's real grammar and so, unlike LDA/STA,
+// do *not* default to page-zero addressing when no index is given — they
+// need the index made explicit (confirmed against a real `dgasm` binary,
+// see eccc's crates/eccc-codegen/src/symbolic.rs module doc).
+def ISZABS : EclipseInst<(outs), (ins i16imm:$addr), "ISZ $addr,0", []>;
+def DSZABS : EclipseInst<(outs), (ins i16imm:$addr), "DSZ $addr,0", []>;
+
+// General pointer dereference, through the shared page-zero scratch word
+// (see README.md / docs/ECLIPSE_ISA_NOTES.md "dereference scratch word"):
+// expands (EclipseISelLowering.cpp's custom inserter) to STABS the address
+// into SCRATCH, then LDABSI/STABSI indirect through it.
+let usesCustomInserter = 1 in {
+def LDIND : EclipsePseudo<(outs GPR:$dst), (ins GPR:$addr), "# LDIND",
+                           [(set GPR:$dst, (load GPR:$addr))]>;
+def STIND : EclipsePseudo<(outs), (ins GPR:$src, GPR:$addr), "# STIND",
+                           [(store GPR:$src, GPR:$addr)]>;
+}
+def : Pat<(i16 (extloadi8  GPR:$addr)), (LDIND GPR:$addr)>;
+def : Pat<(i16 (sextloadi8 GPR:$addr)), (LDIND GPR:$addr)>;
+def : Pat<(i16 (zextloadi8 GPR:$addr)), (LDIND GPR:$addr)>;
+def : Pat<(truncstorei8 GPR:$src, GPR:$addr), (STIND GPR:$src, GPR:$addr)>;
+
+//===----------------------------------------------------------------------===//
+// Software stack (a plain page-zero memory word, `_SP`, manipulated with
+// ISZ/DSZ + single-level indirect addressing — see ECLIPSE_ISA_NOTES.md;
+// the hardware PSH/POP/SAVE/RTN stack instructions were tried by eccc and
+// rejected as needing an OS-managed environment this bare-metal target
+// doesn't have)
+//===----------------------------------------------------------------------===//
+
+let usesCustomInserter = 1 in {
+def PUSH : EclipsePseudo<(outs), (ins GPR:$src), "# PUSH", []>;
+def POP  : EclipsePseudo<(outs GPR:$dst), (ins), "# POP", []>;
+}
+
+let usesCustomInserter = 1 in {
+def ADJCALLSTACKDOWN : EclipsePseudo<(outs), (ins i16imm:$amt1, i16imm:$amt2),
+                                      "# ADJCALLSTACKDOWN $amt1",
+                                      [(EclipseCallSeqStart timm:$amt1, timm:$amt2)]>;
+def ADJCALLSTACKUP   : EclipsePseudo<(outs), (ins i16imm:$amt1, i16imm:$amt2),
+                                      "# ADJCALLSTACKUP $amt1",
+                                      [(EclipseCallSeqEnd timm:$amt1, timm:$amt2)]>;
+}
+
+//===----------------------------------------------------------------------===//
+// Control flow
+//===----------------------------------------------------------------------===//
+
+let isBranch = 1, isTerminator = 1, isBarrier = 1 in
+def JMP : EclipseInst<(outs), (ins brtarget:$dst), "JMP $dst", [(br bb:$dst)]>;
+
+// Skip-then-jump conditional branches: printed as two lines by
+// EclipseAsmPrinter (see docs/CALLING_CONVENTION.md's verified idiom):
+//   MOV# $val,$val,SZR / SNR      (zero test, no write-back)
+//   MOVL# $val,$val,SZC / SNC     (sign test via rotate-through-carry, no write-back)
+//   JMP $dst
+// Falls through to the next layout block otherwise (standard conditional
+// branch, unlike eccc's own always-both-arms form — eccc doesn't do block
+// layout, this backend does, so the tighter skip+single-jump form is both
+// valid on real hardware and more efficient).
+let isBranch = 1, isTerminator = 1 in {
+def BRZ  : EclipseInst<(outs), (ins GPR:$val, brtarget:$dst), "# BRZ",
+                        [(EclipseBrz  GPR:$val, bb:$dst)]>;
+def BRNZ : EclipseInst<(outs), (ins GPR:$val, brtarget:$dst), "# BRNZ",
+                        [(EclipseBrnz GPR:$val, bb:$dst)]>;
+def BGEZ : EclipseInst<(outs), (ins GPR:$val, brtarget:$dst), "# BGEZ",
+                        [(EclipseBgez GPR:$val, bb:$dst)]>;
+def BLTZ : EclipseInst<(outs), (ins GPR:$val, brtarget:$dst), "# BLTZ",
+                        [(EclipseBltz GPR:$val, bb:$dst)]>;
+
+// Unsigned variants: bare "SUB# rhs,lhs" turned out to have a carry
+// output that's independent of lhs/rhs on real Nova/Eclipse hardware (see
+// EclipseAsmPrinter.cpp's BGEU/BLTU case for the full empirical story),
+// so these can't stay a single non-clobbering instruction like
+// BGEZ/BLTZ. Printed by EclipseAsmPrinter as a save-AC2 / copy-lhs /
+// SUBZ-rhs / restore-AC2 / carry-test sequence, borrowing AC2 as scratch
+// exactly like the MUL/DIV pseudo-expansion below — hence `Defs = [AC2]`,
+// even though AC2 is fully restored by the end of the printed sequence
+// (matches MULrr/UDIVrr/UREMrr's own Defs, which declare the same
+// transient clobber for the same save-restore-around-AC2 pattern). $lhs
+// and $rhs themselves are never written — only read — so they need no
+// Defs/tied-outs of their own, avoiding the double-counted-register-
+// demand regression documented on MULrr/UDIVrr/UREMrr above.
+let Defs = [AC2] in {
+def BGEU : EclipseInst<(outs), (ins GPR:$lhs, GPR:$rhs, brtarget:$dst), "# BGEU",
+                        [(EclipseBgeu GPR:$lhs, GPR:$rhs, bb:$dst)]>;
+def BLTU : EclipseInst<(outs), (ins GPR:$lhs, GPR:$rhs, brtarget:$dst), "# BLTU",
+                        [(EclipseBltu GPR:$lhs, GPR:$rhs, bb:$dst)]>;
+}
+}
+
+// Calls go indirectly through a page-zero jump-table slot, exactly like
+// eccc's own `call` (docs/CALLING_CONVENTION.md): JSR's direct displacement
+// can't reach an arbitrarily distant target on real hardware, but
+// page-zero addressing has no such limit. `$func` is already the
+// "<callee>_SLOT" symbol name (EclipseISelLowering.cpp's LowerCall builds
+// it, and EclipseAsmPrinter emits one `var <callee>_SLOT = <callee>`
+// declaration per distinct callee it sees).
+let isCall = 1, Defs = [AC0, AC1, AC3], Uses = [AC2] in
+def CALL : EclipseInst<(outs), (ins calltarget:$func), "JSR @$func,0",
+                        [(EclipseCall texternalsym:$func)]>;
+
+// A genuinely indirect call (callee address only known at runtime -- a
+// real function pointer, not a direct call to a named function) has no
+// symbol to build a "<callee>_SLOT" name from the way CALL's own Pat
+// needs. EclipseISelLowering.cpp's LowerCall handles this by storing the
+// computed address into ONE shared, dedicated page-zero slot
+// ("__indirect_call_SLOT", declared by EclipseAsmPrinter only for a
+// module that actually uses one -- see its own comment) at runtime, via
+// this pseudo, then issuing a perfectly normal CALL through that slot
+// right afterward -- so the only new piece needed here is the store.
+let usesCustomInserter = 1 in
+def CALLSLOT_STORE : EclipsePseudo<(outs), (ins GPR:$src), "# CALLSLOT_STORE", []>;
+
+// AC0 is *not* statically declared here: it's only actually read when the
+// function returns a value, which EclipseISelLowering.cpp's LowerReturn
+// already expresses as a genuine (conditional) extra operand on the
+// EclipseRet node (matched here via SDNPVariadic) — declaring it
+// unconditionally would make void-returning functions falsely claim to
+// read an AC0 nothing ever wrote, tripping the MachineVerifier.
+let isReturn = 1, isTerminator = 1, isBarrier = 1, Uses = [AC3] in
+def RET : EclipseInst<(outs), (ins), "JMP 0,3", [(EclipseRet)]>;
+
+//===----------------------------------------------------------------------===//
+// Interrupt return. The real hardware sequence is exactly two instructions,
+// back to back with nothing in between: `NIOS 077` (INTEN — re-enables
+// interrupts, and the manual's Program Flow Interruption section documents
+// that the processor executes exactly one more instruction before the next
+// interrupt can be taken) immediately followed by `JMP @0` (JMPABSI 0 —
+// resumes the interrupted program via the PC hardware saved into memory
+// location 0 on entry). Modeled as a single post-RA-expanded pseudo, not
+// two independent real instructions, for the same reason MUL/DIV are above:
+// once expanded, nothing (scheduler, spill code) may be inserted between
+// them, and this one is safety-critical, not just a hardware quirk — a
+// stray instruction between INTEN and the jump would run with interrupts
+// already re-enabled but the interrupted program not yet resumed.
+//===----------------------------------------------------------------------===//
+
+// NIOS with the CPU pseudo-device's fixed code (077 octal) baked into the
+// AsmString, exactly like RET bakes AC3's index into "JMP 0,3" above.
+def INTEN : EclipseInst<(outs), (ins), "NIOS 077", []>;
+
+let isPseudo = 1, isReturn = 1, isTerminator = 1, isBarrier = 1 in
+def RETI : EclipseInst<(outs), (ins), "# RETI", [(EclipseRetI)]>;
+
+//===----------------------------------------------------------------------===//
+// MUL / DIV: zero-operand hardware instructions operating implicitly on
+// AC0:AC1:AC2 (multiply-*accumulate*, not plain multiply — see
+// ECLIPSE_ISA_NOTES.md, confirmed the hard way in eccc's own development).
+// Modeled as pseudos with a custom inserter that primes AC0=0, moves
+// operands into AC1/AC2, saves/restores AC2 (our reserved FP) around the
+// call exactly like eccc's func.rs does, and copies the single-word result
+// back out.
+//===----------------------------------------------------------------------===//
+
+// The real zero-operand hardware instructions, operating implicitly on
+// AC0:AC1:AC2 (see the pseudos below for the operand-marshalling sequence
+// around them).
+let Uses = [AC0, AC1, AC2], Defs = [AC0, AC1] in
+def MUL : EclipseInst<(outs), (ins), "MUL", []>;
+let Uses = [AC0, AC1, AC2], Defs = [AC0, AC1] in
+def DIV : EclipseInst<(outs), (ins), "DIV", []>;
+
+// Only unsigned div/rem are real pattern-matched pseudos: signed
+// division is decomposed at the DAG level (EclipseISelLowering.cpp's
+// LowerSDIVREM, Custom-lowering sdiv/srem) into absolute values, one of
+// these, and a conditional negate — see this file's MUL/DIV section
+// header comment for why.
+//
+// Expanded via expandPostRAPseudo (EclipseInstrInfo.cpp), not
+// EmitInstrWithCustomInserter: these pseudos need to temporarily
+// repurpose AC2 (the frame pointer) for the hardware operand convention,
+// and that's only safe to do *after* register allocation has finished —
+// pre-RA expansion would leave ordinary-looking MachineInstrs for the
+// register allocator to freely insert spill/reload code into, and any
+// such code landing inside the AC2-repurposed window would silently
+// compute frame-relative addresses against the wrong base. This was a
+// real, verified-the-hard-way bug (`(a/b)+(a%b)` in one function
+// produced a wrong result until this was fixed to expand post-RA,
+// matching how eccc's own func.rs treats FP save/restore around
+// MUL/DIV as a single uninterruptible unit).
+//
+// Defs lists only AC2: AC0/AC1 are *not* also listed here even though
+// the real expansion clobbers them too, because $dst/$lhs/$rhs (the
+// pseudo's own explicit outs/ins) already exhaust the entire
+// allocatable GPR pool (just AC0/AC1 — see EclipseRegisterInfo.cpp).
+// Redundantly listing them again as implicit Defs made the allocator
+// double-count register demand for this instruction and made it
+// impossible to allocate at all ("ran out of registers") — a second
+// real, verified-the-hard-way bug found immediately after fixing the
+// first one above.
+let Defs = [AC2] in {
+def MULrr : EclipsePseudo<(outs GPR:$dst), (ins GPR:$lhs, GPR:$rhs), "# MUL",
+                           [(set GPR:$dst, (mul GPR:$lhs, GPR:$rhs))]>;
+def UDIVrr : EclipsePseudo<(outs GPR:$dst), (ins GPR:$lhs, GPR:$rhs), "# UDIV",
+                            [(set GPR:$dst, (udiv GPR:$lhs, GPR:$rhs))]>;
+def UREMrr : EclipsePseudo<(outs GPR:$dst), (ins GPR:$lhs, GPR:$rhs), "# UREM",
+                            [(set GPR:$dst, (urem GPR:$lhs, GPR:$rhs))]>;
+}
+// Pat (not itself an instruction) has no `Defs` field, so this must live
+// outside the `let Defs = [AC2] in { ... }` block above.
+def : Pat<(EclipseHalve GPR:$lhs, GPR:$rhs), (UDIVrr GPR:$lhs, GPR:$rhs)>;
+
+// Materializes one of two values based on a comparison, for plain boolean
+// results (`int b = a < c;`) and any `select`/ternary-shaped IR — there's
+// no hardware predicate-select instruction, so this always expands (via
+// EclipseISelLowering.cpp's custom inserter) into an actual two-way branch
+// diamond plus a PHI, using the same skip-test idiom as the real
+// conditional branches above. $condOpc names which of BRZ/BRNZ/BGEZ/BLTZ
+// tests $cmpval.
+let usesCustomInserter = 1 in
+def Select : EclipsePseudo<(outs GPR:$dst),
+                            (ins GPR:$cmpval, i16imm:$condOpc, GPR:$truev,
+                                 GPR:$falsev),
+                            "# SELECT", []>;
+
+// Unsigned counterpart to Select: branches on BGEU/BLTU (two live
+// operands) instead of a single pre-computed $cmpval — see
+// EclipseISD::BGEU's comment. $condOpc names which of BGEU/BLTU tests
+// $lhs/$rhs.
+let usesCustomInserter = 1 in
+def SelectU : EclipsePseudo<(outs GPR:$dst),
+                             (ins GPR:$lhs, GPR:$rhs, i16imm:$condOpc,
+                                  GPR:$truev, GPR:$falsev),
+                             "# SELECTU", []>;
+
+//===----------------------------------------------------------------------===//
+// Misc patterns
+//===----------------------------------------------------------------------===//
+
+// (No zext/sext/anyext patterns needed: i16 is the only integer type this
+// target legalizes to, so there's never a narrower legal type to extend
+// *from* — see EclipseISelLowering.cpp / README.md.)
+
+// (udiv/urem have their own real UDIVrr/UREMrr patterns above now — see
+// the AC0-priming comment there for why signed and unsigned division
+// can't share one pseudo the way an earlier version of this file did.)
diff --git a/llvm/lib/Target/Eclipse/EclipseMCInstLower.cpp b/llvm/lib/Target/Eclipse/EclipseMCInstLower.cpp
new file mode 100644
index 000000000..db157adc9
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseMCInstLower.cpp
@@ -0,0 +1,66 @@
+//===-- EclipseMCInstLower.cpp - Convert MachineInstr to an MCInst ------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseMCInstLower.h"
+#include "llvm/CodeGen/AsmPrinter.h"
+#include "llvm/CodeGen/MachineBasicBlock.h"
+#include "llvm/CodeGen/MachineInstr.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/Support/raw_ostream.h"
+
+using namespace llvm;
+
+MCOperand EclipseMCInstLower::lowerSymbolOperand(const MachineOperand &MO,
+                                                  MCSymbol *Sym) const {
+  const MCExpr *Expr = MCSymbolRefExpr::create(Sym, Ctx);
+  if (MO.getOffset())
+    Expr = MCBinaryExpr::createAdd(
+        Expr, MCConstantExpr::create(MO.getOffset(), Ctx), Ctx);
+  return MCOperand::createExpr(Expr);
+}
+
+void EclipseMCInstLower::Lower(const MachineInstr *MI, MCInst &OutMI) const {
+  OutMI.setOpcode(MI->getOpcode());
+
+  for (const MachineOperand &MO : MI->operands()) {
+    MCOperand MCOp;
+    switch (MO.getType()) {
+    case MachineOperand::MO_Register:
+      if (MO.isImplicit())
+        continue;
+      MCOp = MCOperand::createReg(MO.getReg());
+      break;
+    case MachineOperand::MO_Immediate:
+      MCOp = MCOperand::createImm(MO.getImm());
+      break;
+    case MachineOperand::MO_MachineBasicBlock:
+      MCOp = MCOperand::createExpr(
+          MCSymbolRefExpr::create(MO.getMBB()->getSymbol(), Ctx));
+      break;
+    case MachineOperand::MO_RegisterMask:
+      continue;
+    case MachineOperand::MO_GlobalAddress:
+      MCOp = lowerSymbolOperand(MO, Printer.getSymbol(MO.getGlobal()));
+      break;
+    case MachineOperand::MO_ExternalSymbol:
+      MCOp = lowerSymbolOperand(
+          MO, Printer.GetExternalSymbolSymbol(MO.getSymbolName()));
+      break;
+    case MachineOperand::MO_ConstantPoolIndex:
+      MCOp = lowerSymbolOperand(MO, Printer.GetCPISymbol(MO.getIndex()));
+      break;
+    default:
+      MI->print(errs());
+      llvm_unreachable("unknown Eclipse MachineOperand type");
+    }
+    OutMI.addOperand(MCOp);
+  }
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseMCInstLower.h b/llvm/lib/Target/Eclipse/EclipseMCInstLower.h
new file mode 100644
index 000000000..a89973eca
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseMCInstLower.h
@@ -0,0 +1,36 @@
+//===-- EclipseMCInstLower.h - Lower MachineInstr to MCInst -----*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEMCINSTLOWER_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEMCINSTLOWER_H
+
+#include "llvm/Support/Compiler.h"
+
+namespace llvm {
+class AsmPrinter;
+class MCContext;
+class MCInst;
+class MCOperand;
+class MCSymbol;
+class MachineInstr;
+class MachineOperand;
+
+class LLVM_LIBRARY_VISIBILITY EclipseMCInstLower {
+  MCContext &Ctx;
+  AsmPrinter &Printer;
+
+public:
+  EclipseMCInstLower(MCContext &Ctx, AsmPrinter &Printer)
+      : Ctx(Ctx), Printer(Printer) {}
+  void Lower(const MachineInstr *MI, MCInst &OutMI) const;
+
+  MCOperand lowerSymbolOperand(const MachineOperand &MO, MCSymbol *Sym) const;
+};
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEMCINSTLOWER_H
diff --git a/llvm/lib/Target/Eclipse/EclipseMachineFunctionInfo.h b/llvm/lib/Target/Eclipse/EclipseMachineFunctionInfo.h
new file mode 100644
index 000000000..9ffc1c1fc
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseMachineFunctionInfo.h
@@ -0,0 +1,34 @@
+//===-- EclipseMachineFunctionInfo.h - Eclipse machine func info -*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEMACHINEFUNCTIONINFO_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEMACHINEFUNCTIONINFO_H
+
+#include "llvm/CodeGen/MachineFunction.h"
+
+namespace llvm {
+
+// Tracks the frame index marking where a variadic function's "..."
+// arguments begin, for LowerVASTART (EclipseISelLowering.cpp). Arguments
+// are pushed right-to-left on the flat software stack (see
+// LowerFormalArguments's comment), so the varargs simply start
+// immediately after the last named parameter — no register-save area to
+// model, unlike e.g. x86-64's varargs ABI.
+class EclipseMachineFunctionInfo : public MachineFunctionInfo {
+  int VarArgsFrameIndex = 0;
+
+public:
+  EclipseMachineFunctionInfo(const Function &, const TargetSubtargetInfo *) {}
+
+  int getVarArgsFrameIndex() const { return VarArgsFrameIndex; }
+  void setVarArgsFrameIndex(int FI) { VarArgsFrameIndex = FI; }
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEMACHINEFUNCTIONINFO_H
diff --git a/llvm/lib/Target/Eclipse/EclipseRegisterInfo.cpp b/llvm/lib/Target/Eclipse/EclipseRegisterInfo.cpp
new file mode 100644
index 000000000..10bbd575c
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseRegisterInfo.cpp
@@ -0,0 +1,177 @@
+//===-- EclipseRegisterInfo.cpp - Eclipse Register Information ----------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseRegisterInfo.h"
+#include "EclipseFrameLowering.h"
+#include "EclipseInstrInfo.h"
+#include "llvm/CodeGen/MachineConstantPool.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+#include "llvm/IR/Constants.h"
+
+#define GET_REGINFO_TARGET_DESC
+#include "EclipseGenRegisterInfo.inc"
+
+using namespace llvm;
+
+EclipseRegisterInfo::EclipseRegisterInfo()
+    : EclipseGenRegisterInfo(Eclipse::AC3 /*RA, JSR's implicit target*/) {}
+
+const MCPhysReg *
+EclipseRegisterInfo::getCalleeSavedRegs(const MachineFunction *MF) const {
+  // AC2 (FP) and AC3 (return address) are saved/restored explicitly by
+  // EclipseFrameLowering's prologue/epilogue (mirroring the sibling `eccc`
+  // project's proven convention — see docs/CALLING_CONVENTION.md), not via
+  // the generic callee-saved-register spill mechanism.
+  static const MCPhysReg NoRegs[] = {0};
+  // An `__attribute__((interrupt))` handler is entered asynchronously, not
+  // via a normal call site, so there's no caller-side spill code protecting
+  // AC0/AC1 across it the way there would be across an ordinary call.
+  // Treating them as callee-saved here makes the generic prologue/epilogue
+  // inserter spill/reload whichever of the two the handler body actually
+  // clobbers, using the same frame-slot machinery already used for regular
+  // register-allocator spills.
+  static const MCPhysReg InterruptRegs[] = {Eclipse::AC0, Eclipse::AC1, 0};
+  if (MF && MF->getFunction().hasFnAttribute("interrupt"))
+    return InterruptRegs;
+  return NoRegs;
+}
+
+BitVector EclipseRegisterInfo::getReservedRegs(const MachineFunction &MF) const {
+  BitVector Reserved(getNumRegs());
+  // Only AC0/AC1 are available to the allocator: AC2 is the frame pointer,
+  // AC3 holds JSR's return address. Four accumulators total, two already
+  // spoken for — see docs/CALLING_CONVENTION.md.
+  Reserved.set(Eclipse::AC2);
+  Reserved.set(Eclipse::AC3);
+  return Reserved;
+}
+
+bool EclipseRegisterInfo::eliminateFrameIndex(MachineBasicBlock::iterator II,
+                                               int /*SPAdj*/,
+                                               unsigned FIOperandNum,
+                                               RegScavenger * /*RS*/) const {
+  MachineInstr &MI = *II;
+  int FI = MI.getOperand(FIOperandNum).getIndex();
+  MachineFunction &MF = *MI.getParent()->getParent();
+  const MachineFrameInfo &MFI = MF.getFrameInfo();
+
+  // EclipseFrameLowering assigns every stack object an FP-relative
+  // displacement up front (its processFunctionBeforeFrameFinalized),
+  // matching eccc's frame.rs formula exactly: parameters at FP+2.., locals
+  // /temps at FP-cumulative-word-count.
+  int64_t Offset = MFI.getObjectOffset(FI);
+
+  if (MI.getOpcode() == Eclipse::LEAFI) {
+    // No load-effective-address instruction, and no immediate-operand
+    // instructions at all on this ISA (see EclipseInstrInfo.td) — an
+    // address-of-local genuinely needs: copy FP, materialize the now-known
+    // offset via a fresh constant-pool entry, add it in. This is the one
+    // place the offset is concrete, so it's also the one place this
+    // pseudo can be expanded.
+    MachineBasicBlock &MBB = *MI.getParent();
+    const auto &TII = *MF.getSubtarget().getInstrInfo();
+    MachineRegisterInfo &MRI = MF.getRegInfo();
+    DebugLoc DL = MI.getDebugLoc();
+    Register Dst = MI.getOperand(0).getReg();
+
+    unsigned CPI = MF.getConstantPool()->getConstantPoolIndex(
+        ConstantInt::get(
+            Type::getInt16Ty(MF.getFunction().getContext()), Offset, true),
+        Align(2));
+    Register OffReg = MRI.createVirtualRegister(&Eclipse::GPRRegClass);
+    BuildMI(MBB, MI, DL, TII.get(Eclipse::LDABS), OffReg)
+        .addConstantPoolIndex(CPI);
+    BuildMI(MBB, MI, DL, TII.get(Eclipse::MOVrr), Dst).addReg(Eclipse::AC2);
+    BuildMI(MBB, MI, DL, TII.get(Eclipse::ADDrr), Dst)
+        .addReg(OffReg)
+        .addReg(Dst);
+    MI.eraseFromParent();
+    return true; // MI is gone; caller must not touch it further.
+  }
+
+  // LDFI/STFI bake index=2 (AC2/FP-relative) into their AsmString with an
+  // 8-bit signed displacement field (dgasm enforces -128..127 -- a real
+  // Nova ISA hardware limit on register-relative addressing, not a dgasm
+  // choice), so the common case is just substituting the resolved
+  // displacement in place of the operand. A function with a large enough
+  // frame (e.g. sizeable local arrays, or just many locals) can need an
+  // offset past that range, which dgasm then rejects at assemble time
+  // ("Address out of range") -- confirmed empirically on several tests
+  // that build clean output otherwise. When that happens, fall back to
+  // the exact same two-part trick already used for reaching arbitrarily
+  // large data elsewhere on this backend (see LEAFI just above, and the
+  // page-zero-indirect-global-addressing comment on LDIND/STIND below):
+  // materialize the absolute address (FP + offset, LEAFI's own sequence)
+  // into a register, then go through it *indirectly* via the shared
+  // `_scratch` page-zero word (LDABSI/STABSI, exactly emitIndirectMem's
+  // shape) instead of baking the out-of-range displacement directly.
+  if ((MI.getOpcode() == Eclipse::LDFI || MI.getOpcode() == Eclipse::STFI) &&
+      (Offset < -128 || Offset > 127)) {
+    MachineBasicBlock &MBB = *MI.getParent();
+    const auto &TII = *MF.getSubtarget().getInstrInfo();
+    MachineRegisterInfo &MRI = MF.getRegInfo();
+    DebugLoc DL = MI.getDebugLoc();
+    bool IsLoad = MI.getOpcode() == Eclipse::LDFI;
+
+    // A load can safely reuse its own destination register as scratch
+    // for address materialization since nothing needs to survive from
+    // before the final indirect load overwrites it. A store's source
+    // register holds the value being stored and must survive untouched,
+    // so it needs a genuinely fresh register — resolved by the post-RA
+    // register scavenger the same way LEAFI's own OffReg already is
+    // (see EclipseRegisterInfo.h's requiresRegisterScavenging comment).
+    // Unlike LEAFI, this doesn't need a *second* temp register to hold a
+    // copy of AC2 before adding: AC2 is reserved from the allocator's
+    // perspective but still an ordinary member of the GPR reg class (see
+    // EclipseRegisterInfo.td), so it can be ADDrr's own source operand
+    // directly. That keeps the store case down to exactly two live
+    // registers throughout (Src plus this one scratch) rather than
+    // three (Src, an offset temp, and a separate address-accumulator
+    // temp) — the latter doesn't fit this target's two-GPR budget and
+    // the scavenger can't spill+restore two simultaneously-live
+    // registers through its one emergency slot ("Cannot scavenge
+    // register without an emergency spill slot!", confirmed empirically
+    // when this used a second temp).
+    Register AddrReg = IsLoad ? MI.getOperand(0).getReg()
+                               : MRI.createVirtualRegister(&Eclipse::GPRRegClass);
+
+    unsigned CPI = MF.getConstantPool()->getConstantPoolIndex(
+        ConstantInt::get(
+            Type::getInt16Ty(MF.getFunction().getContext()), Offset, true),
+        Align(2));
+    BuildMI(MBB, MI, DL, TII.get(Eclipse::LDABS), AddrReg)
+        .addConstantPoolIndex(CPI);
+    BuildMI(MBB, MI, DL, TII.get(Eclipse::ADDrr), AddrReg)
+        .addReg(Eclipse::AC2)
+        .addReg(AddrReg);
+
+    BuildMI(MBB, MI, DL, TII.get(Eclipse::STABS))
+        .addReg(AddrReg)
+        .addExternalSymbol("_scratch");
+    if (IsLoad) {
+      BuildMI(MBB, MI, DL, TII.get(Eclipse::LDABSI), MI.getOperand(0).getReg())
+          .addExternalSymbol("_scratch");
+    } else {
+      BuildMI(MBB, MI, DL, TII.get(Eclipse::STABSI))
+          .addReg(MI.getOperand(0).getReg())
+          .addExternalSymbol("_scratch");
+    }
+    MI.eraseFromParent();
+    return true; // MI is gone; caller must not touch it further.
+  }
+
+  MI.getOperand(FIOperandNum).ChangeToImmediate(Offset);
+  return false;
+}
+
+Register EclipseRegisterInfo::getFrameRegister(const MachineFunction & /*MF*/) const {
+  return Eclipse::AC2;
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseRegisterInfo.h b/llvm/lib/Target/Eclipse/EclipseRegisterInfo.h
new file mode 100644
index 000000000..fa703158a
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseRegisterInfo.h
@@ -0,0 +1,48 @@
+//===-- EclipseRegisterInfo.h - Eclipse Register Info ----------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSEREGISTERINFO_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSEREGISTERINFO_H
+
+#include "llvm/CodeGen/TargetRegisterInfo.h"
+
+#define GET_REGINFO_HEADER
+#include "EclipseGenRegisterInfo.inc"
+
+namespace llvm {
+
+struct EclipseRegisterInfo : public EclipseGenRegisterInfo {
+  EclipseRegisterInfo();
+
+  const MCPhysReg *
+  getCalleeSavedRegs(const MachineFunction *MF) const override;
+  BitVector getReservedRegs(const MachineFunction &MF) const override;
+
+  bool eliminateFrameIndex(MachineBasicBlock::iterator MI, int SPAdj,
+                            unsigned FIOperandNum,
+                            RegScavenger *RS = nullptr) const override;
+
+  Register getFrameRegister(const MachineFunction &MF) const override;
+
+  // LEAFI's post-regalloc expansion (see eliminateFrameIndex) needs a
+  // genuine scratch physical register it can't just conjure. Both flags
+  // are required together: requiresRegisterScavenging is what actually
+  // gets PEI to construct a RegScavenger at all; requiresFrameIndexScavenging
+  // is what tells it to run that scavenger over the virtual registers
+  // eliminateFrameIndex leaves behind (see PrologEpilogInserter.cpp).
+  bool requiresRegisterScavenging(const MachineFunction &MF) const override {
+    return true;
+  }
+  bool requiresFrameIndexScavenging(const MachineFunction &MF) const override {
+    return true;
+  }
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSEREGISTERINFO_H
diff --git a/llvm/lib/Target/Eclipse/EclipseRegisterInfo.td b/llvm/lib/Target/Eclipse/EclipseRegisterInfo.td
new file mode 100644
index 000000000..fb7731dbb
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseRegisterInfo.td
@@ -0,0 +1,42 @@
+//===- EclipseRegisterInfo.td - Eclipse Register defs -----*- tablegen -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// The Nova/Eclipse has exactly four 16-bit accumulators, AC0-AC3, encoded in
+// a 2-bit field in every instruction that names one. There is no separate
+// general-purpose register file distinct from the accumulators, and no
+// condition-code register: comparisons are synthesized from a subtraction
+// plus a "skip next word if AC is zero/non-negative" test on an accumulator
+// (see docs/CALLING_CONVENTION.md in the eccc project this backend's ISA
+// knowledge comes from).
+//
+// AC2 and AC3 are permanently reserved by this backend's calling convention
+// (mirroring the sibling `eccc` Rust compiler's own proven convention,
+// verified against a real simulator):
+//   AC2 = frame pointer (FP) for the currently executing function.
+//   AC3 = clobbered by JSR (holds the return address); saved/restored by
+//         every non-leaf function's prologue/epilogue.
+// so only AC0/AC1 are available to the register allocator as scratch.
+//===----------------------------------------------------------------------===//
+
+class EclipseReg<bits<2> num, string n> : Register<n> {
+  field bits<2> Num;
+  let Num = num;
+  let Namespace = "Eclipse";
+}
+
+def AC0 : EclipseReg<0, "0">, DwarfRegNum<[0]>;
+def AC1 : EclipseReg<1, "1">, DwarfRegNum<[1]>;
+def AC2 : EclipseReg<2, "2">, DwarfRegNum<[2]>;
+def AC3 : EclipseReg<3, "3">, DwarfRegNum<[3]>;
+
+// Every Nova/Eclipse word (and hence every accumulator) is 16 bits.
+// AC0/AC1 listed first: the register allocator tries registers in list
+// order, and AC2/AC3 are marked reserved in EclipseRegisterInfo.cpp anyway,
+// but listing the actually-allocatable pair first keeps allocation output
+// readable.
+def GPR : RegisterClass<"Eclipse", [i16], 16, (add AC0, AC1, AC2, AC3)>;
diff --git a/llvm/lib/Target/Eclipse/EclipseSubtarget.cpp b/llvm/lib/Target/Eclipse/EclipseSubtarget.cpp
new file mode 100644
index 000000000..f61fe3b65
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseSubtarget.cpp
@@ -0,0 +1,57 @@
+//===-- EclipseSubtarget.cpp - Eclipse Subtarget Information -------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseSubtarget.h"
+
+#define DEBUG_TYPE "eclipse-subtarget"
+
+#define GET_SUBTARGETINFO_TARGET_DESC
+#define GET_SUBTARGETINFO_CTOR
+#include "EclipseGenSubtargetInfo.inc"
+
+using namespace llvm;
+
+// ParseSubtargetFeatures itself is generated by GET_SUBTARGETINFO_CTOR
+// above, driven by Eclipse.td's FeatureEIS/ProcessorModel definitions —
+// it sets HasEIS (EclipseSubtarget.h) true only for CPU "eclipse_s140",
+// leaving it false (the default) for "generic" and the base-Nova CPUs.
+
+EclipseSubtarget::EclipseSubtarget(const Triple &TT, StringRef CPU,
+                                    StringRef FS, const TargetMachine &TM)
+    : EclipseGenSubtargetInfo(TT, CPU, /*TuneCPU*/ CPU, FS),
+      InstrInfo(*this), FrameLowering(*this), TLInfo(TM, *this) {
+  ParseSubtargetFeatures(CPU.empty() ? "generic" : CPU,
+                          CPU.empty() ? "generic" : CPU, FS);
+}
+
+// See this method's declaration in EclipseSubtarget.h for why this exists
+// separately from EclipseTargetLowering's constructor's own
+// setLibcallImpl calls — both are needed, each reaching a different
+// LibcallLoweringInfo copy consulted by a different part of float
+// softening. Only f32 (`float`) is wired; `double` stays unimplemented
+// (see eclipse_rt.c's soft-float section).
+void EclipseSubtarget::initLibcallLoweringInfo(LibcallLoweringInfo &Info) const {
+  Info.setLibcallImpl(RTLIB::ADD_F32, RTLIB::impl___addsf3);
+  Info.setLibcallImpl(RTLIB::SUB_F32, RTLIB::impl___subsf3);
+  Info.setLibcallImpl(RTLIB::MUL_F32, RTLIB::impl___mulsf3);
+  Info.setLibcallImpl(RTLIB::DIV_F32, RTLIB::impl___divsf3);
+  // See the matching setLibcallImpl block in EclipseISelLowering.cpp's
+  // constructor for why these use the FCMP3_PRED_* (three-way -1/0/1)
+  // family rather than the plain OEQ_F32/UNE_F32/... boolean family.
+  Info.setLibcallImpl(RTLIB::FCMP3_PRED_OEQ_F32, RTLIB::impl___eqsf2);
+  Info.setLibcallImpl(RTLIB::FCMP3_PRED_UNE_F32, RTLIB::impl___nesf2);
+  Info.setLibcallImpl(RTLIB::FCMP3_PRED_OLT_F32, RTLIB::impl___ltsf2);
+  Info.setLibcallImpl(RTLIB::FCMP3_PRED_OLE_F32, RTLIB::impl___lesf2);
+  Info.setLibcallImpl(RTLIB::FCMP3_PRED_OGT_F32, RTLIB::impl___gtsf2);
+  Info.setLibcallImpl(RTLIB::FCMP3_PRED_OGE_F32, RTLIB::impl___gesf2);
+  Info.setLibcallImpl(RTLIB::UO_F32, RTLIB::impl___unordsf2);
+  Info.setLibcallImpl(RTLIB::SINTTOFP_I32_F32, RTLIB::impl___floatsisf);
+  Info.setLibcallImpl(RTLIB::UINTTOFP_I32_F32, RTLIB::impl___floatunsisf);
+  Info.setLibcallImpl(RTLIB::FPTOSINT_F32_I32, RTLIB::impl___fixsfsi);
+  Info.setLibcallImpl(RTLIB::FPTOUINT_F32_I32, RTLIB::impl___fixunssfsi);
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseSubtarget.h b/llvm/lib/Target/Eclipse/EclipseSubtarget.h
new file mode 100644
index 000000000..55491eaa3
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseSubtarget.h
@@ -0,0 +1,83 @@
+//===-- EclipseSubtarget.h - Define Subtarget for the Eclipse -*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSESUBTARGET_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSESUBTARGET_H
+
+#include "EclipseFrameLowering.h"
+#include "EclipseISelLowering.h"
+#include "EclipseInstrInfo.h"
+#include "llvm/CodeGen/LibcallLoweringInfo.h"
+#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+
+#define GET_SUBTARGETINFO_HEADER
+#include "EclipseGenSubtargetInfo.inc"
+
+namespace llvm {
+
+class EclipseSubtarget : public EclipseGenSubtargetInfo {
+  EclipseInstrInfo InstrInfo;
+  EclipseFrameLowering FrameLowering;
+  EclipseTargetLowering TLInfo;
+  SelectionDAGTargetInfo TSInfo;
+
+  // Set by ParseSubtargetFeatures (TableGen-generated, driven by
+  // FeatureEIS in Eclipse.td) whenever the selected CPU has the Eclipse
+  // Extended Instruction Set — concretely, native IOR/XOR. False for the
+  // base-Nova CPUs (nova1/nova3/nova4): dgasm accepts eclipse_s140's IOR/
+  // XOR mnemonics but rejects them outright ("unrecognised instruction")
+  // for those three, confirmed empirically. PerformDAGCombine
+  // (EclipseISelLowering.cpp) checks this to synthesize OR/XOR from
+  // COM/AND instead of emitting IORrr/XORrr when it's false.
+  bool HasEIS = false;
+
+public:
+  EclipseSubtarget(const Triple &TT, StringRef CPU, StringRef FS,
+                    const TargetMachine &TM);
+
+  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);
+
+  bool hasEIS() const { return HasEIS; }
+
+  const EclipseInstrInfo *getInstrInfo() const override { return &InstrInfo; }
+  const TargetFrameLowering *getFrameLowering() const override {
+    return &FrameLowering;
+  }
+  const EclipseRegisterInfo *getRegisterInfo() const override {
+    return &InstrInfo.getRegisterInfo();
+  }
+  const EclipseTargetLowering *getTargetLowering() const override {
+    return &TLInfo;
+  }
+  const SelectionDAGTargetInfo *getSelectionDAGInfo() const override {
+    return &TSInfo;
+  }
+
+  // The soft-float libcall names EclipseTargetLowering's constructor sets
+  // via TargetLowering::setLibcallImpl only reach TargetLowering's own
+  // (separate!) LibcallLoweringInfo copy — SelectionDAG's own copy
+  // (DAG.getLibcalls(), which softenSetCCOperands's selectFPCmpLibcall
+  // uses to *choose* between candidate comparison libcalls before the
+  // final makeLibCall) is built independently, straight from the
+  // architecture-keyed default table, via this exact hook. "eclipse"
+  // isn't a recognized architecture there, so without overriding this,
+  // that selection falls through to the generic three-way FCMP3_F32
+  // libcall, which nothing implements — confirmed empirically ("unsupported
+  // library call operation" crashing inside softenSetCCOperands even
+  // after wiring every individual comparison predicate on
+  // EclipseTargetLowering itself). See EclipseSubtarget.cpp's
+  // implementation for the actual mappings (kept in sync with
+  // EclipseISelLowering.cpp's constructor by hand — both need the same
+  // set, since each is consulted by a different softening code path).
+  void initLibcallLoweringInfo(LibcallLoweringInfo &Info) const override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSESUBTARGET_H
diff --git a/llvm/lib/Target/Eclipse/EclipseTargetMachine.cpp b/llvm/lib/Target/Eclipse/EclipseTargetMachine.cpp
new file mode 100644
index 000000000..ea2bc5d2c
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseTargetMachine.cpp
@@ -0,0 +1,99 @@
+//===-- EclipseTargetMachine.cpp - Define TargetMachine for Eclipse -----===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseTargetMachine.h"
+
+#include "Eclipse.h"
+#include "EclipseMachineFunctionInfo.h"
+#include "TargetInfo/EclipseTargetInfo.h"
+#include "llvm/CodeGen/Passes.h"
+#include "llvm/CodeGen/TargetLoweringObjectFileImpl.h"
+#include "llvm/CodeGen/TargetPassConfig.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/Target/TargetOptions.h"
+#include <optional>
+
+using namespace llvm;
+
+// This target's real output is Nova/Eclipse-S140 assembly *text* (see
+// README.md), not an object file — sections/directives in the usual ELF
+// sense don't apply. EclipseAsmPrinter drives module-level emission (the
+// `org`/`dev`/`var`/jump-table preamble, per-function labels) itself
+// directly, bypassing per-global section selection entirely, so which
+// concrete TargetLoweringObjectFile this is doesn't actually matter — ELF
+// is reused here only because it's a ready-made concrete (non-abstract)
+// implementation, not because this target produces ELF output.
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeEclipseTarget() {
+  RegisterTargetMachine<EclipseTargetMachine> X(getTheEclipseTarget());
+  PassRegistry &PR = *PassRegistry::getPassRegistry();
+  initializeEclipseDAGToDAGISelLegacyPass(PR);
+  initializeEclipseAsmPrinterPass(PR);
+}
+
+static std::string computeDataLayout() {
+  // Big-endian (docs/ECLIPSE_ISA_NOTES.md: 32-bit MUL/DIV results treat
+  // AC0 as the *high* word), 16-bit pointers, i16 the native/natural
+  // integer width. There is no 8-bit-addressable byte on real hardware —
+  // this backend promotes everything narrower than i16 up to i16
+  // (mirroring the sibling eccc project's "char is one 16-bit word"
+  // simplification, docs/LIMITATIONS.md) — but LLVM's memory model always
+  // has an 8-bit "byte" as its base unit regardless, so object sizes as
+  // seen by MachineFrameInfo etc. are still real bytes; this backend's own
+  // frame-offset code (EclipseFrameLowering.cpp) converts those to
+  // 16-bit-word counts itself where it matters.
+  return "E-p:16:16-i16:16-i32:16-f32:16-f64:16-n16";
+}
+
+static Reloc::Model getEffectiveRelocModel(std::optional<Reloc::Model> RM) {
+  return RM.value_or(Reloc::Static);
+}
+
+EclipseTargetMachine::EclipseTargetMachine(
+    const Target &T, const Triple &TT, StringRef CPU, StringRef FS,
+    const TargetOptions &Options, std::optional<Reloc::Model> RM,
+    std::optional<CodeModel::Model> CM, CodeGenOptLevel OL, bool JIT)
+    : CodeGenTargetMachineImpl(T, computeDataLayout(), TT, CPU, FS, Options,
+                                getEffectiveRelocModel(RM),
+                                getEffectiveCodeModel(CM, CodeModel::Small),
+                                OL),
+      Subtarget(TT, CPU, FS, *this),
+      TLOF(std::make_unique<TargetLoweringObjectFileELF>()) {
+  initAsmInfo();
+}
+
+namespace {
+class EclipsePassConfig : public TargetPassConfig {
+public:
+  EclipsePassConfig(EclipseTargetMachine &TM, PassManagerBase &PM)
+      : TargetPassConfig(TM, PM) {}
+
+  EclipseTargetMachine &getEclipseTargetMachine() const {
+    return getTM<EclipseTargetMachine>();
+  }
+
+  bool addInstSelector() override {
+    addPass(createEclipseISelDagLegacyPass(getEclipseTargetMachine()));
+    return false;
+  }
+};
+} // namespace
+
+TargetPassConfig *
+EclipseTargetMachine::createPassConfig(PassManagerBase &PM) {
+  return new EclipsePassConfig(*this, PM);
+}
+
+MachineFunctionInfo *EclipseTargetMachine::createMachineFunctionInfo(
+    BumpPtrAllocator &Allocator, const Function &F,
+    const TargetSubtargetInfo *STI) const {
+  return EclipseMachineFunctionInfo::create<EclipseMachineFunctionInfo>(
+      Allocator, F, STI);
+}
diff --git a/llvm/lib/Target/Eclipse/EclipseTargetMachine.h b/llvm/lib/Target/Eclipse/EclipseTargetMachine.h
new file mode 100644
index 000000000..d9958b5ad
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/EclipseTargetMachine.h
@@ -0,0 +1,46 @@
+//===-- EclipseTargetMachine.h - Define TargetMachine for Eclipse -*- C++ -*-//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_ECLIPSETARGETMACHINE_H
+#define LLVM_LIB_TARGET_ECLIPSE_ECLIPSETARGETMACHINE_H
+
+#include "EclipseSubtarget.h"
+#include "llvm/CodeGen/CodeGenTargetMachineImpl.h"
+#include <optional>
+
+namespace llvm {
+
+class EclipseTargetMachine : public CodeGenTargetMachineImpl {
+  EclipseSubtarget Subtarget;
+  std::unique_ptr<TargetLoweringObjectFile> TLOF;
+
+public:
+  EclipseTargetMachine(const Target &T, const Triple &TT, StringRef CPU,
+                        StringRef FS, const TargetOptions &Options,
+                        std::optional<Reloc::Model> RM,
+                        std::optional<CodeModel::Model> CM,
+                        CodeGenOptLevel OL, bool JIT);
+
+  const EclipseSubtarget *getSubtargetImpl(const Function &) const override {
+    return &Subtarget;
+  }
+
+  TargetPassConfig *createPassConfig(PassManagerBase &PM) override;
+
+  TargetLoweringObjectFile *getObjFileLowering() const override {
+    return TLOF.get();
+  }
+
+  MachineFunctionInfo *
+  createMachineFunctionInfo(BumpPtrAllocator &Allocator, const Function &F,
+                             const TargetSubtargetInfo *STI) const override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_ECLIPSETARGETMACHINE_H
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/CMakeLists.txt b/llvm/lib/Target/Eclipse/MCTargetDesc/CMakeLists.txt
new file mode 100644
index 000000000..680a56f33
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/CMakeLists.txt
@@ -0,0 +1,13 @@
+add_llvm_component_library(LLVMEclipseDesc
+  EclipseInstPrinter.cpp
+  EclipseMCAsmInfo.cpp
+  EclipseMCTargetDesc.cpp
+
+  LINK_COMPONENTS
+  EclipseInfo
+  MC
+  Support
+
+  ADD_TO_COMPONENT
+  Eclipse
+)
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseInstPrinter.cpp b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseInstPrinter.cpp
new file mode 100644
index 000000000..0fc7b5ea8
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseInstPrinter.cpp
@@ -0,0 +1,48 @@
+//===-- EclipseInstPrinter.cpp - Convert MCInst to dgasm syntax ----------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseInstPrinter.h"
+#include "llvm/MC/MCAsmInfo.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCInst.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "asm-printer"
+
+#include "EclipseGenAsmWriter.inc"
+
+// Register "names" are bare accumulator numbers ("0".."3"), matching real
+// Nova/Eclipse assembly (e.g. "ADD 1,0"), not a "%"-prefixed syntax.
+void EclipseInstPrinter::printRegName(raw_ostream &OS, MCRegister Reg) {
+  OS << getRegisterName(Reg);
+}
+
+void EclipseInstPrinter::printInst(const MCInst *MI, uint64_t Address,
+                                    StringRef Annotation,
+                                    const MCSubtargetInfo & /*STI*/,
+                                    raw_ostream &OS) {
+  printInstruction(MI, Address, OS);
+  printAnnotation(OS, Annotation);
+}
+
+// dgasm parses numbers decimal-by-default (see EclipseMCAsmInfo.cpp /
+// eccc's crates/eccc-codegen/src/symbolic.rs), so plain immediates print
+// decimal here, not hex/octal.
+void EclipseInstPrinter::printOperand(const MCInst *MI, unsigned OpNo,
+                                       raw_ostream &OS) {
+  const MCOperand &Op = MI->getOperand(OpNo);
+  if (Op.isReg()) {
+    printRegName(OS, Op.getReg());
+  } else if (Op.isImm()) {
+    OS << Op.getImm();
+  } else {
+    assert(Op.isExpr() && "Expected an expression");
+    MAI.printExpr(OS, *Op.getExpr());
+  }
+}
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseInstPrinter.h b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseInstPrinter.h
new file mode 100644
index 000000000..c66967b7f
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseInstPrinter.h
@@ -0,0 +1,36 @@
+//===-- EclipseInstPrinter.h - Convert MCInst to dgasm syntax --*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEINSTPRINTER_H
+#define LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEINSTPRINTER_H
+
+#include "llvm/MC/MCInstPrinter.h"
+
+namespace llvm {
+
+class EclipseInstPrinter : public MCInstPrinter {
+public:
+  EclipseInstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
+                      const MCRegisterInfo &MRI)
+      : MCInstPrinter(MAI, MII, MRI) {}
+
+  void printInst(const MCInst *MI, uint64_t Address, StringRef Annot,
+                 const MCSubtargetInfo &STI, raw_ostream &O) override;
+  void printOperand(const MCInst *MI, unsigned OpNo, raw_ostream &O);
+  void printRegName(raw_ostream &OS, MCRegister Reg) override;
+
+  // Autogenerated by tblgen.
+  std::pair<const char *, uint64_t>
+  getMnemonic(const MCInst &MI) const override;
+  void printInstruction(const MCInst *MI, uint64_t Address, raw_ostream &O);
+  static const char *getRegisterName(MCRegister Reg);
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEINSTPRINTER_H
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCAsmInfo.cpp b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCAsmInfo.cpp
new file mode 100644
index 000000000..d1f25587a
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCAsmInfo.cpp
@@ -0,0 +1,33 @@
+//===-- EclipseMCAsmInfo.cpp - Eclipse asm properties --------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseMCAsmInfo.h"
+#include "llvm/TargetParser/Triple.h"
+
+using namespace llvm;
+
+EclipseMCAsmInfo::EclipseMCAsmInfo(const Triple & /*TT*/,
+                                    const MCTargetOptions &Options)
+    : MCAsmInfo(Options) {
+  // Matches Connor Wood's `dgasm` (github.com/CWood1/dgasm) Eclipse-S140
+  // grammar, confirmed against its real lexer/parser by the sibling `eccc`
+  // project (crates/eccc-codegen/src/symbolic.rs) rather than assumed.
+  IsLittleEndian = false;
+  CodePointerSize = CalleeSaveStackSlotSize = 2;
+  CommentString = "//";
+  LabelSuffix = ":";
+  ZeroDirective = nullptr;
+  Data8bitsDirective = nullptr; // no sub-word data directive is used
+  Data16bitsDirective = "\tvar\t";
+  Data32bitsDirective = nullptr;
+  HasDotTypeDotSizeDirective = false;
+  HasFunctionAlignment = false;
+  HasSingleParameterDotFile = false; // no GNU-assembler ".file" in dgasm's grammar
+  SupportsDebugInformation = false; // no DWARF story for this target yet
+  UseIntegratedAssembler = false; // real output goes through dgasm, not us
+}
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCAsmInfo.h b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCAsmInfo.h
new file mode 100644
index 000000000..b331a8d2e
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCAsmInfo.h
@@ -0,0 +1,29 @@
+//===-- EclipseMCAsmInfo.h - Eclipse asm properties ------------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEMCASMINFO_H
+#define LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEMCASMINFO_H
+
+#include "llvm/MC/MCAsmInfo.h"
+
+namespace llvm {
+class Triple;
+
+// Not ELF: this backend emits plain Nova/Eclipse-S140-style assembly text
+// (see README.md), the same textual target the sibling `eccc` project's
+// SymbolicBackend produces for Connor Wood's `dgasm` cross-assembler, not
+// an object-file format. Directives below match what a human-written
+// dgasm source file looks like, not a Unix assembler.
+class EclipseMCAsmInfo : public MCAsmInfo {
+public:
+  explicit EclipseMCAsmInfo(const Triple &TT, const MCTargetOptions &Options);
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEMCASMINFO_H
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCTargetDesc.cpp b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCTargetDesc.cpp
new file mode 100644
index 000000000..3d0cb73c6
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCTargetDesc.cpp
@@ -0,0 +1,70 @@
+//===-- EclipseMCTargetDesc.cpp - Eclipse Target Descriptions -----------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "EclipseMCTargetDesc.h"
+#include "EclipseInstPrinter.h"
+#include "EclipseMCAsmInfo.h"
+#include "TargetInfo/EclipseTargetInfo.h"
+#include "llvm/MC/MCInstrInfo.h"
+#include "llvm/MC/MCRegisterInfo.h"
+#include "llvm/MC/MCSubtargetInfo.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/TargetParser/Triple.h"
+
+#define GET_INSTRINFO_MC_DESC
+#include "EclipseGenInstrInfo.inc"
+
+#define GET_SUBTARGETINFO_MC_DESC
+#include "EclipseGenSubtargetInfo.inc"
+
+#define GET_REGINFO_MC_DESC
+#include "EclipseGenRegisterInfo.inc"
+
+using namespace llvm;
+
+static MCInstrInfo *createEclipseMCInstrInfo() {
+  MCInstrInfo *X = new MCInstrInfo();
+  InitEclipseMCInstrInfo(X);
+  return X;
+}
+
+static MCRegisterInfo *createEclipseMCRegisterInfo(const Triple & /*TT*/) {
+  MCRegisterInfo *X = new MCRegisterInfo();
+  // AC3 doubles as the return-address register (JSR clobbers it).
+  InitEclipseMCRegisterInfo(X, Eclipse::AC3, 0, 0, 0);
+  return X;
+}
+
+static MCSubtargetInfo *
+createEclipseMCSubtargetInfo(const Triple &TT, StringRef CPU, StringRef FS) {
+  return createEclipseMCSubtargetInfoImpl(TT, CPU.empty() ? "generic" : CPU,
+                                           /*TuneCPU*/ CPU.empty() ? "generic"
+                                                                    : CPU,
+                                           FS);
+}
+
+static MCInstPrinter *createEclipseMCInstPrinter(const Triple & /*T*/,
+                                                  unsigned SyntaxVariant,
+                                                  const MCAsmInfo &MAI,
+                                                  const MCInstrInfo &MII,
+                                                  const MCRegisterInfo &MRI) {
+  if (SyntaxVariant == 0)
+    return new EclipseInstPrinter(MAI, MII, MRI);
+  return nullptr;
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeEclipseTargetMC() {
+  Target &T = getTheEclipseTarget();
+  RegisterMCAsmInfo<EclipseMCAsmInfo> X(T);
+  TargetRegistry::RegisterMCInstrInfo(T, createEclipseMCInstrInfo);
+  TargetRegistry::RegisterMCRegInfo(T, createEclipseMCRegisterInfo);
+  TargetRegistry::RegisterMCSubtargetInfo(T, createEclipseMCSubtargetInfo);
+  TargetRegistry::RegisterMCInstPrinter(T, createEclipseMCInstPrinter);
+}
diff --git a/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCTargetDesc.h b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCTargetDesc.h
new file mode 100644
index 000000000..801225763
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/MCTargetDesc/EclipseMCTargetDesc.h
@@ -0,0 +1,39 @@
+//===-- EclipseMCTargetDesc.h - Eclipse Target Descriptions ----*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This backend is text-asm-only (see README.md): no MCCodeEmitter,
+// MCAsmBackend, or ELFObjectWriter are registered, since it never produces
+// an object file, only assembly text meant for a real Nova/Eclipse
+// assembler (dgasm) downstream. `llc -filetype=obj` is unsupported; use
+// `-filetype=asm`.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEMCTARGETDESC_H
+#define LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEMCTARGETDESC_H
+
+#include "llvm/Support/DataTypes.h"
+
+namespace llvm {
+class Target;
+
+Target &getTheEclipseTarget();
+
+} // namespace llvm
+
+#define GET_REGINFO_ENUM
+#include "EclipseGenRegisterInfo.inc"
+
+#define GET_INSTRINFO_ENUM
+#define GET_INSTRINFO_MC_HELPER_DECLS
+#include "EclipseGenInstrInfo.inc"
+
+#define GET_SUBTARGETINFO_ENUM
+#include "EclipseGenSubtargetInfo.inc"
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_MCTARGETDESC_ECLIPSEMCTARGETDESC_H
diff --git a/llvm/lib/Target/Eclipse/README.md b/llvm/lib/Target/Eclipse/README.md
new file mode 100644
index 000000000..d9be9f54c
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/README.md
@@ -0,0 +1,227 @@
+# The Eclipse LLVM backend
+
+An out-of-tree-style LLVM target (`lib/Target/Eclipse`, built via
+`LLVM_EXPERIMENTAL_TARGETS_TO_BUILD=Eclipse`) for the Data General
+Nova/Eclipse minicomputer family — a 16-bit, 4-accumulator, no-immediate-
+operand ISA from the 1970s. Structurally modeled on `lib/Target/Lanai`
+(LLVM's own recommended template for bringing up a new backend).
+
+This backend's ISA knowledge (register roles, addressing idioms, the
+calling convention, the `MUL`/`DIV` multiply-accumulate/divide
+convention, `dgasm`'s real assembly grammar) comes from a sibling project,
+`eccc` — a hand-written Rust C compiler targeting the same machine — whose
+`docs/ECLIPSE_ISA_NOTES.md` and `docs/CALLING_CONVENTION.md` verified every
+non-obvious fact by actually running it on `eclipseemu` (a real Data
+General Eclipse simulator) rather than trusting memory of the historical
+architecture. Where this backend goes beyond what eccc verified (i32,
+floats, structs), that's called out explicitly below rather than presented
+as equally solid.
+
+## Output format
+
+This backend emits **assembly text**, not object code: real Nova/
+Eclipse-S140 source for Connor Wood's
+[`dgasm`](https://github.com/CWood1/dgasm) cross-assembler — the same
+dialect eccc's own `SymbolicBackend`/`--dgasm` flag produces. There is no
+`MCCodeEmitter`/`MCAsmBackend`/`ELFObjectWriter`; `llc -filetype=obj` is
+not supported, only `-filetype=asm`. Assemble the result with:
+
+```
+dgasm -t eclipse_s140 -f simh -o out.simh out.s
+```
+
+and run it the same way eccc's own README documents (`eclipseemu` under
+WSL's `simh` package, or real hardware via `dgasm -f eclipse`).
+
+## Design decisions
+
+- **Registers**: AC0-AC3, matching real hardware. AC2 is permanently the
+  frame pointer, AC3 permanently holds `JSR`'s return address (both
+  reserved, unallocatable) — so only AC0/AC1 are available to the register
+  allocator. This mirrors eccc's own conclusion that "there was never a
+  register allocator worth building" for a 4-accumulator machine, though
+  this backend *does* run LLVM's real allocator over the remaining two,
+  spilling to frame slots under pressure rather than eccc's simpler
+  everything-is-a-memory-temp approach.
+- **i16 is the only legal type.** No 8-bit byte-addressing exists on this
+  ISA; every C type narrower than `int` promotes to i16 (mirrors eccc's
+  "char is one 16-bit word" simplification). i32 and all floating-point
+  types get no register class at all, so LLVM's generic type legalizer
+  handles them on its own: i32 `add`/`sub`/bitwise/shift/compare decompose
+  automatically into i16 pairs (its default behavior for an
+  oversized-but-unregistered integer type), and i32 `mul`/`div`/`rem` plus
+  every float operation fall back to runtime libcalls (`__muldi3`-style).
+  **Those runtime routines don't exist yet** — see "Known limitations."
+- **No immediate-operand instructions exist on the base ISA at all.**
+  Every constant this backend needs — a literal, a computed frame offset
+  for `&local`, a global's address — goes through a `var`/constant-pool
+  data word, loaded with `LDA`. See `EclipseInstrInfo.td`'s file header
+  and `EclipseRegisterInfo::eliminateFrameIndex`'s `LEAFI` handling.
+  Real hardware backs this up: Nova/Eclipse never had an ADDI-style
+  instruction either.
+- **Comparisons/branches**: every one reduces to a subtraction plus one of
+  four skip-then-jump conditional branches (`MOV#`/`MOVL#` +
+  `SZR`/`SNR`/`SZC`/`SNC` + `JMP`) — the same technique
+  `docs/CALLING_CONVENTION.md` verified. Unlike eccc (which always emits
+  both the true and false arm as separate `JMP`s, since it does no block
+  layout), this backend uses the tighter skip-and-fall-through form and
+  implements real `analyzeBranch`/`insertBranch` so LLVM's branch-folding
+  and layout passes can reason about it normally.
+- **Calls go through a page-zero jump table** (`var <callee>_SLOT =
+  <callee>`, `JSR @<callee>_SLOT,0`), exactly like eccc: a direct `JSR`'s
+  signed displacement can't reach an arbitrarily distant function on real
+  hardware, but page-zero addressing has no such limit.
+- **The software stack** (`_SP`, a plain page-zero word manipulated with
+  `ISZ`/`DSZ` plus one level of indirect addressing) and the
+  prologue/epilogue sequence (push caller's FP and return address, adopt
+  the new top as FP, reserve locals+temps; every function, including
+  leaves, uses the same sequence) are a direct port of
+  `docs/CALLING_CONVENTION.md`'s verified design. The real hardware
+  `PSH`/`POP`/`SAVE`/`RTN` stack instructions are not used — eccc found
+  they expect an OS-managed frame environment this bare-metal target
+  doesn't have. `LowerCall` (`EclipseISelLowering.cpp`) passes `0`, not
+  the argument count, as `CALLSEQ_START`'s reservation size — `PUSH`
+  (`emitPushPop`) already reserves its own word per argument via its own
+  `DSZABS` before storing, so reserving the same count again via
+  `ADJCALLSTACKDOWN` double-decremented `_SP` per argument, a leak
+  `ADJCALLSTACKUP` never fully undid. Invisible in a single shallow call
+  (nothing after it depended on the exact depth), it compounded across
+  nested/looped calls — e.g. `puts()` calling `putchar()` once per
+  character — until it clobbered `AC2`/`AC3` (frame pointer / return
+  address) badly enough to silently skip code or return through a
+  garbage address. Found via `eclipse_rt.c`'s `puts()`, which was the
+  first code in this project with that call shape.
+
+## Working end to end
+
+This has been built against a real llvm-project checkout and verified
+against real `dgasm` + `eclipseemu` runs (not just `.ll` hand-written
+tests) for all of the following:
+
+- **Full C frontend**: `clang -cc1 -triple eclipse-dg-none` works —
+  `clang/lib/Basic/Targets/Eclipse.{h,cpp}` and
+  `clang/lib/CodeGen/Targets/Eclipse.cpp` register a real `TargetInfo`/
+  `TargetCodeGenInfo`, so this is not "backend only, hand-write your own
+  `.ll`" — ordinary C source compiles.
+- **`char`/`i8`** loads, stores, and sign-extension (the ABI's
+  signext-return convention) — words are the only addressable unit on
+  this ISA, so `char` is a full 16-bit word underneath, but the C-visible
+  8-bit truncation/extension semantics are real (`LowerSIGN_EXTEND_INREG`
+  in `EclipseISelLowering.cpp`).
+- **`<<`/`>>`**, including arithmetic right-shift of negative values —
+  built from self-add/UDIV/SDIV-with-floor-correction loops
+  (`LowerShift`), since the ISA exposes no shift/rotate-with-writeback
+  instruction. Constant shift counts only.
+- **Signed and unsigned `*`/`/`/`%`.** `MUL`/`DIV` are zero-operand
+  hardware instructions operating on `AC0:AC1:AC2`; expansion happens
+  *post*-register-allocation (`EclipseInstrInfo::expandPostRAPseudo`), not
+  via the usual pre-RA custom inserter, because `AC2` doubles as the frame
+  pointer and the expansion has to be atomic with respect to spill-code
+  insertion — see the comment there for the corruption bug this fixes.
+  Signed division decomposes into abs-value + unsigned division +
+  conditional negate (`LowerSDIVREM`) rather than sign-priming the
+  hardware divide, since that only works for the unsigned case.
+- **Address-of-global** (`&global`, string literals used as `char *`,
+  array-to-pointer decay) via `LEAGA` in `EclipseInstrInfo.td`: since `LDA`
+  always dereferences and there are no immediate-operand instructions,
+  taking an address materializes an auxiliary page-zero pointer word
+  (`var <name>_PTR = <name>`), the same indirection `CALL` already uses
+  for function addresses. Address-of-*local* (`&x` for a stack variable)
+  uses the separate `LEAFI` pseudo.
+- **Device I/O via inline asm** (`asm volatile("DOAS %0,011" :: "r"(c))`)
+  — the escape hatch for `SKPDN`/`SKPBN`/`DIAC`/`DOAS`/`NIO*`, which have
+  no ordinary-C mapping. `eclipse-toolchain/rt/eclipse_rt.c` wraps the
+  verified idiom (docs/IO_DEVICES.md in the sibling eccc project) as real
+  `putchar`/`getchar`/`puts`/`printf`/`scanf`, so most programs never need
+  to write inline asm themselves — `eclipse-toolchain/eclipse-cc` links it
+  in automatically. All confirmed against a real running `eclipseemu`,
+  including `scanf`/`getchar` with genuine live keyboard input (needs a
+  real pty in raw mode with `go`, not `step` — a piped/batch harness or a
+  canonical-mode pty can't deliver a keystroke while the simulator is
+  actually executing, only once it's back at the idle `sim>` prompt,
+  where it's misread as a command instead).
+- **Multi-file programs.** dgasm has no linker, so
+  `eclipse-toolchain/eclipse-cc` compiles each source (plus the runtime
+  library) to `.ll` separately and merges them with `llvm-link` before a
+  single `llc` run — the normal way to get a program made of more than
+  one `.c` file.
+- **Page-zero placement.** `LDA`/`STA ac,addr` default to page-zero
+  (absolute, 8-bit displacement) addressing, and dgasm assigns addresses
+  strictly in textual source order — so `var` declarations that must
+  actually *be in page-zero* (the constant pool, the call/address/
+  long-jump pointer slots) have to precede all code in the final file,
+  regardless of which function emitted them, while a global's *actual
+  content* (no longer page-zero-resident at all — see "page-zero holds
+  pointers, not data" below) is moved to the very *end* instead, after
+  all code. `eclipse-toolchain/reorder_asm.py` does this as a pure text
+  pass after `llc` (also sanitizing LLVM's `.str`-style anonymous-
+  constant names, which dgasm's identifier grammar rejects, and
+  selectively relaxing any internal jump that ends up out of dgasm's
+  8-bit PC-relative range) — deliberately *not* solved inside
+  `AsmPrinter` itself; see `EclipseAsmPrinter.h`'s header comment for
+  why.
+
+## Known limitations
+
+Recorded here deliberately, the same way eccc's own `docs/LIMITATIONS.md`
+does, rather than silently leaving gaps for someone to rediscover:
+
+- **`float` (IEEE-754 single precision) has a real soft-float runtime now**
+  (`eclipse_rt.c`'s `__addsf3`/`__subsf3`/`__mulsf3`/`__divsf3`, all six
+  comparison libcalls, and int<->float conversions) — verified correct on
+  `eclipseemu` individually and in small combinations. `double` (f64)
+  still has no runtime implementation (would need genuine 64-bit integer
+  support this backend doesn't have — see the `__muldi3`/i64 bullet
+  below) and no `i32` (`__muldi3` etc.) runtime either — programs needing
+  those will fail to *link* (no linker in this pipeline — dgasm resolves
+  symbols within one source file), not silently miscompile. Combining
+  *many* substantial float operations in one program (e.g. all four
+  arithmetic ops plus all six comparisons at once) can exceed the shared
+  256-word page-zero budget (see the budget bullet below) even though
+  each operation is individually correct — this is a real capacity limit
+  of the real hardware, not a correctness bug.
+- **No hand-verified Eclipse FPU opcodes.** Deliberately: eccc's own docs
+  explicitly refused to guess unverified ISA facts, and this backend
+  follows the same rule rather than fabricating `FAD`/`FSUB`/etc.
+  encodings from general historical knowledge of the Nova/Eclipse FPU
+  option. Wiring up real hardware float support (as an alternative to the
+  soft-float path above) is future work requiring the same
+  `eclipseemu`-verification methodology as `ECLIPSE_ISA_NOTES.md`.
+- **Variable shift counts are not supported** (`LowerShift` handles
+  constant counts only — `report_fatal_error`s otherwise).
+- **No tail calls, single-word return values only.** (Varargs *are*
+  supported — real `va_start`/`va_arg`/`va_end`, which is what
+  `printf`/`scanf` are built on.)
+- **Not relocatable.** Globals/constants/the jump table all live at
+  compiler-chosen absolute page-zero addresses, baked in at compile time —
+  fundamentally position-*dependent*, same as eccc. `org 0100` in
+  `EclipseAsmPrinter::emitStartOfAsmFile` is just a convenient default
+  address for paper-tape/console loading, not a relocation base; making
+  the output truly relocatable would mean reworking global/constant
+  addressing to go through a runtime-computed base instead.
+- **Page-zero holds pointers, not data — fixed from an earlier, real
+  budget problem.** `LDA`/`STA ac,addr` only has an 8-bit displacement
+  (page-zero, 0-255 words), and this backend originally stored every
+  global's/string's *actual content* there directly, meaning the whole
+  program's data shared one 256-word budget — confirmed to fail outright
+  on nothing fancier than `printf` + `string.h` + `ctype.h` + `stdlib.h`
+  together in one file. Fixed by routing every global access through
+  indirection instead (`EclipseInstrInfo.td`'s "page-zero indirect data
+  addressing" comment): a global's actual content now lives wherever
+  `reorder_asm.py` places it (no longer forced to the front), and
+  page-zero holds only a one-word pointer to it — composed from two
+  already-proven, independent pieces (`LEAGA` materializes the address,
+  `LDIND`/`STIND`'s existing register-pointer-dereference pattern reads
+  through it), not a new addressing mode. Page-zero cost per global
+  dropped from "one word per word of content" to exactly one word,
+  regardless of size; the previously-failing `printf`+`string.h`+
+  `ctype.h`+`stdlib.h` combination now compiles, assembles, and runs
+  correctly (verified). `eclipse-cc`'s `opt -passes=internalize,globaldce`
+  step (strips unused runtime-library functions before `llc`) remains in
+  the pipeline as a complementary, independent optimization. Constant-pool
+  entries (`tconstpool`) deliberately still use direct page-zero
+  addressing, not this indirection — they're already exactly one word
+  each, so indirecting them would add overhead with no page-zero savings.
+  The tradeoff: every global access now costs one extra instruction (the
+  address materialization) plus one extra memory read (through the
+  shared `_scratch` word), versus the old direct scheme.
diff --git a/llvm/lib/Target/Eclipse/TargetInfo/CMakeLists.txt b/llvm/lib/Target/Eclipse/TargetInfo/CMakeLists.txt
new file mode 100644
index 000000000..78e9504f0
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/TargetInfo/CMakeLists.txt
@@ -0,0 +1,10 @@
+add_llvm_component_library(LLVMEclipseInfo
+  EclipseTargetInfo.cpp
+
+  LINK_COMPONENTS
+  MC
+  Support
+
+  ADD_TO_COMPONENT
+  Eclipse
+  )
diff --git a/llvm/lib/Target/Eclipse/TargetInfo/EclipseTargetInfo.cpp b/llvm/lib/Target/Eclipse/TargetInfo/EclipseTargetInfo.cpp
new file mode 100644
index 000000000..26acb5550
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/TargetInfo/EclipseTargetInfo.cpp
@@ -0,0 +1,25 @@
+//===-- EclipseTargetInfo.cpp - Eclipse Target Implementation -----------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "TargetInfo/EclipseTargetInfo.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+
+using namespace llvm;
+
+Target &llvm::getTheEclipseTarget() {
+  static Target TheEclipseTarget;
+  return TheEclipseTarget;
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeEclipseTargetInfo() {
+  RegisterTarget<Triple::eclipse> X(getTheEclipseTarget(), "eclipse",
+                                     "Data General Nova/Eclipse (16-bit)",
+                                     "Eclipse");
+}
diff --git a/llvm/lib/Target/Eclipse/TargetInfo/EclipseTargetInfo.h b/llvm/lib/Target/Eclipse/TargetInfo/EclipseTargetInfo.h
new file mode 100644
index 000000000..a52149a91
--- /dev/null
+++ b/llvm/lib/Target/Eclipse/TargetInfo/EclipseTargetInfo.h
@@ -0,0 +1,28 @@
+//===-- EclipseTargetInfo.h - Eclipse Target Implementation ---*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM
+// Exceptions. See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// Out-of-tree LLVM backend for the Data General Nova/Eclipse minicomputer
+// family. See llvm/lib/Target/Eclipse/README.md for the ISA facts and design
+// decisions this backend is built on (all empirically verified against a
+// real `eclipseemu` simulator by the sibling `eccc` project, not taken from
+// memory of the historical architecture).
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_ECLIPSE_TARGETINFO_ECLIPSETARGETINFO_H
+#define LLVM_LIB_TARGET_ECLIPSE_TARGETINFO_ECLIPSETARGETINFO_H
+
+namespace llvm {
+
+class Target;
+
+Target &getTheEclipseTarget();
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_ECLIPSE_TARGETINFO_ECLIPSETARGETINFO_H
diff --git a/llvm/lib/TargetParser/Triple.cpp b/llvm/lib/TargetParser/Triple.cpp
index 1e421c428..b83637d83 100644
--- a/llvm/lib/TargetParser/Triple.cpp
+++ b/llvm/lib/TargetParser/Triple.cpp
@@ -78,6 +78,8 @@ StringRef Triple::getArchTypeName(ArchType Kind) {
     return "kalimba";
   case lanai:
     return "lanai";
+  case eclipse:
+    return "eclipse";
   case loongarch32:
     return "loongarch32";
   case loongarch64:
@@ -346,6 +348,8 @@ StringRef Triple::getArchTypePrefix(ArchType Kind) {
     return "kalimba";
   case lanai:
     return "lanai";
+  case eclipse:
+    return "eclipse";
   case shave:
     return "shave";
   case wasm32:
@@ -513,6 +517,7 @@ Triple::ArchType Triple::getArchTypeForLLVMName(StringRef Name) {
       .Case("spirv64", spirv64)
       .Case("kalimba", kalimba)
       .Case("lanai", lanai)
+      .Case("eclipse", eclipse)
       .Case("shave", shave)
       .Case("wasm32", wasm32)
       .Case("wasm64", wasm64)
@@ -664,6 +669,7 @@ Triple::ArchType Triple::parseArch(StringRef ArchName) {
                  Triple::spirv64)
           .StartsWith("kalimba", Triple::kalimba)
           .Case("lanai", Triple::lanai)
+          .Case("eclipse", Triple::eclipse)
           .Case("renderscript32", Triple::renderscript32)
           .Case("renderscript64", Triple::renderscript64)
           .Case("shave", Triple::shave)
@@ -972,6 +978,7 @@ static Triple::ObjectFormatType getDefaultFormat(const Triple &T) {
   case Triple::hsail:
   case Triple::kalimba:
   case Triple::lanai:
+  case Triple::eclipse:
   case Triple::loongarch32:
   case Triple::loongarch64:
   case Triple::m68k:
@@ -1732,6 +1739,7 @@ unsigned Triple::getArchPointerBitWidth(llvm::Triple::ArchType Arch) {
 
   case llvm::Triple::avr:
   case llvm::Triple::msp430:
+  case llvm::Triple::eclipse:
     return 16;
 
   case llvm::Triple::aarch64_32:
@@ -1956,6 +1964,7 @@ Triple Triple::get64BitArchVariant() const {
   case Triple::hexagon:
   case Triple::kalimba:
   case Triple::lanai:
+  case Triple::eclipse:
   case Triple::m68k:
   case Triple::msp430:
   case Triple::r600:
@@ -2156,6 +2165,7 @@ Triple Triple::getLittleEndianArchVariant() const {
   switch (getArch()) {
   case Triple::UnknownArch:
   case Triple::lanai:
+  case Triple::eclipse:
   case Triple::sparcv9:
   case Triple::systemz:
   case Triple::m68k:
